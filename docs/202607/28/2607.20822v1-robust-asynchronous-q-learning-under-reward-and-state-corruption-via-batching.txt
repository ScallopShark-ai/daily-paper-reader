Title: Robust Asynchronous Q-Learning under Reward and State Corruption via Batching

URL Source: https://arxiv.org/pdf/2607.20822v1

Published Time: Fri, 24 Jul 2026 00:22:34 GMT

Number of Pages: 9

Markdown Content:
# Robust Asynchronous Q-Learning under Reward and State Corruption via Batching 

Sreejeet Maity and Aritra Mitra 

Abstract — Motivated by reinforcement learning in harsh environments, we consider the problem of learning an optimal policy subject to adversarially corrupted feedback. Specifically, at each time-step, an adversary can perturb both the reward and state observations of the learner following the Huber contamination model. To defend against such data corruption, we propose BR-Async-Q : a novel, epoch-based, robust Q-learning algorithm built upon two key ideas: (i) partitioning the online data stream into batches to reduce variance, and (ii) constructing robust estimates of the Bellman optimality operator using such batched data. We prove a high-probability 

ℓ∞ error bound for BR-Async-Q that matches that for vanilla 

Q-learning, up to a small additive term that scales with the fraction of corrupted samples. To our knowledge, this provides the first robustness guarantee for asynchronous Q-learning sub-ject to both reward and state corruption. Furthermore, when only rewards are corrupted, the dependence of our algorithm’s bound on the corruption fraction is minimax optimal. 

I. I NTRODUCTION 

Reinforcement learning (RL) has emerged as a powerful framework for sequential decision-making, with successful applications spanning healthcare, recommendation systems, autonomous driving, and robotics. At its core, an agent repeatedly interacts with an unknown environment, observes rewards and successor states, and uses this feedback to im-prove its policy and maximize long-term return. In practice, however, this feedback is often imperfect : rewards may be noisy, biased, or adversarially manipulated, and observed next states may be distorted by sensing errors, logging failures, or targeted perturbations. Such imperfections are particularly harmful in sequential learning algorithms where corrupted feedback-induced biases can compound over time, generating poor decision-making policies. 

The Setting. Motivated by the gap between idealized and noisy feedback, we study discounted infinite-horizon RL under adversarially corrupted data . We consider learning from an online trajectory generated by a behavior policy, as in Q-learning [1], but allow an adversary to corrupt both the realized reward and the observed next state at each step, thereby distorting the learner’s update signal. We model cor-ruption using the classical Huber model [2], [3]: at each time step, the observed next state and reward can be arbitrarily 

perturbed with a small corruption probability ε ∈ [0 , 1/2) .

Moreover, we even allow the clean reward distributions to be heavy-tailed, requiring them to admit no more than a second moment. This makes it particularly challenging for the learner to distinguish between deliberately corrupted           

> The authors are with the Department of Electrical and Com-puter Engineering, North Carolina State University. Email: {smaity2, amitra2 }@ncsu.edu .

reward signals and clean rewards generated from the tails of reward distributions. Subject to our observation model, which captures severe noise, sensing failures, and deliberate poisoning, we ask: Is it still possible to reliably learn an optimal policy? As we discuss below, there are several gaps in our understanding of this question. 

Related Work. Although robustness in sequential decision-making has been extensively studied for simpler settings such as stochastic bandits [4], [5], much less is known for the online, infinite-horizon, discounted RL setting we consider in this paper. A very recent line of work [6]– [8] has initiated the study of robust RL under reward-only corruption, assuming perfect state-feedback at all times. However, even for this weaker corruption model, there is a fundamental gap between the upper and lower bounds derived in [6], [7]; we explain this gap in Section II. Further-more, prior work has left it unclear how to simultaneously control the effects of reward and state corruptions. In this paper, we propose a novel approach that resolves both the aforementioned issues. Our contributions are as follows. 

• Algorithmic Contribution. We develop BR-Async-Q 

in Section IV, an epoch-based, robust variant of Q-learning, that can handle asynchronous online sampling and joint corruption of states and rewards. Our approach partitions the online data stream into batches (epochs) and relies on computing a robust, low-variance estimate of the Bellman optimality operator within each epoch. Unlike standard Q-learning [1] and prior robust variants thereof [6], [8] which update the Q-table at all time steps, BR-Async-Q updates the Q-table infrequently, only once per epoch, but using 

variance-reduced update directions. It is precisely variance-reduction via batching that enables us to close the gap between the upper and lower bounds in prior work [6], [7]. 

• Theoretical Contribution. In Theorem 2 of Section V, we prove a high-probability finite-time ℓ∞-error bound for 

BR-Async-Q , showing that one can still converge “close” to the optimal state-action value function. Given T samples, our bound features a statistical error term on the order of 

˜O(1 /√T ), which, in fact, sharpens even the standard Q-learning bound in [9] in terms of its dependence on the visitation probability of the least-visited state-action pair. Our bound also exhibits a small unavoidable additive bias term that scales with the corruption probability. When only rewards are corrupted, our bias term is minimax optimal, 

exactly matching the fundamental lower bound in [6]. Thus, our work not only improves existing finite-time rates, but also provides the first provable guarantee of robustness for 

Q-learning under the joint state and reward corruption model. arXiv:2607.20822v1 [cs.LG] 23 Jul 2026 II. B ACKGROUND AND PROBLEM FORMULATION 

We begin with the standard RL background and then describe the problem of interest. Consider a γ-discounted infinite-horizon Markov Decision Process (MDP) M =(S, A, P, R, γ ), where S and A are finite state and action spaces, P is the probability transition kernel, R is the mean reward function, and γ ∈ (0 , 1) is the discount factor. When in state-action pair (s, a ), the learner observes a next state s′ ∼ P (· | s, a ) and a stochastic reward r(s, a ) ∼R(s, a ) drawn from the reward distribution R(s, a ), such that 

E[r(s, a )] = R(s, a ) and E[( r(s, a ) − R(s, a )) 2] = σ2(s, a ).We assume that the reward means and variances are bounded, i.e., ∃ ¯R, ¯σ ≥ 1 such that |R(s, a )| ≤ ¯R and σ2(s, a ) ≤ ¯σ2

for all (s, a ) ∈ S × A . We define ˜σ := max { ¯R, ¯σ}.A policy μ : S → ∆( A) assigns to each state adistribution over actions. The quality of a policy μ is captured by an expected discounted infinite-horizon cumu-lative reward known as the value function V μ, defined as        

> Vμ(s) = E
> "∞X
> t=0
> γtR(st, a t)s0=s, μ
> #
> ,

where st and at are the state and action at time t, respectively, under the action of the policy μ on the MDP M. The goal of the learner is to find a policy μ that maximizes the value function V μ for all states, 

without knowledge of the transition kernels P and reward functions R of the underlying MDP. To explain how this is done, we will need to introduce the notion of a state-action value function Qμ for a policy μ, defined as     

> Qμ(s, a ) = E
> "∞X
> t=0
> γtR(st, a t)(s0, a 0) = ( s, a ), μ
> #
> .

(1) The celebrated Q-learning algorithm [1] uses data collected by a suitable behavior/sampling policy μ to iteratively main-tain an estimate of the optimal state-action value function, denoted by Q∗. It turns out that Q∗ is the fixed point of a contractive operator T known as the Bellman optimal-ity operator [10]. Using this contraction property, classical asymptotic results [11], [12] established that the sequence of iterates generated by Q-learning converges to Q∗ almost surely under suitable assumptions on μ. More recently, finite-time rates have been established [9], [13], [14], revealing that when run for T iterations, the final iterate of Q-learning converges to Q∗ at a rate of ˜O(1 /√T ), with high probability. 

Adversarially Corrupted Observation Model. As is standard in Q-learning [14], [15], we assume that data are generated under a stochastic behavior policy μ satisfying 

μ(a | s) > 0 for all (s, a ). In the nominal model, the learner observes at ∼ μ(· | st), st+1 ∼ P(· | st, a t),and rt(st, a t) ∼ R (st, a t). Here, we assume that the noise process {R(st, a t)−rt(st, a t)} is independent over time and of all other sources of randomness. Our formulation departs from standard Q-learning in two key aspects. First, unlike prior work which either requires the rewards to be deter-ministic or “light-tailed” (e.g., sub-Gaussian), we impose nothing beyond the reward distributions R(s, a ) admitting a finite second moment, i.e., we allow for heavy-tailed rewards. Second, the learner’s feedback can be adversarially corrupted according to the classical Huber contamination model [2]. To explain this corruption model, let {Yt, 1}t≥0 and 

{Yt, 2}t≥0 be two i.i.d. Bernoulli ( Bern ) sequences with parameters εR, ε Y ∈ [0 , 1/2) , respectively, independent of the past and of all other sources of randomness. Note, however, that the processes {Yt, 1}t≥0 and {Yt, 2}t≥0 might be mutually correlated. At each time step t, the learner observes (st, a t), and then receives an observed reward ert

and an observed next state est+1 that are generated as follows: 

ert(st, a t) = (1 − Yt, 1) rt(st, a t) + Yt, 1 zt,

est+1 = (1 − Yt, 2) st+1 + Yt, 2 ut, (2) where Yt, 1i.i.d.

∼ Bern (εR), Yt, 2i.i.d.

∼ Bern (εY ), at ∼ μ(· | 

st), st+1 ∼ P (· | st, a t), and rt(st, a t) ∼ R (st, a t). Here, 

zt is an arbitrary (possibly unbounded) adversarial reward value, and ut ∈ S is an arbitrary adversarially chosen next state. Both zt and ut may depend on the entire history up to time t. In simple words, at each time-step t, with probability 

1 − εR (resp., 1 − εY ), the learner observes a noisy reward (resp., next state) from the true reward distribution R(st, a t)

(resp., true state distribution P(·| st, a t)), and with probability 

εR (resp., εY ), an arbitrary reward (resp., next state). Our problem of interest can now be summarized as follows. 

Problem 1. Given T samples {(st, a t, ˜st+1 , ˜rt(st, a t)) }T −1

> t=0

generated under the adversarial model in (2) , and a failure probability δ ∈ (0 , 1) , our goal is to construct a robust estimate ˆQ of the optimal state-action value function Q∗, and derive a high-probability bound on the ℓ∞-error ∥ ˆQ−Q∗∥∞

that holds with probability at least 1 − δ.

Challenges and Research Gaps. The standard Q-learning update [1] uses a “bootstrapping” mechanism where the current estimate of the Q-table is used to approximate the “reward-to-go”. The joint reward and state observation corruption model in (2) not only introduces a bias in the current reward but also in such a bootstrapped estimate. Unless accounted for carefully, this bias might accumulate over time, leading to vacuous bounds. Indeed, even when only rewards are perturbed, such a phenomenon is reported in [8]. As far as we are aware, the more challenging joint corruption model we study here has not been explored at all in prior Q-learning literature. Furthermore, restricted to the simpler reward corruption model (i.e., when εY =0) under asynchronous sampling, the upper-bounds derived in [6] exhibit a corruption-induced O(√εR) term that scales inversely with the visitation probability of the least-visited state-action pair. This seems to suggest that if the data-generation behavior policy μ causes certain state-action pairs to be visited infrequently, then the effect of reward corruption can become pronounced. Interestingly, however, [6] estab-lishes a fundamental information-theoretic lower bound on the order Ω( √εR) that does not exhibit any dependence at all on visitation probabilities. Thus, prior work has left it unclear how to close the aforementioned gap between upper and lower bounds. In this paper, we develop a novel robust 

Q-learning algorithm that not only closes this gap, but also handles the joint corruption model. To clearly explain the main technical ideas behind our approach, we focus on the tabular setting in this paper. Even for this setting, a non-trivial technical analysis is needed to handle the interplay between asynchronous sampling, heavy-tailed reward distributions, and joint corruptions of states and rewards. To proceed, we make the following assumption that is standard in prior RL work [9], [16]–[19]. 

Assumption 1. The Markov chain induced by the behavior policy μ is aperiodic and irreducible. 

Let π denote the stationary distribution of the Markov chain induced by μ. Assumption 1 ensures that π(s) > 0

for all s ∈ S . Thus, the stationary visitation probability of any state-action pair (s, a ) is λ(s, a ) := π(s)μ(a|s) > 0. We also define the minimum visitation probability by λmin := min (s,a )∈S×A λ(s, a ). As is fairly common [17], [20]–[22], we assume that at each time step t, the state st is sampled 

independently from π. Throughout the paper, we will refer to this as the “i.i.d. data generation model”. This assumption is made solely for ease of presentation, since, as shown in [6], [7], the analysis extends naturally to the single-trajectory Markovian setting via blocking arguments [23]. III. R OBUST ESTIMATION UNDER HUBER CORRUPTION 

Before presenting our proposed algorithm in Section IV, we first describe a key building block, namely Algorithm 1, which enables robust mean estimation under Huber con-tamination. To that end, consider a data set D consisting of M i.i.d. samples of a scalar random variable X with mean μX and variance σ2 

> X

. We consider the contamination model in (2), under which each sample in D is independently corrupted with probability ε, and remains clean otherwise. Let D′ denote the resulting corrupted sample set. Note that 

D′ contains εM corrupted samples on average; the realized number of corruptions is, however, a random variable. To estimate μX robustly from D′, we use a slightly modified version of the trimmed mean estimator from [24], where the modification accounts for a random number of corrupted samples. We partition D′ into two equal halves, D1

and D2. The first half is used to construct lower and upper cutoff levels (a, b), and the second half is used to compute a clipped empirical mean over this interval; see lines 1-3 of Algorithm 1. The resulting robust estimator is denoted by 

ˆμH 

> X

= TRIM H[D′, ε, δ ]. (3) When the clean samples are known to be bounded in the interval (Γ , Γ) , one can obtain a tighter dependence on the corruption fraction ε by setting the cutoff levels as (a, b) = (Γ , Γ) . In this case, the resulting estimator is given by 

ˆμB 

> X

= TRIM B[D′, (Γ , Γ)] = 1

> M
> X
> Xi∈D ′

ϕΓ,Γ(Xi), (4) where for a < b, we define the function ϕa,b(x) := min {max {x, a}, b}, ∀x ∈ R, and D′ = {Xi}i∈[M ]. Before proceeding to analyze the estimators in (3) and (4), we note that for the quantiles in Line 1 of Algorithm 1 to be well defined, the object ζ computed in Algorithm 1 must be a fraction, implicitly imposing that ¯ε ≤ 1/8.1 We will work under this implicit assumption on ¯ε throughout the paper. It is worth emphasizing that such an assumption is also implicitly made in the work of Lugosi [24], and papers that have leveraged similar algorithms in other contexts [25]. 

Algorithm 1 Robust Mean under Huber Corruption ( TRIM H)

Require: Contaminated dataset D′ = {X1, X 2, . . . , X M } =

D1 ⊕ D 2, where |D 1| = |D 2| = M/ 2; contamination probability ε; confidence level δ. Define ¯ε := 3 ε/ 2 + 16 log(8 /δ )/M, ζ := 8¯ ε + 24 log(8 /δ )/M.  

> 1:

Let X∗ 

> 1

≤ X∗ 

> 2

≤ · · · ≤ X∗ 

> M/ 2

represent a non-decreasing arrangement of D1. Compute quantiles :

a = X∗

> ζM/ 2

, b = X∗

> (1 −ζ)M/ 2

, where a < b. 

> 2:

Define the function ϕa,b(x) := min {max {x, a}, b}. 

> 3:

Output ˆμH 

> X

= 2

> M
> X
> Xi∈D 2

ϕa,b(Xi).

The following result will play a key role in our subsequent algorithmic design and analysis. 

Theorem 1. (Robust Mean under Huber contamination) 

Let δ ∈ (0 , 1) satisfy δ ≥ 8e−M/ 2. There exists a universal constant C ≥ 1 such that, with probability at least 1 − δ, the estimate ˆμH 

> X

in (3) satisfies the following bound: 

|ˆμH 

> X

− μX | ≤ C σX

√ε +

q log(8 /δ )

> M



. (5) 

Moreover, if X ∈ [Γ , Γ] , then the estimate ˆμB 

> X

in (4) satisfies the following bound with probability at least 1 − δ,

|ˆμB 

> X

− μX | ≤ ¯c  Γ − Γ 

ε +

q log(8 /δ )

> M



, (6) 

where ¯c ≥ 1 is a universal constant. 

The proof of the claim in (5) is a minor modification of that in [24], and can be found in [6, Appendix D]. Establishing the claim in (6) requires a somewhat different argument that we defer to Appendix A. 

Remark 1. It is instructive to compare the bound in (5) 

with that in (6) . While the former exhibits a dependence of 

O(√ε), the latter has a sharper dependence of O(ε). This improvement is made possible by using the known bounds on the random variable to inform the trimming process. We will exploit this idea in our algorithm design in the next section. 

IV. T HE PROPOSED ALGORITHM 

The standard model-free Q-learning algorithm [1] and its robust counterparts in [6], [8] have the following feature in common: they update the Q-table at every time-step as soon as a new data sample arrives. Since an update is made using just a single sample, the update direction has a high variance, making it more vulnerable to corruption. Thus, as alluded to in Sections I and II, the resulting guarantees in [6] are loose, and fail to match the fundamental lower bound. To   

> 1The object ζis defined to account for the concentration of (i) empirical quantiles around true quantiles, and (ii) the number of corrupted samples around its mean value.

mitigate this challenge, we depart fundamentally from prior approaches and develop a novel, epoch-based robust variant of asynchronous Q-learning that uses online batching of data to generate robust estimates under adversarial feedback. Our algorithm, Batch Robust Asynchronous Q-Learning ,abbreviated as BR-Async-Q , is formally described in Al-gorithm 2. At a high level, BR-Async-Q partitions the interaction horizon T into K epochs (batches) of length H

each, with epoch k (where k = 0 , 1, . . . ) given by Ik := 

{kH, . . . , (k+1) H −1}. From the data collected within each epoch k, we then generate a robust empirical estimate bTk of the Bellman optimality operator T by invoking Algorithm 1 as a sub-routine. Crucially, our main insight here is that choosing the epoch length H to be large enough leads to a low variance in the estimate bTk. This turns out to be the key to achieving tight bounds. Using bTk, a single robust update to the Q-table is made at the end of the k-th epoch. Later, we shall see that K = O(log( T )) epochs suffice. Thus, the essence of our approach lies in making infrequent Q-table updates using robust, low-variance estimates of the Bellman operator . We now provide the details of our approach. 

Algorithm 2 BR-Async-Q  

> 1:

Input: Step size α, corruption levels [εY , ε R], confi-dence level δ, total iterations T = KH , epoch length 

H, and number of epochs K. 

> 2:

Initialize Q0 = 0  

> 3:

for epoch k = 0 , . . . , K − 1 do  

> 4:

Initialize {D k(s, a ), Yk(s, a )} = ∅, ∀ (s, a ) ∈ S ×A . 

> 5:

for t ∈ I k do  

> 6:

Observe (st, a t) and feedback (ert(st, a t), est+1 ). 

> 7:

Append Dk(st, a t) ← ert(st, a t), Yk(st, a t) ←

max a∈A Qk(est+1 , a ). 

> 8:

end for  

> 9:

Compute robust estimates following (9).  

> 10:

Compute ( bTkQk)( s, a ) via (12) for all (s, a ).

Update Qk+1 as per (13).  

> 11:

end for 

• Online Batching and Robust Bellman Operator Estimation. Fix an epoch k ∈ [K] with index set Ik. During epoch k, BR-Async-Q maintains two dynamic data-sets for each state-action pair (s, a ) ∈ S × A :                 

> Dk(s, a ) :=  ert(st, a t):t∈ I k,(st, a t) = ( s, a ),
> Yk(s, a ) := max a′∈A Qk(est+1 , a ′) : t∈ I k,(st, a t) = ( s, a ).

(7) For each state-action pair (s, a ) ∈ S × A , let 

Nk(s, a ) := X

> t∈I k

1{(st, a t) = ( s, a )} (8) denote the number of visits to (s, a ) ∈ S × A during epoch 

k ∈ [K]. Since the observations are collected asynchronously over a finite-length epoch, one may encounter degenerate scenarios where some state–action pairs are not visited during a given epoch. Consequently, for each pair (s, a ) and each epoch k, Algorithm 2 must account for the following two cases: Nk(s, a ) > 0 and Nk(s, a ) = 0 .                 

> Fig. 1. Illustration of BR-Async-Q which runs in epochs of length H.During each epoch k, the learner generates the data sets Dkand Ykin (7), constructs a robust empirical estimate bTkof the Bellman optimality operator using such data, and then updates the Q-table to Qk+1 using bTk.
> ■

Case 1 (Nk(s, a ) > 0) : The first data-set Dk(s, a )

contains the possibly corrupted reward observations for state-action pair (s, a ) collected during epoch k, where rewards are generated as per (2). Similarly, the second dataset Yk(s, a )

contains the one-step look-ahead (bootstrapped) values eval-uated using the frozen iterate Qk (from the beginning of epoch k), and a next state observation generated as per (2). Note that under our sampling model, where st is generated independently from π at each time-step, the clean samples in each of the above data sets are independent and identically distributed. Furthermore, the fractions of corrupted samples in Dk(s, a ) and Yk(s, a ) are εR and εY , respectively, on average. Based on these observations, we use the trimmed-mean estimators TRIM H and TRIM B from Section III to estimate for every (s, a ) ∈ S × A , the clean reward mean  

> R(s, a ) := E[r(s, a )]

, and the clean epoch-k bootstrap mean     

> μk(s, a ) := Es′∼P (·| s,a )
> 
> max
> a′∈A Qk(s′, a ′)
> 

as follows: 

bRk(s, a ) ← TRIM H [Dk(s, a ), ε R, δ 1],

bμk(s, a ) ← TRIM B [Yk(s, a ), (−B, B )] , (9) where B = 3 C ˜σ/ (1 − γ), δ1 = δ/ (4 |S||A| T ), and C is the universal constant in (5); the choice of B is justified in Lemma 1. Had we not used this threshold B in the trimming operation for estimating bμk(s, a ), our final bounds would have featured a O(√εY ) term. Using the threshold information allows us to instead obtain a sharper dependence of O(εY ) in our main convergence bound in (14).  

> ■

Case 2 (Nk(s, a ) = 0) : If a state–action pair (s, a ) ∈S ×A is not visited during epoch k ∈ [K], i.e., if Nk(s, a ) = 0, then the corresponding datasets Dk(s, a ) and Yk(s, a ) are empty, and hence, the robust estimators in (9) cannot be evaluated. In this case, we set the corresponding estimates to zero by convention, i.e., bRk(s, a ) = bμk(s, a ) = 0 .

• Stabilizing the Update via Clipping. Although the robust estimators in (9) control the reward and lookahead targets with high probability, they can still fail on rare events. In particular, a rare failure of the robust estimator can yield an extreme value of bRk(s, a ) or bμk(s, a ) defined in (9), which, in turn, may destabilize the Q-table iterates. To guard against these low-probability but high-impact deviations, we apply a simple clipping step that uniformly bounds the empirical Bellman operator, ensuring that updates remain bounded even on extreme events. For this, our clipping threshold will be guided by the guarantees from Theorem 1. However, as apparent from the statement of this theorem, we need the number of samples fed to the robust estimator in Algorithm 1 to exceed a minimum amount. Translated to the estimators in (9), we need enough visits to each state-action pair (s, a ) ∈ S × A in each epoch k. Accordingly, in Lemma 2, we will show that as long as 

H ≥

l

(20 /λ min ) · log (8 T |S||A| /δ )

m

, (10) a standard Bernstein bound ensures that in each epoch 

k, the number of visits to each state-action pair (s, a ) is at least (1 /2) λmin H, with high probability. Recall here that λmin > 0 is the minimum state-action pair visitation probability. On this event, for each pair (s, a ) and epoch k,

Nk(s, a ) = |D k(s, a )| = |Y k(s, a )| ≥ (1 /2) λmin H, which is exactly the regime in which Theorem 1 provides meaningful high-probability control. For epoch length H satisfying (10), informed by the bound (5) from Theorem 1, we construct a clipping threshold as follows: 

Gk := C ¯σ

r 2 log(8 /δ 1)  

> λmin H

+ √εR

!

+ ˜ σ, (11) where recall that ˜σ := max { ¯R, ¯σ}, C is the univer-sal constant from Theorem 1, and δ1 = δ/ (4 |S||A| T ).Next, define the clipping operator as clip [−Gk ,Gk ](x) := min {max {x, −Gk}, Gk}, ∀x ∈ R. Based on the estimates in (9) and the clipping radius in (11), we construct a robust empirical estimate bTk of the Bellman optimality operator T

at the end of each epoch k, as follows: 

( bTkQk)( s, a ):= clip [−Gk ,Gk ]

h bRk(s, a )

i

+γ bμk(s, a ), (12) 

∀(s, a ) ∈ S × A . The Q-table at the end of epoch k is then updated for each (s, a ) ∈ S × A as 

Qk+1 (s, a ) = (1 − α)Qk(s, a ) + α bTkQk(s, a ). (13) Clipping bRk(s, a ) at the threshold in (11) serves a two-fold purpose. First, it controls the magnitudes of the rewards used for updating Qk, protecting against heavy-tailed reward dis-tributions and reward corruptions. Second, it also indirectly controls the look-ahead estimate bμk(s, a ), since the latter is computed from samples of the form max a′∈A Qk(est+1 , a ′),whose size is determined by ∥Qk∥∞. As Lemma 1 reveals, clipping the rewards ensures that ∥Qk∥∞ remains uniformly bounded. This completes our description of BR-Async-Q .

Remark 2. It is instructive to note that the “asynchronous” aspect in our setup comes from the sampling model, where only one state-action pair is observed per time-step, as opposed to a synchronous sampling model [26]–[28] which assumes that all state-action pairs can be accessed at all times. That said, from (13) , notice that although data arrives asynchronously, updates to the Q-table are made synchronously, once the learner has seen each state-action pair sufficiently often within each epoch. This strategy of acquiring enough samples before making updates is precisely what allows us to obtain order-optimal bounds. 

V. M AIN RESULT AND DISCUSSION 

In this section, we provide our main convergence result for BR-Async-Q . Define Qk := [ Qk(s, a )] (s,a )∈S×A , and 

ek := Qk − Q∗, ∀k ≥ 0. We then have the following result. 

Theorem 2. Suppose Assumption 1 holds. Given δ ∈ (0 , 1) 

and T , set the number of epochs to K = ⌈2 log T / (1 − γ)⌉, and suppose T ≥ K ⌈(64 /λ min ) log(8 T |S||A| /δ )⌉. Then, the output of Algorithm 2 with step-size α := log T / ((1 − γ)K) satisfies the following bound with probability at least 1 − δ:                     

> ∥eK∥∞≤∥e0∥∞
> T+O
> ˜σ
> (1 −γ)52
> s
> log( T) log( T|S||A| /δ )
> λmin T
> !
> +
> 
> O(˜ σ)
> εY
> (1 −γ)2
> 
> ∨ O (¯ σ)
> √εR
> 1−γ
>  |{z }
> ψ
> ,

(14) 

where a ∨ b = max ( a, b ).

Discussion of Theorem 2. Theorem 2 gives a finite-time, high-probability ℓ∞-error bound for Algorithm 2. The bound in (14) has two key components: a dominant statistical error term capturing nominal behavior, and a corruption-induced bias term. We discuss each of them below. 

• Statistical term. Up to logarithmic factors, the dominant statistical term in (14) scales as ˜O ˜σ(1 − γ)−5/2/√λmin T  .

While our dependence on γ matches existing bounds [9], [13], our dependence on the minimum visitation probability 

λmin is, in fact, an improvement over that in [9] where the corresponding rate is ˜O



˜σ(1 − γ)−5/2/(λmin 

√T )



. This improvement can be attributed to our batching strategy which leads to synchronous variance-reduced updates. 

• Corruption-induced bias. The non-vanishing term 

ψ in (14) captures the effect of corruption-induced bias. Crucially, this term does not scale with the magnitude of corruption, but rather only the small corruption probabilities 

εY and εR. The significance of Theorem 2 is two-fold. It is the first result to show that Q∗ can be accurately estimated up to a small bias term, despite joint corruption of state and reward observations. Second, when only rewards are corrupted, i.e., when εY = 0 , the bias term O (¯ σ)

 √εR  

> 1−γ



exactly matches the fundamental lower bound derived in [6], sharpening the previous known best bounds under reward corruption. Specifically, the corruption-induced term in [6] scales inversely with λmin , suggesting that infrequent vis-itation of certain state-action pairs can amplify the effects of corruption. Our current work is the first to reveal that via variance-reduction, such an amplification can be avoided under the Huber model, where the adversary can only control 

what to inject, but not when to inject corruption signals; the latter is controlled by the exogenous Bernoulli processes defined in (2). Overall, our main findings contribute to a finer understanding of Q-learning under adversarial feedback. VI. S IMULATION RESULTS 

We evaluate BR-Async-Q on a classic grid-world envi-ronment. The environment is an MDP with |S| = 100 states, |A| = 40 actions, and discount factor γ = 0 .5. The mean reward upper bound for all (s, a ) ∈ S × A satisfies ¯R ∈

(0 , 10] , and the reward variances are uniformly bounded as 

¯σ2 ≤ 10 . Setting ε = εY = εR, we compare the performance of our proposed algorithm with vanilla Q-learning in Fig. 2. The plots reveal the vulnerability of standard Q-learning to corrupted feedback, and show that BR-Async-Q continues to guarantee convergence to a neighborhood of Q∗.                                                

> Fig. 2. (Top Left) ℓ∞error Ek=∥Qk−Q⋆∥∞for Vanilla-Q
> under the Huber-contaminated model in (2), with ε∈ { 0.001 ,0.005 ,0.01 },reward-noise variance σ2= 5 , a −10 6biasing attack on corrupted reward samples, and next-state corruption in which observed next states are replaced by arbitrary states, modeled by uniform resampling over S.(Top Right)
> Ekfor BR-Async-Q under the same corruption levels, noise statistics, and attack. (Bottom Left) Ekfor Vanilla-Q with ε= 0 .2and reward-noise variance σ2∈ { 2,5,10 }.(Bottom Right) Ekfor BR-Async-Q
> with ε= 0 .2and reward-noise variance σ2∈ { 2,5,10 }. Each curve in Fig. 2 reports the average over 100 independent runs.

We next consider the same MDP with asymmetric next-state and reward corruption rates (εY , ε R) i.e., εY̸ = εR.Corrupted rewards are replaced by −10 8, while corrupted next states are sampled uniformly from S. As shown in Fig. 3, vanilla Q-learning is highly vulnerable to such corrup-tion, whereas BR-Async-Q remains stable and converges to a neighborhood of Q⋆.                            

> Fig. 3. (Left) ℓ∞error Ek=∥Qk−Q⋆∥∞for Vanilla-Q under the Huber-contaminated model in (2), with distinct next-state and reward corrup-tion fractions (εY, ε R)∈ { (0 .001 ,0.005) ,(0 .005 ,0.01) ,(0 .01 ,0.02) }.Corrupted reward samples are subjected to a −10 8biasing attack, while corrupted next-state observations are replaced by states sampled uniformly from S.(Right) The corresponding error Ekfor BR-Async-Q under the same corruption levels and attack model. Each curve in Figure 3 reports the average over 100 independent runs, and the shaded region represents one standard deviation around the mean.

Lastly, we examine the effect of the epoch length H on 

BR-Async-Q under two asymmetric corruption settings; see Figure 4.                            

> Fig. 4. (Left) The ℓ∞error Ek=∥Qk−Q⋆∥∞for BR-Async-Q
> under the asymmetric corruption levels (εY, ε R) = (0 .001 ,0.005) , with epoch length H∈ { 1000 ,4000 ,10000 }and a fixed number of epochs
> K= 50 .(Right) The corresponding error under the higher corruption levels
> (εY, ε R) = (0 .01 ,0.02) . In both panels, corrupted rewards are replaced by
> −10 6, corrupted next states are sampled uniformly from S, and the reward-noise variance is σ2= 5 . Each curve in Figure 4 reports the average over 50 independent runs, and the shaded region represents one standard deviation around the mean.

VII. M AIN CONVERGENCE ANALYSIS 

In this section, we provide the detailed proof of Theo-rem 2. Essentially, this boils down to controlling the error between the estimated Bellman operator bTk in (12) and the true operator T . To control this error, we will de-rive high-probability bounds on the reward estimation error 

∥ bRk − R∥∞ in Lemma 4 and look-ahead estimation error 

∥bμk − μ∥∞ in Lemma 3. Here, bRk, R, bμk, and μ are |S| × |A| dimensional vectors with bRk(s, a ), R (s, a ), bμk(s, a ), and 

μ(s, a ) as their respective (s, a )-th components. Our first step is to establish that the clipping operation in (12) ensures that the sequence {Qk} remains uniformly bounded. 

Lemma 1. The following bound holds for all k ≥ 0:

∥Qk∥∞ ≤ B := 3C ˜σ   

> 1−γ

, (15) 

where C is the universal constant from Theorem 1. Proof. Recalling the definition of Nk(s, a ) in (8), we prove the result by induction on k, treating separately the cases 

Nk(s, a ) > 0 and Nk(s, a ) = 0 .The base case holds since Q0 = 0 . For the inductive step, suppose that ∥Qk∥∞ ≤ B. It then follows that 

|max a∈A Qk(s, a )| ≤ B, ∀s ∈ S . Consequently, every look-ahead sample max a′∈A Qk(est+1 , a ′) lies in [−B, B ].We now bound the estimators for an arbitrary (s, a ) ∈S × A by considering two cases. If Nk(s, a ) > 0, then 

TRIM B in (9) averages only values in [−B, B ], and hence 

|bμk(s, a )| ≤ B. Moreover, by the definition of the clipping operator, clip [−Gk ,Gk ]

  bRk(s, a ) ≤ Gk. If Nk(s, a ) = 0 ,then, by convention, bμk(s, a ) = bRk(s, a ) = 0 , so the same two bounds hold. Therefore, uniformly over all (s, a ) ∈S × A , |bμk(s, a )| ≤ B, clip [−Gk ,Gk ]

  bRk(s, a ) ≤ Gk.

It follows from (12) that ∥ bTkQk∥∞ ≤ Gk + γB. Using the update rule in (13), we obtain ∥Qk+1 ∥∞ ≤ (1 − α)B +

α Gk + γB . By the definition of Gk in (11), whenever the epoch length H satisfies (10), we have Gk ≤ 3C ˜σ. Hence, 

∥Qk+1 ∥∞ ≤  1 − α(1 − γ)B + 3 αC ˜σ = B, where the final equality follows from B = 3 C ˜σ/ (1 − γ). This completes the induction step and the proof. Our next step is to control the per-epoch look-ahead esti-mation error. This requires ensuring that within every epoch, we have adequate data for every state-action pair. However, in a given epoch k, the number of visits to any state-action pair (s, a ), denoted by Nk(s, a ) := X

> t∈I k

1{(st, a t) = (s, a )}, is random. Nonetheless, we next argue that as long as the epoch length H is long enough, every state-action pair will be sufficiently visited on a high-probability event. 

Lemma 2. Consider the following event:        

> V:=
> K−1\
> k=0
> 
> min
> (s,a )∈S×A Nk(s, a )≥12λmin H
> 
> .

(16) 

If H satisfies the condition in (10) , then event V holds with probability at least 1 − δ1, where δ1 = δ/ (4 |S||A| T ).Proof. Fix an epoch k and a state-action pair (s, a ). Under our sampling model, {1{(st, a t) = ( s, a )}} t∈I k is an i.i.d. sequence with mean λ(s, a ) and variance at most λ(s, a ). Asimple application of Bernstein’s inequality then tells us that 

P (Nk(s, a ) ≤ 0.5λmin H) ≤ 2 exp 



− 328 Hλ (s, a )



.

Requiring the R.H.S. of the above display to be smaller than 

δ1, and then union-bounding over all state-action pairs and 

K ≤ T epochs, leads to the desired claim; the details are identical to those in [6, Lemma 6]. We can now control the look-ahead estimation error. 

Lemma 3. (Look-ahead Estimation Error ) Suppose the conditions of Theorem 2 hold. Then, with probability at least 

1 − δ/ 2, the following bound holds simultaneously for all epochs k = 0 , . . . , K − 1:              

> ∥ˆμk−μk∥∞≤∆1:= O
> ˜σ
> 1−γ
> s
> log( T|S||A| /δ )
> λmin H+εY
> !
> .

Proof. Fix an epoch k and a state-action pair (s, a ). Our goal is to bound the error |bμk(s, a ) − μk(s, a )|, where 

bμk(s, a ) = TRIM B [Yk(s, a ), (−B, B )] , and μk(s, a ) =

Es′∼P (·| s,a )[max a′∈A Qk(s′, a ′)] . To do so, we wish to leverage Theorem 1. However, the main difficulty arises from the fact that while the number of samples M in Theorem 1 is deterministic, |Y k(s, a )| = Nk(s, a ) is a random variable. To work around this issue, we start by defining a random-threshold deviation event as follows:             

> Ek, 1(s, a ) :=
> (
> |bμk(s, a )−μk(s, a )| ≤ 6¯ cC˜σ
> 1−γ
> s
> log(8 /δ 1)
> Nk(s, a )+εY
> !)
> ,

where ¯c and C are the universal constants that appear in (6) and (5), respectively. We define a corresponding deterministic-threshold version of this event as              

> Ek(s, a ) :=
> |bμk(s, a )−μk(s, a )| ≤ 6¯ cC˜σ
> 1−γ
> s
> 2 log(8 /δ 1)
> λmin H+εY
> .

The above events are informed by the fact that each sample in Yk(s, a ) is corrupted with probability εY . Our immediate goal is to control the failure probability of the “good” event 

Ek(s, a ). To set the stage for this, let Fk be the sigma-field generated by all the randomness up to the beginning of epoch k. By construction, Qk is Fk-measurable. Con-ditioned on Fk then, the only randomness that remains in the uncorrupted samples of the form max a′∈A Qk(st+1 , a ′)

in Yk(s, a ) comes from the next state observation st+1 ∼P(·| s, a ). Under our i.i.d. data generation assumption, all the clean samples in Yk(s, a ) are i.i.d. and have mean μk(s, a )

conditioned on Fk. Moreover, in light of Lemma 1, each clean sample in Yk(s, a ) is contained in the interval [−B, B ],

where B = 3 C ˜σ/ (1 − γ). For any fixed j ≥ 0.5λmin H,appealing to (6) in Theorem 1 then yields           

> β:= P Ek, 1(s, a )CFk, N k(s, a ) = j≤δ1.

(17) We next remove the conditioning on Fk by using the tower property of expectations as follows:                  

> P Ek, 1(s, a )CNk(s, a ) = j=E
> h
> 1Ek, 1(s,a )CNk(s, a ) = j
> i
> =E
> h
> E
> h
> 1Ek, 1(s,a )CFk, N k(s, a ) = j
> ii
> =E[β]≤δ1.

(18) To relate the above result back to our event of inter-est Ek(s, a ), observe from the definitions of the events 

Ek, 1(s, a ) and Ek(s, a ), that for any j ≥ 0.5λmin H, the event n

Ek(s, a )C | Nk(s, a ) = j

o

implies the event 

n

Ek, 1(s, a )C |

Nk(s, a ) = j

o

. As a result,          

> P
> 
> Ek(s, a )CNk(s, a ) = j
> 
> ≤P
> 
> Ek, 1(s, a )CNk(s, a ) = j
> 
> ≤δ1,

(19) where the last inequality follows from (18). To get rid of the conditioning w.r.t. Nk(s, a ) in the above probabilities, we will now leverage the event V from Lemma 2 where all state-action pairs are sufficiently visited. To that end, let 

p := P  Ek(s, a )C ∩ V  and observe that:                                   

> p=
> HX
> j=0
> P Ek(s, a )C∩ V ∩ { Nk(s, a ) = j}
> (a)
> =
> HX
> j=0 .5λmin H
> P Ek(s, a )C∩ V ∩ { Nk(s, a ) = j}
> ≤
> HX
> j=0 .5λmin H
> P Ek(s, a )C∩ { Nk(s, a ) = j}
> =
> HX
> j=0 .5λmin H
> P Ek(s, a )C|Nk(s, a ) = jP Nk(s, a ) = j
> (b)
> ≤δ1
> HX
> j=0 .5λmin H
> P Nk(s, a ) = j≤δ1
> HX
> j=0
> P Nk(s, a ) = j=δ1.

(20) For (a), we used Nk(s, a ) ≥ 0.5λmin H on event V, and for (b), we used (19). Using (20) and the fact that P(VC) ≤

δ1 from Lemma 2, we conclude that P  Ek(s, a )C ≤

P  Ek(s, a )C ∩ V  + P (VC) ≤ 2δ1 ≤ δ/ (2 |S||A| T ).2

We have thus established a failure probability bound for the “good” event Ek(s, a ) for a fixed state-action pair (s, a )

and a fixed epoch k. The claim of the lemma then follows by simply union-bounding over all state-action pairs and all epochs, and using K ≤ T. 

We now establish a similar result for reward estimation.                

> 2Here, we used that for any two events Aand B, the following is true:
> P(A) = P A ∩ B +P A ∩ B C≤P A ∩ B +P BC.

Lemma 4. (Reward Estimation Error ) Suppose the con-ditions of Theorem 2 hold. Then, with probability at least 

1 − δ/ 2, the following bound holds simultaneously for all epochs k = 0 , . . . , K − 1:            

> ∥ˆRk−Rk∥∞≤∆2:= O(¯ σ)
> s
> log( T|S||A| /δ )
> λmin H+√εR
> !
> .

Proof. The argument mirrors Lemma 3, and hence we only highlight the essential points of departure. Like before, fix a state-action pair (s, a ) and an epoch k. This time, we wish to study the error | bRk(s, a ) − R(s, a )|, where bRk(s, a ) = 

TRIM H [Dk(s, a ), ε R, δ 1], and Dk(s, a ) is the data-set of noisy rewards for pair (s, a ) collected during epoch k, with 

|D k(s, a )| = Nk(s, a ). Similar to the proof of Lemma 3, we define the following two events:                 

> Jk, 1(s, a ) : =
> (
> |bRk(s, a )−R(s, a )| ≤ C ¯σ
> s
> log(8 /δ 1)
> Nk(s, a )+√εR
> !)
> ,
> Jk(s, a ) : =
> (
> |bRk(s, a )−R(s, a )| ≤ C ¯σ
> s
> 2 log(8 /δ 1)
> λmin H+√εR
> !)
> .

Following (2), each sample in Dk(s, a ) is corrupted with probability εR, and the clean samples are independent with mean R(s, a ) and variance at most ¯σ2. For any fixed j ≥

0.5λmin H, appealing to (5) in Theorem 1 then yields 

P  Jk, 1(s, a )C Nk(s, a ) = j ≤ δ1. (21) Here, we used the fact that the choice of H in (10) ensures 

δ1 ≥ 8e−j/ 2 when j ≥ 0.5λmin H, allowing us to invoke Theorem 1. Next, we observe that for j ≥ 0.5λmin H,conditioned on {Nk(s, a ) = j}, the event Jk(s, a )C im-plies the event Jk, 1(s, a )C. Thus, from (21), we obtain 

P  Jk(s, a )C Nk(s, a ) = j ≤ δ1. Following an identical reasoning as in (20), we can remove the conditioning on 

{Nk(s, a ) = j} to obtain P  Jk(s, a )C ∩ V  ≤ δ1, implying 

P  Jk(s, a )C ≤ P  Jk(s, a )C ∩ V  + P (VC) ≤ 2δ1 ≤

δ/ (2 |S||A| T ). The rest follows from union-bounding. We are now ready to complete the proof of Theorem 2. 

Proof. (Proof of Theorem 2 ) Let ek := Qk − Q⋆. Our strategy is to set up a recursion for ek in terms of the perturbation ∥ ˆTk − T ∥ ∞, and then use Lemmas 3 and 4 to control the perturbation. Before doing so, we note that the Bellman optimality operator T is defined as follows: 

(T Q)( s, a ) = R(s, a ) + γEs′∼P (·| s,a ) [max a′∈A Q(s′, a ′)] .

(22) We will use the fact that Q∗ is the fixed point of T , i.e., Q∗ =

T Q∗, and that T is a γ-contraction in the ∞-norm [10]: 

∥T Q1 − T Q2∥∞ ≤ γ∥Q1 − Q2∥∞, ∀Q1, Q 2 ∈ R|S|×|A| .

(23) Now (13) gives Qk+1 = (1 − α)Qk + α bTkQk, where, by (12), ( bTk Qk )( s, a ) = clip [−Gk ,Gk ]     

>   bRk(s, a )+γbμk(s, a )

.Since Q⋆ = T Q⋆, adding and subtracting T Qk then yields 

ek+1 = (1 − α)ek + α(T Qk − T Q⋆) + α( bTkQk − T Qk).

Taking ∥ · ∥ ∞ on both sides above and using (23) gives 

∥ek+1 ∥∞ ≤  1 − α(1 − γ)∥ek∥∞ + α bTkQk − T Qk ∞.

(24) Now to control the perturbation bTkQk − T Qk ∞, we note from Lemmas 3 and 4 that there exists an event G of measure at least 1 − δ, on which, the following is true for every (s, a ) and every epoch k:

|bμk(s, a ) − μ(s, a )| + | bRk(s, a ) − R(s, a )| ≤ ∆1 + ∆ 2, (25) where ∆1 and ∆2 are as in the statements of Lemmas 3 and 4, respectively. We will work on this event G for the rest of the proof. From the definition of ( bTkQk)( s, a ) in (12) and 

(T Qk)( s, a ) in (22), we further have for every (s, a ):                   

> |(bTkQk)( s, a )−(TQk)( s, a )| ≤ | bμk(s, a )−μk(s, a )|
> +clip [−Gk,Gk](bRk(s, a )) −R(s, a ).

(26) Since |R(s, a )| ≤ ˜σ, we have R(s, a ) ∈ [−Gk, Gk]

for the Gk defined in (11), and clipping is pre-cisely the projection onto this interval. As a result,             

> clip [−Gk,Gk](bRk(s, a )) −R(s, a )≤ | bRk(s, a )−R(s, a )|.

Hence, using (25) and (26), on the event G, we have 

∥ bTkQk − T Qk∥∞ ≤ ∆1 + ∆ 2 := ∆ . (27) Iterating (24) and using ρ := 1 − α(1 − γ) then gives 

∥eK ∥∞ ≤ ρK ∥e0∥∞ + ∆1 − γ . Since 1−x ≤ e−x, ∀x ∈ (0 , 1) 

the choice of α in Theorem 2 implies that ρK ≤ (1 /T ). The final form in (14) then follows by using the expressions for 

∆1 and ∆2 from Lemmas 3 and 4, combined with T = KH 

and the expression for K in Theorem 2. VIII. C ONCLUSION 

We developed a robust variant of Q-learning that remains reliable under simultaneous corruption of reward and state feedback. Our finite-time guarantees match those of vanilla 

Q-learning up to corruption-dependent bias terms, with a minimax-optimal bias under reward-only corruption. The current implementation requires storing all reward and look-ahead samples within each epoch, leading to auxiliary mem-ory that scales linearly with the epoch length H. A natural direction is to replace these batch estimators with online robust mean estimators that process samples sequentially while maintaining only a compact summary [29]. Another important direction is to extend our framework beyond the tabular setting to function approximation. APPENDIX 

A. Proof of Theorem 1 

Here, we provide a proof for the claim in (6) of Theorem 1. Let the data set D′ be partitioned as D′ = B ∪ S , where B

and S contain the corrupted and clean samples, respectively, within D′. Then, we have:             

> ˆμB
> X−μX=1
> M
> X
> Xi∈B
>  ϕΓ,Γ(Xi)−μX
> |{z }
> T1
> + 1
> M
> X
> Xi∈S
>  ϕΓ,Γ(Xi)−μX
> |{z }
> T2
> .

(28) We now control T1 and T2 separately by working on an event where the number of corrupted samples does not deviate too much from its mean value of εM. To do so, let us define the event W := {|B| ≤ ¯εM }, where ¯ε is the inflated corruption fraction given by: ¯ε := 3 ε/ 2 + 16 log(8 /δ )/M. 

Since each sample in D′ is corrupted independently with probability ε, a simple application of Bernstein’s inequality yields P(W) ≥ 1 − δ/ 4; see, for instance, [6, Appendix D]. Using ϕΓ,Γ(Xi), μ X ∈ [Γ , Γ] , on event W, we have 

|T1|

M ≤ |B| 

M (Γ − Γ) ≤ ¯ε(Γ − Γ) . (29) The above inequality thus holds with probability (w.p.) at least 1 − δ/ 4. Next, to control T2, we note that since S

only contains clean realizations of the random variable X,and X ∈ [Γ , Γ] , clipping causes no change to the samples in S, implying: T2 = X

> Xi∈S

 Xi − μX

. Since {Xi} is a uniformly bounded i.i.d. sequence with mean μX , we can control T2 by using Hoeffding’s inequality. However, care needs to be taken to account for the fact that |S| is a random variable. To proceed, set t = (Γ − Γ) 

r

M ln 

 8

δ



. Defining 

q := P({| T2| ≥ t} ∩ W ), observe that 

q =

> MX
> j=0

P ({| T2| ≥ t} ∩ W ∩ {|S| = j})

> (a)

=

> MX
> j=0 .5M

P ({| T2| ≥ t} ∩ W ∩ {|S| = j})

≤

> MX
> j=0 .5M

P ({| T2| ≥ t}|{|S| = j}) P ({|S| = j})

> (b)

≤

> MX
> j=0 .5M

2 exp( −2t2/(j(Γ − Γ) 2)) P ({|S| = j})

> (c)

≤ 2 exp( −2t2/(M (Γ − Γ) 2)) 

> MX
> j=0

P ({|S| = j}) ≤ δ/ 4.

(30) Here, for (a), we used the fact that on event W, |S| ≥ M −

¯εM ≥ M/ 2, since ¯ε < 0.5. For (b), we used Hoeffding’s inequality, and for (c), we used our choice of t. We thus have: P (|T2| ≥ t) ≤ q + P(WC) ≤ δ/ 2. Thus, w.p. 1 − δ/ 2,

|T2|

M ≤ (Γ − Γ) 

r 1

M ln 

 8

δ



.

Union-bounding the above event with the one on which (29) holds, using (28), and simplifying using the expression for 

¯ε and M > ln(8 /δ ) leads to the final form in (6). REFERENCES [1] Christopher JCH Watkins and Peter Dayan. Q-learning. Machine learning , 8:279–292, 1992. [2] Peter J Huber. Robust estimation of a location parameter. In 

Breakthroughs in statistics , pages 492–518. Springer, 1992. [3] Peter J Huber. Robust statistics , volume 523. John Wiley & Sons, 2004. [4] Thodoris Lykouris, Vahab Mirrokni, and Renato Paes Leme. Stochastic bandits robust to adversarial corruptions. In Proceedings of the 50th Annual ACM SIGACT Symposium on Theory of Computing , pages 114–122, 2018. [5] Sayash Kapoor, Kumar Kshitij Patel, and Purushottam Kar. Corruption-tolerant bandit learning. Machine Learning , 108(4):687– 715, 2019. [6] Sreejeet Maity and Aritra Mitra. Corruption-tolerant asynchronous Q-learning with near-optimal rates. In Proceedings of the 43rd International Conference on Machine Learning , 2026. [7] Sreejeet Maity and Aritra Mitra. Adversarially-robust TD learning with markovian data: Finite-time rates and fundamental limits. In Proceed-ings of The 28th International Conference on Artificial Intelligence and Statistics , volume 258, pages 2431–2439. PMLR, 2025. [8] Sreejeet Maity and Aritra Mitra. Robust Q-learning under corrupted rewards. In 2024 IEEE 63rd Conference on Decision and Control (CDC) , pages 1181–1186. IEEE, 2024. [9] Adam Wierman Guannan Qu. Finite-time analysis of asynchronous stochastic approximation and Q-learning. Proceedings of Machine Learning Research , 125:1–21, 2020. [10] Richard S Sutton and Andrew G Barto. Reinforcement learning: An introduction . MIT press, 2018. [11] John N Tsitsiklis. Asynchronous stochastic approximation and Q-learning. Machine learning , 16:185–202, 1994. [12] Tommi Jaakkola, Michael Jordan, and Satinder Singh. Convergence of stochastic iterative dynamic programming algorithms. Advances in neural information processing systems , 6, 1993. [13] Martin J Wainwright. Stochastic approximation with cone-contractive operators: Sharp ℓ∞-bounds for Q-learning. arXiv preprint arXiv:1905.06265 , 2019. [14] Gen Li, Changxiao Cai, Yuxin Chen, Yuting Wei, and Yuejie Chi. Is Q-learning minimax optimal? a tight sample complexity analysis. 

Operations Research , 72(1):222–236, 2024. [15] Zaiwei Chen, Sheng Zhang, Thinh T Doan, John-Paul Clarke, and Siva Theja Maguluri. Finite-sample analysis of nonlinear stochastic approximation with applications in reinforcement learning. Automat-ica , 146:110623, 2022. [16] John N Tsitsiklis and Benjamin Van Roy. An analysis of temporal-difference learning with function approximation. In IEEE Transactions on Automatic Control , 1997. [17] Jalaj Bhandari, Daniel Russo, and Raghav Singal. A finite time analy-sis of temporal difference learning with linear function approximation. In Conference on learning theory , pages 1691–1692. PMLR, 2018. [18] Rayadurgam Srikant and Lei Ying. Finite-time error bounds for linear stochastic approximation and TD learning. In Conference on Learning Theory , pages 2803–2830. PMLR, 2019. [19] Aritra Mitra. A simple finite-time analysis of TD learning with linear function approximation. IEEE Transactions on Automatic Control ,70(2):1388–1394, 2024. [20] Nathaniel Korda and Prashanth La. On TD(0) with function approxi-mation: Concentration bounds and a centered variant with exponential convergence. In ICML , pages 626–634. PMLR, 2015. [21] C Narayanan and Csaba Szepesv´ ari. Finite time bounds for temporal difference learning with function approximation: Problems with some “state-of-the-art” results. Technical report, Technical report, 2017. [22] Gal Dalal, Bal´ azs Sz¨ or´ enyi, Gugan Thoppe, and Shie Mannor. Finite sample analyses for TD (0) with function approximation. In Proceed-ings of the AAAI Conference on Artificial Intelligence , volume 32, 2018. [23] Ron Dorfman and Kfir Yehuda Levy. Adapting to mixing time in stochastic optimization with Markovian data. In International Conference on Machine Learning , pages 5429–5446. PMLR, 2022. [24] Gabor Lugosi and Shahar Mendelson. Robust multivariate mean estimation: the optimality of trimmed mean. The Annals of Statistics ,49(1):393–410, 2021. [25] Dong Yin, Yudong Chen, Ramchandran Kannan, and Peter Bartlett. Byzantine-robust distributed learning: Towards optimal statistical rates. In International Conference on Machine Learning , pages 5650–5659. PMLR, 2018. [26] Michael Kearns and Satinder Singh. Finite-sample convergence rates for Q-learning and indirect algorithms. Advances in neural information processing systems , 11, 1998. [27] Eyal Even-Dar, Yishay Mansour, and Peter Bartlett. Learning rates for Q-learning. Journal of machine learning Research , 5(1), 2003. [28] Aaron Sidford, Mengdi Wang, Xian Wu, Lin Yang, and Yinyu Ye. Near-optimal time and sample complexities for solving Markov decision processes with a generative model. Advances in Neural Information Processing Systems , 31, 2018. [29] Tong Yao and Shreyas Sundaram. Robust online and distributed mean estimation under adversarial data corruption. In 2022 IEEE 61st Conference on Decision and Control (CDC) , pages 4193–4198. IEEE, 2022.