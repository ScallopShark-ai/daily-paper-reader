---
title: Residuals-based Offline Reinforcement Learning
title_zh: 基于残差的离线强化学习
authors: "Qing Zhu, Xian Yu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01378v1"
tags: ["query:rl"]
score: 10.0
evidence: 基于残差的离线强化学习框架
tldr: 本文针对离线强化学习中数据覆盖受限和分布偏移的挑战，提出了一种基于残差的离线强化学习框架。该框架通过定义一种新型贝尔曼最优算子，利用经验残差将环境动力学估计误差显式引入策略优化过程。研究不仅在理论上证明了该算子的收缩性、渐近最优性及有限样本保证，还开发了相应的离线深度Q网络（DQN）算法，并在随机CartPole环境中验证了其有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01378v1/fig-001.webp\", \"caption\": \"Fig. 1: Flowchart of the residuals-based offline RL.\", \"page\": 3, \"index\": 1, \"width\": 512, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.01378v1/fig-002.webp\", \"caption\": \"Fig. 2: Comparison of different sample sizes\", \"page\": 7, \"index\": 2, \"width\": 429, \"height\": 577}, {\"url\": \"assets/figures/arxiv/2604.01378v1/fig-003.webp\", \"caption\": \"Fig. 3: Comparison of Models\", \"page\": 8, \"index\": 3, \"width\": 383, \"height\": 516}]"
motivation: 旨在解决离线强化学习在缺乏环境交互时，因数据覆盖不足和分布偏移导致的策略性能下降问题。
method: 提出一种基于残差的贝尔曼最优算子，通过经验残差补偿环境动力学学习中的估计误差。
result: 理论证明了该算子具备收缩映射特性与渐近最优性，并在随机控制任务实验中展现出优越的性能。
conclusion: 该研究通过将残差分析引入贝尔曼算子，为提升离线强化学习的稳健性和学习效率提供了有效的理论支撑与算法方案。
---

## 摘要
离线强化学习（RL）因其能够从先前收集的数据中学习策略而无需与真实环境交互而受到越来越多的关注，这在具有高风险的应用中尤为重要。尽管已有大量研究开发了离线强化学习算法，但这些方法通常依赖于对数据覆盖范围的限制性假设，并受到分布偏移的影响。在本文中，我们针对通用状态和动作空间提出了一种基于残差的离线强化学习框架。具体而言，我们定义了一个基于残差的贝尔曼最优算子，该算子通过利用经验残差，将学习转移动态过程中的估计误差显式地纳入策略优化中。我们证明了该贝尔曼算子是一个收缩映射，并确定了其不动点在何种条件下具有渐近最优性及有限样本保证。我们进一步开发了一种基于残差的离线深度Q学习（DQN）算法。通过使用随机倒立摆（CartPole）环境，我们展示了基于残差的离线DQN算法的有效性。

## Abstract
Offline reinforcement learning (RL) has received increasing attention for learning policies from previously collected data without interaction with the real environment, which is particularly important in high-stakes applications. While a growing body of work has developed offline RL algorithms, these methods often rely on restrictive assumptions about data coverage and suffer from distribution shift. In this paper, we propose a residuals-based offline RL framework for general state and action spaces. Specifically, we define a residuals-based Bellman optimality operator that explicitly incorporates estimation error in learning transition dynamics into policy optimization by leveraging empirical residuals. We show that this Bellman operator is a contraction mapping and identify conditions under which its fixed point is asymptotically optimal and possesses finite-sample guarantees. We further develop a residuals-based offline deep Q-learning (DQN) algorithm. Using a stochastic CartPole environment, we demonstrate the effectiveness of our residuals-based offline DQN algorithm.

---

## 论文详细总结（自动生成）

这是一份关于论文《Residuals-based Offline Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：离线强化学习（Offline RL）在不与环境交互的情况下，如何从静态数据集中学习最优策略。
*   **研究动机**：传统的离线 RL 面临三大挑战：
    1.  **数据覆盖限制**：通常要求数据集覆盖所有状态-动作空间，这在连续空间中难以实现。
    2.  **反事实查询难题**：难以预测未执行动作的后果。
    3.  **分布偏移（Distribution Shift）**：训练策略与行为策略不一致导致价值评估不可靠。
*   **整体含义**：本文旨在通过引入“经验残差（Empirical Residuals）”来显式建模环境动力学的估计误差，从而构建一个更稳健的离线学习框架。

### 2. 方法论
*   **核心思想**：借鉴上下文随机优化中的 ER-SAA（经验残差样本平均近似）方法，将环境转移函数分解为“确定性预测 + 随机残差”。
*   **关键技术细节**：
    1.  **回归阶段**：利用离线数据集 $D_N$ 通过监督学习训练一个转移模型 $\hat{f}_N(s, a)$，并计算每个样本的经验残差 $\hat{\epsilon}^{(i)}_N = s'^{(i)} - \hat{f}_N(s^{(i)}, a^{(i)})$。
    2.  **基于残差的贝尔曼算子**：定义新算子 $\hat{T}_N$，在计算期望时，通过在点预测 $\hat{f}_N(s, a)$ 上叠加随机采样的经验残差来模拟下一个状态。
    3.  **理论证明**：证明了该算子是收缩映射（Contraction Mapping），确保了不动点的唯一性；并证明了其在 Lipschitz 连续性假设下具有渐近最优性和有限样本误差界限。
*   **算法流程**：开发了 **Residuals-based Offline DQN**。在训练过程中，算法不直接使用原始数据，而是利用学习到的模型和残差池生成模拟轨迹，从而实现“在线式”的离线训练。

### 3. 实验设计
*   **实验场景**：随机倒立摆（Stochastic CartPole）环境。通过在推车位置观测中加入高斯噪声 $\mathcal{N}(0, 0.25)$ 来增强环境的随机性。
*   **Benchmark（基准）**：
    *   **纯回归模型（w/o residuals）**：仅使用 $\hat{f}_N(s, a)$ 进行点预测，不添加残差。
*   **对比维度**：
    1.  **样本量敏感性**：对比了离线轨迹数 $n \in \{500, 600, \dots, 1000\}$ 对性能的影响。
    2.  **模型有效性**：对比“有残差”与“无残差”在真实环境中的测试奖励。

### 4. 资源与算力
*   **硬件说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **软件/参数细节**：提到了使用 Adam 优化器，学习率为 $10^{-4}$，折扣因子 $\gamma=0.99$，Q 网络包含两个 64 宽度的隐藏层，转移模型使用单层 64 神经元的网络。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每个设置进行了 **5 次独立运行**并报告了平均结果。
    *   进行了 6 组不同样本量的对比实验。
    *   包含了一组关键的消融实验（验证残差的作用）。
*   **充分性评价**：实验在验证理论一致性方面是**初步充分**的，但由于仅使用了 CartPole 这一种相对简单的控制环境，其在复杂高维任务（如 MuJoCo 或真实机器人任务）上的表现尚未得到验证。

### 6. 主要结论与发现
*   **残差的必要性**：实验表明，即使纯回归模型在训练期间表现良好，但在真实随机环境中测试时性能极差；而加入残差的模型能准确捕捉系统随机性，测试表现显著优于基准。
*   **数据量正相关**：随着离线数据量的增加，转移模型和残差分布的估计更加准确，最终策略的性能稳步提升。
*   **理论一致性**：实验结果验证了论文提出的收缩映射和渐近最优性理论。

### 7. 优点
*   **理论严谨**：为离线 RL 提供了一套完整的基于残差的收缩映射理论框架，并给出了有限样本保证。
*   **缓解分布偏移**：通过残差采样生成新状态，使算法能够在一定程度上探索数据集之外的区域，并进行“准在线”训练。
*   **无需强假设**：相比传统方法，该框架对数据覆盖率（Concentrability）的要求较低。

### 8. 不足与局限
*   **环境复杂度低**：实验仅限于随机 CartPole，缺乏在更复杂、非线性更强的环境中的验证。
*   **假设依赖**：理论证明依赖于 Lipschitz 连续性假设，在状态转移存在突变或不连续的系统中可能失效。
*   **计算开销**：虽然论文未详述，但在训练过程中频繁采样残差并进行投影操作（$\Pi_S$）可能会在大规模状态空间中增加计算负担。
*   **分布偏移风险**：虽然残差采样能模拟新状态，但如果初始回归模型 $\hat{f}_N$ 偏差过大，残差也无法完全纠正错误的动力学趋势。

（完）
