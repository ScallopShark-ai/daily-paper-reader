---
title: Near-Optimal Primal-Dual Algorithm for Learning Linear Mixture CMDPs with Adversarial Rewards
title_zh: 具有对抗性奖励的线性混合 CMDP 学习的近优原对偶算法
authors: "Kihyun Yu, Seoungbin Bae, Dabeen Lee"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27884v1"
tags: ["query:rl"]
score: 10.0
evidence: 受限马尔可夫决策过程的理论收敛与遗憾界
tldr: "本研究针对具有对抗性奖励和未知转移核的有限步线性混合约束马尔可夫决策过程（CMDP），提出了一种近乎最优的原始-对偶策略优化算法。该算法在温和条件下实现了 $\\widetilde{O}(\\sqrt{d^2 H^3 K})$ 的遗憾值和约束违反界限，是首个针对此类问题的可证明高效算法。通过引入正则化对偶更新和加权岭回归参数估计，研究成功匹配了极小极大下界，解决了奖励函数动态变化下强对偶分析失效的挑战。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27884v1/fig-001.webp\", \"caption\": \"Fig. 1: Regret and constraint violation of PD-POWERS.\", \"page\": 6, \"index\": 1, \"width\": 350, \"height\": 435}]"
motivation: 旨在解决在奖励函数具有对抗性且环境转移模型未知的线性混合约束强化学习中，如何实现低遗憾值和低约束违反的问题。
method: 提出一种结合了正则化对偶更新以支持漂移分析，并利用加权岭回归构建紧致置信区间的原始-对偶策略优化算法。
result: "该算法在遗憾值和约束违反上均达到了 $\\widetilde{O}(\\sqrt{d^2 H^3 K})$ 的界限，在对数因子意义下匹配了已知的极小极大下界。"
conclusion: 本研究为对抗性奖励下的线性混合CMDP提供了首个具有近乎最优理论保障的高效算法，扩展了安全强化学习的理论边界。
---

## 摘要
我们研究了在全信息反馈和未知转移核下，具有对抗性奖励的有限时界线性混合约束马尔可夫决策过程（CMDPs）中的安全强化学习。我们提出了一种原对偶策略优化算法，在温和条件下实现了 $\widetilde{O}(\sqrt{d^2 H^3 K})$ 的遗憾（regret）和约束违反（constraint violation）界，其中 $d$ 是特征维度，$H$ 是时界，$K$ 是回合数。据我们所知，这是首个针对具有对抗性奖励的线性混合 CMDP 的可证明高效算法。特别地，我们的遗憾界是近优的，在对数因子内匹配了已知的极小极大下界。核心思想是引入正则化对偶更新，从而实现基于漂移（drift-based）的分析。这一步至关重要，因为当奖励函数在不同回合之间变化时，基于强对偶性的分析无法直接应用。此外，我们将基于加权岭回归的参数估计扩展到约束设置中，使我们能够构建更紧致的置信区间，这对于推导近优遗憾界至关重要。

## Abstract
We study safe reinforcement learning in finite-horizon linear mixture constrained Markov decision processes (CMDPs) with adversarial rewards under full-information feedback and an unknown transition kernel. We propose a primal-dual policy optimization algorithm that achieves regret and constraint violation bounds of $\widetilde{O}(\sqrt{d^2 H^3 K})$ under mild conditions, where $d$ is the feature dimension, $H$ is the horizon, and $K$ is the number of episodes. To the best of our knowledge, this is the first provably efficient algorithm for linear mixture CMDPs with adversarial rewards. In particular, our regret bound is near-optimal, matching the known minimax lower bound up to logarithmic factors. The key idea is to introduce a regularized dual update that enables a drift-based analysis. This step is essential, as strong duality-based analysis cannot be directly applied when reward functions change across episodes. In addition, we extend weighted ridge regression-based parameter estimation to the constrained setting, allowing us to construct tighter confidence intervals that are crucial for deriving the near-optimal regret bound.

---

## 论文详细总结（自动生成）

这是一份关于论文《Near-Optimal Primal-Dual Algorithm for Learning Linear Mixture CMDPs with Adversarial Rewards》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在环境模型未知、奖励函数具有对抗性（随时间任意变化）且存在安全约束的复杂环境中，实现最优的学习效率？
*   **研究背景**：受限马尔可夫决策过程（CMDP）是安全强化学习的标准框架。现有研究多集中在表格型（Tabular）或固定奖励场景。在线性函数近似（Linear Function Approximation）领域，现有算法要么假设奖励平稳，要么在处理非平稳环境时遗憾界（Regret）不达标。
*   **整体含义**：本文旨在填补线性混合 CMDP 在对抗性奖励下的理论空白，挑战在于当奖励函数逐回合变化时，传统的“强对偶性”分析失效，且需要更紧致的置信区间来达到极小极大（Minimax）最优速率。

### 2. 方法论：PD-POWERS 算法
该论文提出了 **PD-POWERS**（Primal-Dual Policy Optimization With BERnstein bonuS）算法，核心组件包括：
*   **策略优化（Primal Update）**：采用结合了熵正则化和策略扰动的在线镜像下降（OMD）。通过引入混合参数 $\theta$ 对策略进行微扰，确保策略远离概率单纯形的边界，防止对偶变量爆炸。
*   **正则化对偶更新（Dual Update）**：不同于传统基于强对偶性的剪切更新，本文引入了带有正则化项的对偶更新公式。这种设计允许进行“基于漂移（Drift-based）”的分析，能够有效处理每一回合都在变化的奖励函数。
*   **加权岭回归参数估计**：为了获得更紧致的置信区间，算法将加权岭回归扩展到受限设置。通过估计状态值函数的条件方差并将其作为权重，利用 Bernstein 型集中不等式，将遗憾界对时界 $H$ 的依赖优化至近乎最优。
*   **算法流程**：在每个回合中，算法交替更新原始策略和对偶变量，利用历史轨迹通过加权岭回归更新对转移核参数 $\theta^*$ 的估计，并构造带有乐观奖励和悲观约束的 $Q$ 函数进行策略改进。

### 3. 实验设计
*   **实验场景**：采用了一个合成的 CMDP 实例。状态空间 $S$ 具有分层结构，动作空间 $A$ 为高维向量空间。
*   **奖励与约束设置**：奖励函数设计为每 10 个回合切换一次分布（对抗性体现），约束阈值设为固定值。
*   **Benchmark（基准）**：
    *   **Random Policy**：在每一步随机选择动作的策略。
    *   **理论对比**：在理论分析部分，对比了 Ding et al. [3]、Ghosh et al. [4] 和 Yu et al. [8] 等前沿算法的遗憾界和违反界。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   **实现细节**：作者提供了 GitHub 代码链接，实验规模（$K=2000$ 回合，$d=5$ 维特征）表明该实验可以在普通的个人电脑或标准服务器 CPU 上运行，无需大规模算力集群。

### 5. 实验数量与充分性
*   **实验规模**：进行了 5 组不同随机种子的重复实验，并绘制了均值和 95% 置信区间。
*   **充分性评价**：
    *   **理论验证角度**：实验主要用于验证算法的收敛性（遗憾值和违反量是否呈次线性增长），在这方面是充分的。
    *   **对比角度**：实验部分相对单薄，仅对比了随机策略，未在数值上直接与其他最先进的线性 CMDP 算法进行性能对比（尽管在理论表格中做了详尽对比）。
    *   **消融实验**：缺乏针对正则化项或加权系数的消融实验。

### 6. 主要结论与发现
*   **理论突破**：PD-POWERS 算法在遗憾值和约束违反上均达到了 $\widetilde{O}(\sqrt{d^2 H^3 K})$ 的界限。
*   **近优性**：该遗憾界在对数因子意义下匹配了无约束线性混合 MDP 的极小极大下界，证明了在增加安全约束和对抗奖励的情况下，依然可以达到最优学习速率。
*   **分析工具**：证明了正则化对偶更新配合策略扰动是处理非平稳 CMDP 的有效数学工具，克服了强对偶分析的局限性。

### 7. 优点与亮点
*   **理论严谨性**：这是首个为对抗奖励线性混合 CMDP 提供近优保障的工作，理论贡献显著。
*   **技术融合**：巧妙地将加权岭回归（用于紧致界）与正则化对偶更新（用于非平稳性）结合。
*   **无需先验知识**：对偶更新不依赖于 Slater 常数 $\gamma$ 的先验知识，增强了算法的实用性。

### 8. 不足与局限
*   **计算复杂度**：算法依赖于一个积分算子（Integrating Oracle）来计算期望值。如果该算子不可用，在大状态空间下的计算开销将变得非常大。
*   **对 Slater 条件的依赖**：遗憾界和违反界中包含 $1/\gamma^2$ 和 $1/\gamma$ 项。当安全区域非常狭窄（$\gamma$ 趋近于 0）时，算法性能会剧烈下降。
*   **实验覆盖面**：数值实验仅限于简单的合成环境，缺乏在更复杂的模拟器（如 Safety Gym）或真实数据集上的验证。

（完）
