---
title: "GraSP-STL: A Graph-Based Framework for Zero-Shot Signal Temporal Logic Planning via Offline Goal-Conditioned Reinforcement Learning"
title_zh: GraSP-STL：一种通过离线目标条件强化学习实现零样本信号时序逻辑规划的基于图的框架
authors: "Ancheng Hou, Ruijia Liu, Xiang Yin"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29533v1"
tags: ["query:rl"]
score: 9.0
evidence: 离线目标条件强化学习
tldr: 本文提出GraSP-STL框架，旨在解决无动力学模型和环境交互下的离线零样本信号时序逻辑（STL）规划问题。该方法利用离线任务无关数据学习目标条件价值函数并构建状态空间的可达性图抽象，通过在图上搜索路径点序列并结合算术几何平均鲁棒性评估，实现了对未见STL任务的零样本泛化与长时程规划。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29533v1/fig-001.webp\", \"caption\": \"Fig. 3: Case study of planning and execution.\", \"page\": 7, \"index\": 1, \"width\": 512, \"height\": 538}, {\"url\": \"assets/figures/arxiv/2603.29533v1/fig-002.webp\", \"caption\": \"TABLE II: Performance Metrics for Large-Scale Evaluation\", \"page\": 8, \"index\": 2, \"width\": 506, \"height\": 691}, {\"url\": \"assets/figures/arxiv/2603.29533v1/fig-003.webp\", \"caption\": \"TABLE I: STL formula templates grouped by difficulty\", \"page\": 6, \"index\": 3, \"width\": 506, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2603.29533v1/fig-004.webp\", \"caption\": \"Fig. 1: Overall framework of proposed GraSP-STL.\", \"page\": 3, \"index\": 4, \"width\": 481, \"height\": 512}]"
motivation: 针对在缺乏环境模型和在线交互的离线强化学习场景下，如何使智能体能够直接完成从未见过的复杂STL逻辑任务。
method: 通过学习目标条件价值函数构建有向图抽象，并利用图搜索算法结合鲁棒性语义评估来合成满足STL规范的路径点序列。
result: 实验结果表明，该框架在多种离线STL规划任务中表现出色，能够有效组合短时程行为以完成长时程逻辑任务。
conclusion: GraSP-STL成功实现了可达性学习与任务规划的分离，为离线数据驱动的复杂逻辑任务规划提供了一种高效的零样本泛化方案。
---

## 摘要
本文研究了信号时序逻辑（STL）规范下的离线零样本规划。我们假设仅能访问由任务无关的行为策略收集的状态-动作-状态转移离线数据集，且没有解析动力学模型，无需进一步的环境交互，也不进行针对特定任务的重新训练。目标是合成一种控制策略，使其生成的轨迹满足任意未见的 STL 规范。为此，我们提出了 GraSP-STL，这是一个基于图搜索的框架，用于从离线轨迹中进行零样本 STL 规划。该方法从离线数据中学习目标条件价值函数，并利用其在状态空间上诱导出有限时域可达性度量。基于该度量，它构建了一个有向图抽象，其节点代表具有代表性的状态，边则编码了可行的短时域转移。规划随后被表述为对路标点序列的图搜索，使用算术-几何平均鲁棒性及其区间语义进行评估，并由学习到的目标条件策略执行。所提出的框架将可重用的可达性学习与任务条件规划相分离，从而实现了对未见 STL 任务的零样本泛化，并通过组合离线数据中的短时域行为实现长时域规划。实验结果证明了该方法在一系列离线 STL 规划任务中的有效性。

## Abstract
This paper studies offline, zero-shot planning under Signal Temporal Logic (STL) specifications. We assume access only to an offline dataset of state-action-state transitions collected by a task-agnostic behavior policy, with no analytical dynamics model, no further environment interaction, and no task-specific retraining. The objective is to synthesize a control strategy whose resulting trajectory satisfies an arbitrary unseen STL specification. To this end, we propose GraSP-STL, a graph-search-based framework for zero-shot STL planning from offline trajectories. The method learns a goal-conditioned value function from offline data and uses it to induce a finite-horizon reachability metric over the state space. Based on this metric, it constructs a directed graph abstraction whose nodes represent representative states and whose edges encode feasible short-horizon transitions. Planning is then formulated as a graph search over waypoint sequences, evaluated using arithmetic-geometric mean robustness and its interval semantics, and executed by a learned goal-conditioned policy. The proposed framework separates reusable reachability learning from task-conditioned planning, enabling zero-shot generalization to unseen STL tasks and long-horizon planning through the composition of short-horizon behaviors from offline data. Experimental results demonstrate its effectiveness on a range of offline STL planning tasks.

---

## 论文详细总结（自动生成）

### 论文总结：GraSP-STL 框架

#### 1. 核心问题与整体含义（研究动机和背景）
本文针对**离线、零样本信号时序逻辑（STL）规划**问题展开研究。在许多实际场景中，获取精确的系统动力学模型非常困难，且出于安全或成本考虑，智能体无法在环境中进行实时在线试错。
*   **研究动机**：现有的 STL 规划方法要么依赖解析模型（如优化法、采样法），要么需要针对特定任务进行昂贵的在线强化学习。
*   **核心挑战**：如何在仅有“任务无关”的离线碎片化数据集（即不知道具体任务目标，仅有状态转移数据）的情况下，使智能体能够直接完成从未见过的、具有复杂时空约束的逻辑任务。

#### 2. 方法论：核心思想与技术细节
GraSP-STL 提出了一种将**可达性学习**与**逻辑规划**解耦的分层框架，主要包含三个阶段：
*   **离线目标条件强化学习 (GCRL)**：
    *   利用离线数据学习一个目标条件价值函数 $V(s, g)$，将其转化为时间距离度量 $\hat{d}(s, g) = -V(s, g)$，用于衡量状态间的可达性。
    *   同时训练一个方向感知的底层控制策略 $\pi(a|s, g)$，负责在 $k$ 个控制步长内实现短时程的状态转移。
*   **可达性图构建 (Graph Construction)**：
    *   **节点采样**：通过网格均匀采样和时间距离感知聚类，从海量离线数据中提取代表性状态作为图节点。
    *   **边连接**：若两个节点间的估计距离小于预设阈值 $k$，则建立有向边。采用方向多样性策略确保图的稀疏性与连通性，并提取最大强连通分量（SCC）以保证全局可达。
*   **基于 AGM 鲁棒性的图搜索**：
    *   将规划问题转化为在增强状态空间（图节点 + STL 监控状态）上的搜索。
    *   **核心技术**：引入**算术几何平均（AGM）鲁棒性**及其**区间语义**。在搜索过程中，对尚未完成的路径点序列计算鲁棒性上下界，利用上界小于 0 进行剪枝，利用下界大于 0 判定任务达成。
    *   **扩展性**：该算法改进了前人的监控器，使其能够处理具有任意嵌套深度的 STL 公式。

#### 3. 实验设计
*   **实验场景**：使用 OGBench 中的 **AntMaze** 环境。这是一个高维（29维状态空间）、非线性动力学的 8 自由度四足机器人任务。
*   **数据集**：包含 10,000 条由任务无关策略收集的轨迹（每条 500 步）。
*   **Benchmark 与任务设置**：
    *   设计了 **12 种 STL 模板**（T1-T12），涵盖了从简单的到达（Reachability）到复杂的顺序访问（Sequencing）、安全性约束（Safety）、持久性（Persistence）以及嵌套逻辑（Nested）。
    *   每种模板生成 200 个随机配置（位置、大小、起点），共计 **2400 个测试任务**。
*   **对比指标**：规划成功率 (PSR)、执行成功率 (ESR) 和规划耗时 (PT)。

#### 4. 资源与算力
*   论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。
*   但从算法描述看，其核心计算压力在于离线 GCRL 的训练（通常需要主流 GPU 运行数小时至数天）以及图搜索阶段的计算开销。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 2400 组大规模随机实验，这在 formal methods 与 RL 结合的论文中属于较大规模的验证。
*   **充分性**：实验覆盖了从基础到高级的多种逻辑结构，并提供了详细的消融分析（如规划统计、鲁棒性区间演化等）。
*   **客观性**：通过 PSR 和 ESR 两个指标区分了“规划能力”与“底层控制能力”，客观地揭示了在紧凑时间约束下执行失败的风险。

#### 6. 主要结论与发现
*   **零样本能力**：GraSP-STL 能够有效处理从未见过的 STL 任务，无需任何任务特定的微调。
*   **性能表现**：整体规划成功率达到 **95.58%**，执行成功率达到 **81.58%**。
*   **长时程规划**：通过将短时程行为（$k$ 步转移）进行图组合，成功解决了长达 140 个路标点的长时程任务。
*   **瓶颈发现**：实验发现“持久性（Persistence）”任务（如 $G[a,b]\mu$）最具挑战性，因为这类任务压缩了状态转移的可用时间，对规划的容错度要求极高。

#### 7. 优点（亮点）
*   **解耦设计**：将通用的环境可达性（离线学习）与具体的逻辑任务（在线搜索）分离，极大地提升了泛化性。
*   **鲁棒性评估**：引入 AGM 鲁棒性区间语义，解决了离散图搜索中评估部分路径满足度的难题，且支持嵌套公式。
*   **无需模型**：完全基于数据驱动，不依赖任何解析动力学方程，适用于复杂机器人系统。

#### 8. 不足与局限
*   **数据依赖**：框架的性能高度依赖离线数据的空间覆盖率。如果数据中未包含某些区域，图构建将出现断裂。
*   **离散化误差**：图抽象本质上是离散的，可能无法找到连续空间中的最优路径，且在狭窄通道或复杂障碍物处可能失效。
*   **执行偏差**：底层策略在跟踪路标点时存在误差累积，尤其在时间约束极紧的任务中，微小的延迟可能导致整个 STL 公式失效。
*   **计算复杂度**：对于极复杂的嵌套公式，图搜索的增强状态空间可能会迅速膨胀。

（完）
