---
title: "The Compression Gap: Why Discrete Tokenization Limits Vision-Language-Action Model Scaling"
title_zh: 压缩鸿沟：为什么离散化标记限制了视觉-语言-动作模型的扩展
authors: Takuya Shiba
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03191v1"
tags: ["query:vla"]
score: 10.0
evidence: 解释了离散标记化如何限制视觉-语言-动作模型的扩展
tldr: 本研究揭示了视觉-语言-动作（VLA）模型在扩展视觉编码器时性能提升受限的深层原因，提出了“压缩间隙”理论。研究指出，当动作被离散化为固定容量的标记时，码本会成为信息瓶颈，导致视觉表征的增强无法转化为下游任务的性能提升。通过在LIBERO基准上的对比实验，证明了连续动作表示（如扩散策略）能随编码器升级而显著进步，而离散模型则受限于码本容量，为具身智能模型的有效扩展提供了重要指导。
source: arxiv
selection_source: fresh_fetch
motivation: 探究为何在视觉-语言模型中有效的视觉编码器升级，在采用离散动作标记化的VLA模型中往往无法带来预期的性能提升。
method: 提出“压缩间隙”信息论原则，并在LIBERO基准上通过控制变量实验对比了连续动作与离散动作模型在不同编码器及码本规模下的表现。
result: "实验表明升级编码器使扩散策略性能提升超21%，而离散模型增益微弱，且扩大码本容量能有效缓解离散模型对编码器升级不敏感的问题。"
conclusion: 具身智能模型的扩展性能受限于流水线中最窄的信息瓶颈，离散动作码本的容量限制是阻碍VLA模型随视觉表征扩展的关键因素。
---

## 摘要
通过升级视觉编码器来扩展视觉-语言-动作（VLA）模型，预期会像在视觉-语言建模中那样提升下游操控性能。我们证明，当动作被表示为离散标记（tokens）时，这一预期会失效，并利用一个我们称之为“压缩鸿沟”的信息论原理来解释其原因：在任何视觉运动流水线中，扩展行为都受限于最窄信息瓶颈所在的位置。当动作为连续值（如扩散策略 Diffusion Policy）时，视觉编码器是关键约束，升级编码器能直接提升性能。当动作通过固定容量的代码簿（如 OAT）进行离散化时，代码簿成为了关键约束，无论上游表示多么丰富，编码器的改进都无法跨越该瓶颈进行传播。我们在 LIBERO 基准测试上通过三方面的证据验证了这一原理：一项析因实验显示，编码器升级使扩散策略的性能提升了超过 21 个百分点，而 OAT 的收益在不同模型规模下均显著衰减；针对四个编码器的质量梯度实验确认了扩散策略的性能随编码器质量单调提升，而 OAT 则保持平稳；代码簿大小实验证明，放宽代码簿容量可以部分恢复编码器的敏感性，为瓶颈假设提供了因果证据。我们的研究结果表明，具身智能（Physical AI）的扩展需要识别流水线中信息瓶颈的位置，而不是一味地增加模型或数据规模。

## Abstract
Scaling Vision-Language-Action (VLA) models by upgrading the vision encoder is expected to improve downstream manipulation performance--as it does in vision-language modeling. We show that this expectation fails when actions are represented as discrete tokens, and explain why through an information-theoretic principle we call the Compression Gap: in any visuomotor pipeline, scaling behavior is governed by the location of the tightest information bottleneck. When actions are continuous (e.g., Diffusion Policy), the vision encoder is the binding constraint, and upgrading it directly improves performance. When actions are discretized through a fixed-capacity codebook (e.g., OAT), the codebook becomes the binding constraint, and encoder improvements cannot propagate past it--regardless of how rich the upstream representation is. We validate this principle on the LIBERO benchmark with three lines of evidence: a factorial experiment showing that encoder upgrades improve Diffusion Policy by over 21 percentage points while OAT gains are substantially attenuated across model scales; an encoder quality gradient across four encoders confirming that Diffusion Policy tracks encoder quality monotonically while OAT remains flat; and a codebook size experiment demonstrating that relaxing codebook capacity partially recovers encoder sensitivity, providing causal evidence for the bottleneck hypothesis. Our findings reveal that scaling in Physical AI requires identifying where information bottlenecks lie in the pipeline, rather than uniformly increasing model or data size.

---

## 论文详细总结（自动生成）

这是一份关于论文《The Compression Gap: Why Discrete Tokenization Limits Vision-Language-Action Model Scaling》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在视觉-语言-动作（VLA）模型中，升级视觉编码器（如从 ResNet 升级到 SigLIP）是否总能提升机器人操控任务的性能？
*   **研究背景**：在视觉-语言模型（VLM）领域，增强视觉表征能显著提升下游任务表现。然而，在机器人领域，研究者发现这种“扩展定律”在某些模型中失效了。
*   **核心发现**：论文提出了**“压缩鸿沟”（Compression Gap）**理论，指出性能提升受限于流水线中最窄的“信息瓶颈”。如果动作被离散化为固定容量的标记（Tokens），那么瓶颈就在动作码本（Codebook）处，此时升级上游的视觉编码器无法带来收益。

### 2. 方法论：信息论视角下的瓶颈分析
*   **核心思想**：利用数据处理不等式（Data Processing Inequality）分析视觉运动流水线 $O \to Z \to A$（观测 $\to$ 表征 $\to$ 动作）。
*   **连续路径（如 Diffusion Policy）**：信息流为 $O \xrightarrow{f_{enc}} Z \xrightarrow{\epsilon_\theta} A$。由于所有阶段都在连续空间运行，互信息 $I(Z; A)$ 不受硬性组合限制，瓶颈通常在视觉编码器 $I(O; Z)$。因此，升级编码器能直接拓宽信息通道。
*   **离散路径（如 OAT）**：信息流引入了量化阶段 $Q$，即 $O \to Z \xrightarrow{Q} T \to A$。互信息被限制在 $I(Z; T) \leq H_l \log_2 |V|$（其中 $H_l$ 是标记长度，$|V|$ 是词表大小）。
*   **关键结论**：对于 OAT 等离散模型，如果码本容量（约 80 bits/chunk）已饱和，即使视觉编码器提供了更丰富的信息，也会在量化阶段被丢弃。

### 3. 实验设计与对比
*   **基准测试**：使用 **LIBERO-10** 机器人操控基准（包含 10 个任务，每个任务 50 个演示视频）。
*   **对比方法**：
    *   **Diffusion Policy (DP)**：代表连续动作表示。
    *   **Ordered Action Tokenization (OAT)**：代表最先进的离散动作标记化方法。
*   **变量控制**：
    *   **视觉编码器**：ResNet-18（基础）、SigLIP（强）、SigLIP 2、DINOv2（极强）。
    *   **模型规模**：M（4层 Transformer）、L（6层 Transformer）。
    *   **码本容量**：改变 FSQ（有限标量量化）的层级，对比词表大小 $|V|$ 为 1000、1920 和 4375 的表现。

### 4. 资源与算力
*   **硬件**：每项实验运行使用 **1 张 NVIDIA A100 GPU**。
*   **训练时长**：所有模型训练 **300 个 Epoch**。
*   **细节**：使用了预提取特征（Pre-cached features）以提高训练效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **析因实验**：2（表示法）× 2（编码器）× 2（规模）= 8 组核心对比。
    *   **梯度实验**：测试了 4 种不同质量的编码器。
    *   **因果实验**：针对 3 种码本规模进行了消融研究。
*   **充分性评价**：实验设计非常严密，通过控制变量法排除了模型容量（M/L）带来的干扰，并通过改变码本大小提供了因果证据。虽然只在一个基准（LIBERO）上测试，但其覆盖了从基础到前沿的多种编码器，论证逻辑闭环且客观。

### 6. 主要结论与发现
*   **性能差异**：升级编码器使 Diffusion Policy 的成功率提升了 **21.2% - 26.0%**；而 OAT 的提升仅为 **3.6% - 10.4%**。
*   **质量跟踪**：Diffusion Policy 的性能随编码器质量单调上升；OAT 的性能则在不同编码器下保持平平，甚至在极强编码器下出现波动。
*   **瓶颈转移**：当 OAT 的码本容量扩大时，它对视觉编码器升级的敏感性开始恢复，证明了码本确实是限制扩展的“元凶”。
*   **相对优势反转**：在弱编码器下，离散模型（OAT）优于连续模型；但在强编码器下，连续模型（DP）反超离散模型。

### 7. 优点与亮点
*   **理论深刻**：首次从信息论瓶颈的角度解释了 VLA 模型扩展失效的底层逻辑，提出了“压缩鸿沟”这一极具启发性的概念。
*   **实验纯净**：通过在同一代码库、同一 Backbone 下对比 DP 和 OAT，消除了架构差异的干扰。
*   **前瞻性**：指出了未来具身智能的研究不应只关注模型规模，更应关注信息流中的瓶颈位置。

### 8. 不足与局限
*   **基准单一**：实验主要集中在 LIBERO-10 模拟环境，缺乏在更复杂、真实世界场景或其他机器人基准（如 RT-1 数据集）上的验证。
*   **编码器适配性**：实验发现即使是目前最强的视觉编码器，在 DP 上也遇到了性能瓶颈（60% 左右），这暗示现有的语义视觉编码器可能并不完全适合捕获物理交互所需的空间动力学信息。
*   **种子数限制**：论文提到最终报告的是峰值成功率，未详细展示多随机种子下的方差分析。

（完）
