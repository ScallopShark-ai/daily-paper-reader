---
title: Optimistic Online LQR via Intrinsic Rewards
title_zh: 基于内在奖励的乐观在线 LQR
authors: "Marcell Bartos, Bruce D. Lee, Lenart Treven, Andreas Krause, Florian Dörfler, Melanie N. Zeilinger"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28938v1"
tags: ["query:rl"]
score: 9.0
evidence: 应用强化学习内在奖励的乐观在线LQR算法
tldr: 本文针对未知线性系统的在线LQR问题，提出了IR-LQR算法。该方法借鉴强化学习中的内在奖励机制和方差正则化思想，通过修改代价函数来实现“面对不确定性时的乐观”探索。相比于现有依赖复杂迭代优化或高昂计算开销的算法，IR-LQR保持了标准LQR的合成结构，计算简便且高效。研究证明其达到了最优的sqrt(T)遗憾率，并在飞行器控制等实验中验证了其优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28938v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison of online LQR methods on the aircraft pitch angle control (top) and unmanned aerial vehicle (bottom) examples, in terms of cumulative regret (left), number of controller updates (middle), and wall-clock time per controller update (right). The median is shown together with bootstrap confidence intervals (60%) across runs. (40 samples, T = 200)\", \"page\": 4, \"index\": 1, \"width\": 1031, \"height\": 288}]"
motivation: 现有的乐观在线LQR算法通常涉及复杂的迭代搜索或高昂的计算开销，缺乏一种简单且计算高效的实现方式。
method: 提出IR-LQR算法，通过在标准LQR代价函数中引入基于内在奖励和方差正则化的项，引导系统进行不确定性驱动的探索。
result: 理论证明该算法实现了最优的sqrt(T)遗憾率，并在飞机俯仰角控制和无人机实验中展现出优于现有算法的性能。
conclusion: IR-LQR通过简单的代价函数修改实现了高效的在线探索，为解决线性动力系统的平衡探索与利用问题提供了一种简洁且性能卓越的方案。
---

## 摘要
面对不确定性的乐观（Optimism in the face of uncertainty）是强化学习中平衡探索与利用的一种流行方法。在本文中，我们考虑在线线性二次型调节器（LQR）问题，即通过在运行过程中收集的闭环数据在线调整控制策略，从而学习未知线性动力系统对应的 LQR。在这项工作中，我们提出了内在奖励 LQR（IR-LQR），这是一种乐观的在线 LQR 算法，它应用了源自强化学习的内在奖励思想和方差正则化概念，以促进不确定性驱动的探索。IR-LQR 通过仅修改代价函数，保留了标准 LQR 综合问题的结构，从而得到了一种直观、简单、计算开销低且高效的算法。这与现有的乐观在线 LQR 公式形成对比，后者依赖于更复杂的迭代搜索算法或求解计算量巨大的优化问题。我们证明了 IR-LQR 达到了 $\sqrt{T}$ 的最优最坏情况遗憾率，并通过在飞机俯仰角控制和无人机示例上进行的数值实验，将其与各种最先进的在线 LQR 算法进行了比较。

## Abstract
Optimism in the face of uncertainty is a popular approach to balance exploration and exploitation in reinforcement learning. Here, we consider the online linear quadratic regulator (LQR) problem, i.e., to learn the LQR corresponding to an unknown linear dynamical system by adapting the control policy online based on closed-loop data collected during operation. In this work, we propose Intrinsic Rewards LQR (IR-LQR), an optimistic online LQR algorithm that applies the idea of intrinsic rewards originating from reinforcement learning and the concept of variance regularization to promote uncertainty-driven exploration. IR-LQR retains the structure of a standard LQR synthesis problem by only modifying the cost function, resulting in an intuitively pleasing, simple, computationally cheap, and efficient algorithm. This is in contrast to existing optimistic online LQR formulations that rely on more complicated iterative search algorithms or solve computationally demanding optimization problems. We show that IR-LQR achieves the optimal worst-case regret rate of $\sqrt{T}$, and compare it to various state-of-the-art online LQR algorithms via numerical experiments carried out on an aircraft pitch angle control and an unmanned aerial vehicle example.

---

## 论文详细总结（自动生成）

以下是对论文《Optimistic Online LQR via Intrinsic Rewards》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
该论文探讨了**在线线性二次型调节器（LQR）**的学习问题，即在系统动力学参数未知的情况下，如何通过在线收集的数据实时调整控制策略，以最小化累积代价。
*   **核心矛盾**：强化学习中的“探索-利用”（Exploration-Exploitation）权衡。
*   **研究动机**：现有的“面对不确定性的乐观”（OFU）方法虽然在统计学上是高效的（能达到最优遗憾率），但在计算上通常非常昂贵，往往需要求解复杂的半正定规划（SDP）或非凸优化问题。而简单的“确定性等价”方法虽然计算快，但往往需要注入额外的随机噪声进行探索，导致效率低下。
*   **目标**：设计一种既能保持标准 LQR 计算简洁性（即只需解 Riccati 方程），又能实现理论最优遗憾率的算法。

### 2. 论文提出的方法论：IR-LQR
论文提出了 **IR-LQR（Intrinsic Rewards LQR）** 算法，其核心思想是将强化学习中的“内在奖励”概念引入控制理论。

*   **核心思想**：不改变 LQR 的线性动力学结构，而是通过**修改代价函数**来引导探索。在原始的二次代价函数中减去一个与系统参数不确定性成正比的项（即内在奖励），从而鼓励控制器访问那些数据较少、不确定性较高的状态-动作空间。
*   **关键技术细节**：
    1.  **参数估计**：使用正则化最小二乘法（Regularized Least Squares）估计系统矩阵 $\hat{A}$ 和 $\hat{B}$。
    2.  **内在奖励构造**：利用经验协方差矩阵的逆 $V_t^{-1}$ 来衡量不确定性。构造一个探索奖励矩阵 $\mathcal{V}_t$，并将其从原始代价权重矩阵中减去。
    3.  **光谱裁剪（Spectral Clipping）**：为了保证修改后的代价函数依然是正定的（从而使 LQR 问题可解），对探索奖励矩阵进行特征值裁剪。
    4.  **分阶段更新（Epoch-based）**：算法并非每步更新，而是基于“信息增益”准则（当协方差矩阵的行列式翻倍时）触发控制器重算，以降低计算负担。
*   **算法流程**：观测状态 $\rightarrow$ 更新协方差 $V_t$ $\rightarrow$ 检查更新准则 $\rightarrow$ 若满足，则根据当前估计和修改后的代价函数求解广义 Riccati 方程得到新增益 $K_t$ $\rightarrow$ 应用控制律。

### 3. 实验设计
*   **实验场景**：
    1.  **飞机俯仰角控制（Aircraft Pitch Angle Control）**：经典的航空控制基准。
    2.  **二维无人机控制（UAV 2D）**：具有更高维度的动力学系统。
*   **Benchmark（对比方法）**：
    *   **CEC+PE**：确定性等价控制 + 持续激励噪声。
    *   **OSLO**：基于 SDP 的乐观在线 LQR 算法。
    *   **LagLQ**：基于拉格朗日松弛的乐观 LQR。
    *   **TS**：汤普森采样（Thompson Sampling）LQR。
*   **评估指标**：累积遗憾（Cumulative Regret）、控制器更新次数、单次更新的墙钟时间（Wall-clock time）。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **Apple M1 处理器**和 **32 GB RAM** 的机器上运行。
*   **软件环境**：使用 Python 编写。
*   **算力消耗**：论文强调了 IR-LQR 的计算高效性。实验结果显示，IR-LQR 的单次更新耗时在毫秒级（约 1-10ms），而 OSLO 和 LagLQ 等竞争对手则需要数百毫秒甚至更久。

### 5. 实验数量与充分性
*   **实验规模**：针对每个场景进行了 **40 组随机噪声序列**的独立实验，时间步长 $T=200$。
*   **充分性评价**：
    *   **统计显著性**：实验给出了中位数及 60% 的自助法（Bootstrap）置信区间，证明了结果的稳定性。
    *   **维度覆盖**：涵盖了从基础物理系统到多变量无人机系统的测试。
    *   **公平性**：所有对比算法均经过了超参数调优，且在相同的噪声实现下运行。
    *   **局限性**：实验的时间跨度（$T=200$）相对较短，主要验证了算法在初期的快速收敛能力。

### 6. 论文的主要结论与发现
*   **理论结论**：证明了 IR-LQR 能够达到 **$\tilde{O}(\sqrt{T})$** 的累积遗憾率，这在理论上是未知线性系统在线控制的最优速率。
*   **性能表现**：IR-LQR 在累积遗憾上优于或等同于现有的最先进算法（SOTA）。
*   **计算优势**：IR-LQR 显著降低了计算开销，其复杂度与标准的确定性等价方法相当，远低于其他乐观探索算法。
*   **无需噪声**：与 CEC+PE 不同，IR-LQR 不需要人为注入探测噪声，而是通过“乐观”的控制律自发进行定向探索。

### 7. 优点
*   **简洁高效**：将复杂的乐观探索问题简化为“修改代价函数后的标准 LQR 问题”，易于工程实现。
*   **计算友好**：直接利用成熟的 Riccati 方程求解器，适合实时性要求高的嵌入式系统。
*   **理论完备**：在保持计算优势的同时，严格证明了其满足 OFU 原理并具备最优遗憾界。
*   **初始鲁棒性**：相比于其他方法，IR-LQR 对初始估计精度的要求不随时间 $T$ 增长，具有更好的实用性。

### 8. 不足与局限
*   **初始稳定假设**：算法假设初始参数集 $C_0$ 内的所有系统都是可稳定的，且需要一个初始的稳定控制器。在完全未知的“冷启动”场景下，仍需配合预热（Warm-up）阶段。
*   **超参数敏感性**：虽然理论上给出了参数 $g_t$ 和 $c(\lambda)$ 的形式，但在实际应用中，这些参数可能需要根据具体噪声水平进行精细调优。
*   **线性限制**：该方法严格依赖于线性动力学和二次代价的假设，难以直接推广到具有复杂约束或高度非线性的系统中。

（完）
