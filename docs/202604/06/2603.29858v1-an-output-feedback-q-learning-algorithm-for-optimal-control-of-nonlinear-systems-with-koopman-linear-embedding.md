---
title: An Output Feedback Q-learning Algorithm for Optimal Control of Nonlinear Systems with Koopman Linear Embedding
title_zh: 一种基于Koopman线性嵌入的非线性系统最优控制输出反馈Q学习算法
authors: "Victor G. Lopez, Malte Heinrich, Matthias A. Müller"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29858v1"
tags: ["query:rl"]
score: 9.0
evidence: 非线性系统中Q学习的理论保证
tldr: 本研究针对非线性系统强化学习缺乏强理论保证的现状，提出一种基于Koopman线性嵌入的输出反馈Q学习算法。该方法仅利用输入输出数据，无需系统模型或Koopman提升函数的先验知识，且避免了传统的函数近似技术。其核心贡献在于使非线性系统能够保留与线性系统一致的理论保证，并深入分析了在近似嵌入情况下的算法表现，通过仿真验证了其在最优控制中的适用性。
source: arxiv
selection_source: fresh_fetch
motivation: 解决非线性系统强化学习中因依赖神经网络等函数近似手段而导致的理论保证较弱的问题。
method: 结合Koopman线性嵌入理论与输出反馈Q学习，直接从输入输出数据中学习最优控制策略。
result: 实现了无需模型和提升函数的非线性控制，且在理论上保持了与线性时不变系统同等的收敛性与稳定性。
conclusion: 该算法为非线性系统的最优控制提供了一种具有强理论支撑且无需函数近似的新途径。
---

## 摘要
在强化学习文献中，适用于线性时不变（LTI）系统的算法已获得了强大的理论保证。然而，在非线性情况下，主要依赖于函数逼近策略（例如神经网络）的算法仅获得了较弱的结果。在本文中，我们研究了一种已知的输出反馈Q学习算法在具有Koopman线性嵌入的一类非线性系统中的适用性。该算法仅使用输入输出数据，且不需要了解系统模型或Koopman提升函数。此外，该算法不使用函数逼近技术，并保留了与LTI系统相同的理论保证。此外，我们还分析了当Koopman线性嵌入仅为真实非线性系统的近似时，该算法的性能表现。仿真实例验证了该方法的适用性。

## Abstract
In the reinforcement learning literature, strong theoretical guarantees have been obtained for algorithms applicable to LTI systems. However, in the nonlinear case only weaker results have been obtained for algorithms that mostly rely on the use of function approximation strategies like, for example, neural networks. In this paper, we study the applicability of a known output-feedback Q-learning algorithm to the class of nonlinear systems that admit a Koopman linear embedding. This algorithm uses only input-output data, and no knowledge of either the system model or the Koopman lifting functions is required. Moreover, no function approximation techniques are used, and the same theoretical guarantees as for LTI systems are preserved. Furthermore, we analyze the performance of the algorithm when the Koopman linear embedding is only an approximation of the real nonlinear system. A simulation example verifies the applicability of this method.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对具有 Koopman 线性嵌入（KLE）特性的非线性系统的强化学习（RL）控制方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的非线性系统强化学习算法大多依赖神经网络（NN）等函数近似技术，这导致算法缺乏强有力的稳定性、收敛性和最优性理论保证。相比之下，线性时不变（LTI）系统的 RL 算法理论非常成熟。
*   **研究动机**：作者试图利用 **Koopman 理论**将非线性系统映射到高维线性空间，从而将成熟的线性系统输出反馈 Q 学习算法扩展到非线性领域。
*   **整体含义**：该研究旨在为一类特定的非线性系统提供一种**无需系统模型、无需已知提升函数（Lifting Functions）、且具有二次收敛速度**的最优控制方案。

### 2. 论文提出的方法论
*   **核心思想**：如果一个非线性系统存在 Koopman 线性嵌入（KLE），那么其输入输出行为可以由一个高维线性系统表示。通过构建基于历史输入输出数据的“非最小状态（Non-minimal State）”，可以绕过对原始非线性状态和复杂提升函数的直接处理。
*   **关键技术细节**：
    *   **非最小状态构建**：利用时滞输入输出向量 $z_t = [u_{t-\ell, t-1}^\top, (\Gamma y_{t-\ell, t-1})^\top]^\top$ 作为状态，其中 $\Gamma$ 通过数据驱动方法（算法 1）获得。
    *   **输出反馈 Q 学习（算法 2）**：在不需要知道系统矩阵 $(A, B, C)$ 的情况下，通过迭代求解基于贝尔曼方程（Bellman Equation）的广义李雅普诺夫方程。
    *   **理论保证**：证明了在满足持续激励（PE）条件下，该算法在每一步迭代中都能保持策略的稳定性，并以二次速率收敛至最优控制策略。
    *   **鲁棒性分析**：初步探讨了当 KLE 只是真实系统的近似（存在建模误差或噪声）时，算法通过最小二乘逼近仍能保持一定的性能。

### 3. 实验设计
*   **实验场景**：一个具有 3 维状态和 2 维输入的非线性多项式系统。该系统具有复杂的非线性项（如 $x^3, x^2, x^5$），但存在精确的 KLE。
*   **Benchmark（基准）**：
    *   **RL-NN1**：使用传统的基于神经网络的强化学习算法，采用不完全的基函数近似。
    *   **RL-NN2**：使用相同的 NN 算法，但提供足够丰富的基函数（包含理想的提升函数）。
*   **对比指标**：控制成本误差（Relative Cost Error）、计算时间（Computation Time）、迭代次数。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 或 CPU 型号。
*   **训练时长**：论文提供了平均计算时间对比。提出的 Q 学习算法平均耗时约 **0.002 秒**（迭代 10 次），而对比的 NN 算法耗时约 **7.6 至 9.0 秒**（迭代 10,000 次）。这表明该方法在计算效率上具有显著优势。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 100 次随机初始条件的独立运行以计算平均成本误差。
    *   对比了三种不同的方法（QL, RL-NN1, RL-NN2）。
*   **充分性评价**：实验在验证“理论一致性”方面是充分的，证明了该算法在存在精确 KLE 的系统上远优于 NN 方法。然而，实验仅限于一个数值仿真案例，缺乏在更复杂、更大规模或实际物理系统上的验证。

### 6. 论文的主要结论与发现
*   **最优性实现**：提出的算法能够收敛到与已知模型时完全一致的最优 LQR 控制器，误差低至 $10^{-16}$ 级别。
*   **效率优势**：相比于需要数万次迭代且难以保证收敛到全局最优的神经网络方法，该算法仅需约 10 次迭代即可精确收敛。
*   **无需先验知识**：成功实现了在不知道非线性函数 $f(x,u)$、输出函数 $h(x)$ 以及 Koopman 提升函数 $\Psi(x)$ 的情况下进行最优控制。

### 7. 优点
*   **强理论保证**：将线性系统的稳定性、收敛性保证无损地引入了非线性系统控制。
*   **数据驱动且无模型**：完全基于输入输出数据，避免了复杂的系统辨识或人工设计提升函数的过程。
*   **计算极其高效**：通过代数运算而非深度学习训练，极大地降低了算力需求。

### 8. 不足与局限
*   **KLE 存在性假设**：算法的前提是系统必须承认一个 Koopman 线性嵌入。虽然论文引用了相关存在性研究，但并非所有非线性系统都能找到精确或低维的 KLE。
*   **近似误差敏感性**：虽然论文分析了不精确嵌入的情况，但在高度非线性且无法良好线性化的系统中，近似误差可能导致性能下降。
*   **实验覆盖面**：缺乏对高维复杂系统（如机器人控制、流体动力学）的测试，也未深入探讨大规模噪声干扰下的鲁棒性边界。

（完）
