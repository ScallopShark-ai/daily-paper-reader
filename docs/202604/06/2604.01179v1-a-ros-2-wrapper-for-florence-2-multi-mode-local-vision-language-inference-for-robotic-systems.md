---
title: "A ROS 2 Wrapper for Florence-2: Multi-Mode Local Vision-Language Inference for Robotic Systems"
title_zh: Florence-2 的 ROS 2 封装：面向机器人系统的多模式本地视觉-语言推理
authors: J. E. Domínguez-Vidal
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01179v1"
tags: ["query:vla"]
score: 9.0
evidence: 用于机器人系统的Florence-2视觉语言模型ROS 2封装
tldr: 该研究为Florence-2视觉语言模型开发了ROS 2封装器，旨在为机器人系统提供包括图像描述、OCR和开放词汇检测在内的丰富语义感知能力。该封装器支持话题、服务和动作三种交互模式，并兼容本地原生及Docker部署。通过提供标准ROS 2消息绑定并在消费级GPU上进行性能验证，该工具显著降低了在机器人软件栈中集成先进视觉语言模型的门槛。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01179v1/fig-001.webp\", \"caption\": \"TABLE I MAIN ROS 2 INTERFACES EXPOSED BY THE WRAPPER.\", \"page\": 3, \"index\": 1, \"width\": 1052, \"height\": 390}]"
motivation: 旨在解决视觉语言模型在机器人软件栈中缺乏可复用中间件集成的问题，使Florence-2的强大感知能力更易于被机器人系统调用。
method: 开发了一个支持连续话题、同步服务和异步动作三种模式的ROS 2封装器，并提供JSON与标准ROS消息的转换接口。
result: 实验证明该封装器在消费级GPU上具有良好的推理吞吐量，能够满足本地化部署的实时性需求。
conclusion: 该开源封装器为机器人本地集成多模态视觉语言推理提供了一种高效、灵活且易于部署的解决方案。
---

## 摘要
基础视觉-语言模型在机器人领域正变得日益重要，因为它们能够提供比单一的特定任务流水线更丰富的语义感知。然而，这些模型在机器人软件栈中的实际应用不仅取决于模型质量，还依赖于可复现的中间件集成。Florence-2 在这方面表现出极大的吸引力，因为它在相对适中的模型尺寸下，统一了图像描述（captioning）、光学字符识别（OCR）、开放词汇检测、视觉定位（grounding）及相关的视觉-语言任务。本文介绍了一个 Florence-2 的 ROS 2 封装，该封装通过三种互补的交互模式提供模型能力：连续的话题驱动处理、同步服务调用和异步动作（actions）。该封装专为本地执行而设计，支持原生安装和 Docker 容器部署。此外，它还将通用的 JSON 输出与面向检测任务的标准 ROS 2 消息绑定相结合。本文还报告了功能验证以及在多种 GPU 上的吞吐量研究，结果表明使用消费级硬件进行本地部署是可行的。该代码库已公开：https://github.com/JEDominguezVidal/florence2_ros2_wrapper

## Abstract
Foundation vision-language models are becoming increasingly relevant to robotics because they can provide richer semantic perception than narrow task-specific pipelines. However, their practical adoption in robot software stacks still depends on reproducible middleware integrations rather than on model quality alone. Florence-2 is especially attractive in this regard because it unifies captioning, optical character recognition, open-vocabulary detection, grounding and related vision-language tasks within a comparatively manageable model size. This article presents a ROS 2 wrapper for Florence-2 that exposes the model through three complementary interaction modes: continuous topic-driven processing, synchronous service calls and asynchronous actions. The wrapper is designed for local execution and supports both native installation and Docker container deployment. It also combines generic JSON outputs with standard ROS 2 message bindings for detection-oriented tasks. A functional validation is reported together with a throughput study on several GPUs, showing that local deployment is feasible with consumer grade hardware. The repository is publicly available here: https://github.com/JEDominguezVidal/florence2_ros2_wrapper

---

## 论文详细总结（自动生成）

这是一份关于论文《A ROS 2 Wrapper for Florence-2: Multi-Mode Local Vision-Language Inference for Robotic Systems》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管视觉-语言模型（VLM）在语义感知和推理方面表现卓越，但将这些模型集成到实际机器人软件栈（如 ROS 2）中仍存在巨大鸿沟。现有的集成往往缺乏标准化的中间件接口、可复现的部署方式以及对多种交互模式（如同步与异步）的支持。
*   **研究背景**：Florence-2 是微软开发的一种轻量级且功能强大的基础视觉模型，能够统一处理检测、描述、OCR 等任务。作者旨在通过开发一个专用的 ROS 2 封装器，使 Florence-2 能够作为本地感知组件，无缝接入机器人的实时控制与决策流程中，减少对云端 API 的依赖。

### 2. 论文提出的方法论
*   **核心思想**：设计一个单节点架构的 ROS 2 封装器，将 Florence-2 的多种视觉任务抽象为统一的提示词（Prompt）接口，并提供三种互补的交互模式。
*   **关键技术细节**：
    *   **多模式交互设计**：
        1.  **连续模式（Continuous Mode）**：订阅图像话题，对每一帧进行自动推理，适用于实时语义流。
        2.  **服务模式（Service Mode）**：通过 `ExecuteTask.srv` 进行同步调用，适用于低频、按需的感知查询。
        3.  **动作模式（Action Mode）**：通过 `ExecuteTask.action` 进行异步调用，支持进度反馈和任务监控，适用于耗时较长的复杂推理。
    *   **混合输出机制**：为了兼顾通用性和标准性，封装器同时发布通用的 **JSON 格式**结果（涵盖所有任务）和 **ROS 标准消息**（如 `vision_msgs/Detection2DArray`，用于目标检测任务）。
    *   **部署优化**：支持原生 Python 环境和 Docker 容器化部署（提供 CPU 和 CUDA 两个版本），确保环境的可复现性。

### 3. 实验设计
*   **实验场景**：主要验证封装器在机器人系统中的功能完整性和实时性能。
*   **Benchmark 与对比**：
    *   **功能验证**：测试了图像描述（Captioning）、目标检测（OD）、OCR 等任务在三种交互模式下的正确性。
    *   **性能测试**：在不同硬件配置下，针对 Florence-2 的 **Base** 和 **Large** 两个版本模型，测量了目标检测任务的端到端吞吐量（FPS）。
    *   **对比对象**：对比了不同代际、不同功耗限制的 GPU 硬件表现。

### 4. 资源与算力
*   **GPU 型号**：
    1.  **GTX 1060 Mobile** (80W)：代表老旧/入门级移动端硬件。
    2.  **RTX 3060 Mobile** (80W)：代表主流移动端工作站。
    3.  **RTX 3080 Ti Desktop**：代表高性能桌面端计算平台。
*   **软件环境**：Ubuntu 24.04, ROS 2 Jazzy, Python 3.12, PyTorch, Hugging Face Transformers。
*   **训练时长**：本文属于工程集成类论文，未涉及模型训练，仅涉及推理测试。

### 5. 实验数量与充分性
*   **实验规模**：作者在 3 种不同的 GPU 硬件上分别运行了 Base 和 Large 模型的推理测试，记录了最小、平均和最大 FPS。
*   **充分性评价**：
    *   **功能性实验充分**：涵盖了 ROS 2 的核心通信机制（Topic, Service, Action）。
    *   **性能实验相对客观**：使用了相同的 rosbag 数据源进行回放，确保了输入的一致性。
    *   **局限性**：实验主要集中在吞吐量测试，缺乏在复杂动态机器人任务（如闭环导航或机械臂抓取）中的实战表现评估。

### 6. 论文的主要结论与发现
*   **本地部署可行性**：Florence-2 Base 模型在 RTX 3060 Mobile 上可达到约 10 FPS，在 3080 Ti 上可超过 25 FPS，证明了在消费级硬件上进行本地 VLM 推理是完全可行的。
*   **多模式的必要性**：不同的机器人任务需要不同的交互逻辑，单一的话题订阅无法满足所有场景（如长耗时推理需要 Action 模式）。
*   **Florence-2 的优势**：其统一的任务处理能力显著简化了机器人感知系统的架构，一个节点即可替代多个专用模型节点。

### 7. 优点
*   **工程实用性极强**：解决了 VLM 进入机器人领域的“最后一公里”集成问题。
*   **接口设计优雅**：混合使用 JSON 和标准 ROS 消息，既保证了灵活性，又兼容了现有的 ROS 生态工具（如 RViz 可视化）。
*   **开源贡献**：提供了完整的 Docker 支持和示例客户端，极大降低了开发者的上手门槛。

### 8. 不足与局限
*   **消息类型覆盖有限**：目前仅对检测类任务做了标准消息绑定，OCR 和区域描述等任务仍依赖 JSON 解析。
*   **中断机制不完善**：虽然 Action 模式支持取消请求，但受限于底层推理库的阻塞特性，无法实现真正的瞬时中断。
*   **缺乏精度评估**：论文未讨论封装过程（如图像转换、量化等）是否对 Florence-2 原有的推理精度产生负面影响。
*   **硬件依赖**：尽管支持 CPU，但实际运行速度较慢，对于无 GPU 的嵌入式机器人平台（如树莓派）支持有限。

（完）
