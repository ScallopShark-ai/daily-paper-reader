---
title: "The Compression Gap: Why Discrete Tokenization Limits Vision-Language-Action Model Scaling"
title_zh: 压缩鸿沟：为什么离散化标记限制了视觉-语言-动作模型的扩展
authors: Takuya Shiba
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03191v1"
tags: ["query:vla"]
score: 10.0
evidence: 视觉-语言-动作（VLA）模型的缩放以及离散标记化的限制
tldr: 本研究揭示了视觉-语言-动作（VLA）模型在扩展过程中的“压缩差距”现象，即离散动作标记化形成的固定容量码本会成为信息瓶颈，导致升级视觉编码器带来的性能增益无法传递。通过在LIBERO基准上的对比实验，研究证明了连续动作表示（如扩散策略）能随编码器升级显著提升性能，而离散表示则受限于码本容量，为物理AI模型的有效扩展提供了关键的理论指导。
source: arxiv
selection_source: fresh_fetch
motivation: 探究为何在离散动作表示的VLA模型中，升级视觉编码器往往无法像在视觉语言模型中那样有效提升下游任务性能。
method: 提出“压缩差距”信息论原则，并在LIBERO基准上通过编码器升级、质量梯度分析及码本容量调整等实验验证瓶颈假设。
result: "实验发现扩散策略随编码器升级性能提升超21%，而离散模型增益极小，且扩大码本容量能显著恢复离散模型对编码器改进的敏感性。"
conclusion: 物理AI的扩展不仅取决于模型或数据规模，更需识别并消除流水线中的信息瓶颈，离散标记化是当前限制VLA扩展的核心因素。
---

## 摘要
通过升级视觉编码器来扩展视觉-语言-动作（VLA）模型，预期会像在视觉-语言建模中那样提升下游操作性能。我们展示了当动作被表示为离散标记（tokens）时，这一预期会失效，并利用一个我们称之为“压缩鸿沟”的信息论原理来解释原因：在任何视觉运动流水线中，扩展行为都受限于最窄信息瓶颈所在的位置。当动作为连续值（如 Diffusion Policy）时，视觉编码器是主要的约束因素，升级它能直接提升性能。当动作通过固定容量的代码簿（如 OAT）进行离散化时，代码簿成为了主要的约束因素，无论上游表示多么丰富，编码器的改进都无法跨越它进行传播。我们在 LIBERO 基准测试上通过三方面的证据验证了这一原理：一项析因实验显示，编码器升级使 Diffusion Policy 的性能提升了超过 21 个百分点，而 OAT 的增益在不同模型规模下均显著衰减；针对四个编码器的质量梯度实验确认了 Diffusion Policy 随编码器质量单调提升，而 OAT 则保持平稳；以及一项代码簿大小实验证明，放宽代码簿容量可以部分恢复编码器的敏感性，为瓶颈假设提供了因果证据。我们的研究结果表明，具身智能（Physical AI）中的扩展需要识别流水线中信息瓶颈的位置，而不是盲目地增加模型或数据规模。

## Abstract
Scaling Vision-Language-Action (VLA) models by upgrading the vision encoder is expected to improve downstream manipulation performance--as it does in vision-language modeling. We show that this expectation fails when actions are represented as discrete tokens, and explain why through an information-theoretic principle we call the Compression Gap: in any visuomotor pipeline, scaling behavior is governed by the location of the tightest information bottleneck. When actions are continuous (e.g., Diffusion Policy), the vision encoder is the binding constraint, and upgrading it directly improves performance. When actions are discretized through a fixed-capacity codebook (e.g., OAT), the codebook becomes the binding constraint, and encoder improvements cannot propagate past it--regardless of how rich the upstream representation is. We validate this principle on the LIBERO benchmark with three lines of evidence: a factorial experiment showing that encoder upgrades improve Diffusion Policy by over 21 percentage points while OAT gains are substantially attenuated across model scales; an encoder quality gradient across four encoders confirming that Diffusion Policy tracks encoder quality monotonically while OAT remains flat; and a codebook size experiment demonstrating that relaxing codebook capacity partially recovers encoder sensitivity, providing causal evidence for the bottleneck hypothesis. Our findings reveal that scaling in Physical AI requires identifying where information bottlenecks lie in the pipeline, rather than uniformly increasing model or data size.

---

## 论文详细总结（自动生成）

以下是对论文《The Compression Gap: Why Discrete Tokenization Limits Vision-Language-Action Model Scaling》的结构化深入分析：

### 1. 核心问题与整体含义
*   **研究动机**：在视觉-语言模型（VLM）中，升级视觉编码器（如从 ResNet 换成 SigLIP）能显著提升性能。但在视觉-语言-动作（VLA）机器人模型中，这种“缩放定律”有时会失效。
*   **核心问题**：为什么在某些 VLA 模型中，提升视觉表征质量无法转化为下游操作任务的性能增益？
*   **整体含义**：论文提出了**“压缩鸿沟”（Compression Gap）**理论，指出离散动作标记化（Discrete Tokenization）产生的固定容量码本（Codebook）构成了信息瓶颈，阻碍了上游视觉信息的传递。

### 2. 方法论：核心思想与技术细节
*   **信息论视角**：利用数据处理不等式（Data Processing Inequality）分析视觉运动流水线：$O \to Z \to A$（观测 $\to$ 表征 $\to$ 动作）。
    *   **连续路径（如 Diffusion Policy）**：信息流为 $O \xrightarrow{f_{enc}} Z \xrightarrow{\epsilon_\theta} A$。瓶颈通常在视觉编码器 $I(O; Z)$，因此升级编码器能直接拓宽瓶颈。
    *   **离散路径（如 OAT）**：引入了量化步骤 $Q$，信息流为 $O \to Z \xrightarrow{Q} T \to A$。互信息受限于码本容量：$I(O; A) \le I(Z; T) \le H_l \log_2 |V|$。
*   **关键假设**：当动作被离散化为固定容量的标记（如 OAT 默认约 80 bits/chunk）时，码本成为“绑定瓶颈”（Binding Bottleneck）。此时，无论上游视觉编码器提供多少额外信息，都会在量化阶段被丢弃。

### 3. 实验设计
*   **基准测试**：使用 **LIBERO-10** 机器人操作基准（包含 10 个任务，每个任务 50 个演示视频）。
*   **对比方法**：
    *   **OAT (Ordered Action Tokenization)**：代表离散标记化路径，使用 FSQ（有限标量量化）。
    *   **Diffusion Policy (DP)**：代表连续动作路径。
*   **变量控制**：
    *   **视觉编码器**：ResNet-18（基础）、SigLIP（高质量）、DINOv2、SigLIP 2。
    *   **模型规模**：M（4层 Transformer）、L（6层 Transformer）。
    *   **码本容量**：改变 FSQ 的层级，调整词表大小 $|V|$（1000, 1920, 4375）。

### 4. 资源与算力
*   **硬件使用**：每个实验运行使用 **1 张 NVIDIA A100 GPU**。
*   **训练细节**：所有模型训练 **300 个 epoch**。
*   **优化器**：使用 AdamW，策略网络学习率为 $5 \times 10^{-5}$，视觉编码器学习率为 $1 \times 10^{-5}$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **析因实验**：2（表示法）× 2（规模）× 2（编码器）= 8 组核心对比。
    *   **梯度实验**：针对 4 种不同质量的编码器进行单调性测试。
    *   **因果实验**：针对 3 种码本大小进行消融研究。
*   **充分性评价**：实验设计非常严密。通过控制变量法排除了模型容量（M/L）的干扰，并通过改变码本大小提供了因果证据，证明了性能停滞确实是由离散瓶颈引起的，而非模型训练不足。

### 6. 主要结论与发现
*   **缩放不对称性**：升级编码器使 Diffusion Policy 的成功率提升了 **21.2% - 26.0%**，而 OAT 仅提升了 **3.6% - 10.4%**。
*   **瓶颈转移**：在连续路径中，性能随编码器质量单调提升；在离散路径中，性能在不同编码器间基本持平（处于 50% 左右的平台期）。
*   **因果验证**：当增大 OAT 的码本容量时，模型重新获得了对视觉编码器升级的敏感性，证实了码本容量是限制扩展的关键因素。
*   **性能反转**：在低质量编码器（ResNet）下，离散模型（OAT）表现更好；但在高质量编码器下，连续模型（DP）反超 OAT 近 13 个百分点。

### 7. 优点
*   **理论深刻**：首次从信息论瓶颈的角度解释了 VLA 模型在离散化过程中的扩展限制。
*   **实验纯净**：在同一代码库和超参数框架下对比 DP 和 OAT，消除了架构实现差异带来的偏差。
*   **前瞻性**：指出了未来 Physical AI 的发展不应只关注模型大小，更要关注流水线中的信息流通效率。

### 8. 不足与局限
*   **基准单一**：实验主要集中在 LIBERO-10 模拟环境，尚未在更复杂的真实世界场景或更大规模的数据集（如 Open X-Embodiment）上验证。
*   **指标局限**：报告的是训练过程中的“峰值成功率”（Peak Success Rate），未充分讨论多随机种子下的方差和训练稳定性。
*   **编码器适配**：实验发现即使移除离散瓶颈，现有视觉编码器（如 DINOv2）在机器人任务上也会遇到平台期，暗示目前的视觉基础模型可能缺乏对物理交互（如接触几何、动力学）的表征能力。

（完）
