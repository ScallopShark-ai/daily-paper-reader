---
title: Learning Partial Action Replacement in Offline MARL
title_zh: 在离线多智能体强化学习中学习部分动作替换
authors: "Yue Jin, Giovanni Montana"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28573v1"
tags: ["query:rl"]
score: 9.0
evidence: 结合部分动作替换与PPO的离线多智能体强化学习
tldr: 针对离线多智能体强化学习中联合动作空间爆炸和分布外动作导致的值估计偏差问题，本文提出PLCQL框架。该框架将部分动作替换（PAR）子集选择建模为上下文老虎机问题，利用PPO算法学习状态相关的自适应策略，动态决定替换智能体的数量。相比前作SPaCQL，PLCQL将计算开销从n次评估降至1次，并在多个基准测试中实现了更高的性能和效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28573v1/fig-001.webp\", \"caption\": \"Fig. 1. Illustration of the PLCQL algorithm.\", \"page\": 8, \"index\": 1, \"width\": 581, \"height\": 544}]"
motivation: 现有的部分动作替换方法计算成本高且无法随状态动态调整，难以应对离线MARL中指数级增长的动作空间和严重的分布外偏差。
method: 将PAR子集选择转化为上下文老虎机问题，并使用带有不确定性权重奖励的PPO算法来学习状态相关的自适应替换策略。
result: "在MPE、MaMuJoCo和SMAC等66%的任务中取得最高分，在84%的任务上超越了SPaCQL，同时显著降低了计算开销。"
conclusion: PLCQL通过自适应的动作替换策略，在保证保守值估计的同时提升了策略改进效率，是解决大规模离线MARL挑战的有效方案。
---

## 摘要
离线多智能体强化学习（MARL）面临一个关键挑战：联合动作空间随智能体数量呈指数级增长，导致数据集覆盖范围呈指数级稀疏，且不可避免地会出现分布外（OOD）联合动作。部分动作替换（PAR）通过将一部分智能体锚定在数据集动作上来缓解这一问题，但现有方法依赖于枚举多个子集配置，计算成本高昂，且无法适应不同的状态。我们提出了 PLCQL，该框架将 PAR 子集选择建模为上下文多臂老虎机（contextual bandit）问题，并使用带有不确定性权重奖励的近端策略优化（PPO）来学习状态相关的 PAR 策略。这种自适应策略在每个更新步骤中动态决定要替换多少个智能体，从而在策略改进与保守价值估计之间取得平衡。我们证明了一个价值误差界限，表明估计误差与偏离智能体的预期数量呈线性比例关系。与之前基于 PAR 的方法 SPaCQL 相比，PLCQL 将每次迭代的 Q 函数评估次数从 n 减少到 1，显著提高了计算效率。实验表明，PLCQL 在 MPE、MaMuJoCo 和 SMAC 基准测试的 66% 任务中获得了最高的归一化得分，在 84% 的任务中优于 SPaCQL，同时大幅降低了计算成本。

## 速览
**TLDR**：针对离线多智能体强化学习中联合动作空间庞大导致的分布外（OOD）问题，本文提出PLCQL框架。该框架将部分动作替换（PAR）子集选择建模为上下文老虎机问题，利用PPO算法学习状态相关的自适应策略，动态平衡策略改进与保守估计。相比前作SPaCQL，PLCQL将计算开销从线性降低至常数级别，并在多个基准测试中取得了最优性能。 \
**Motivation**：现有的部分动作替换方法依赖于穷举子集配置，计算成本高昂且无法根据环境状态动态调整替换策略。 \
**Method**：提出PLCQL框架，通过上下文老虎机建模和带不确定性权重的PPO算法，学习状态相关的自适应动作替换策略。 \
**Result**：在MPE、MaMuJoCo和SMAC等66%的任务中取得最高分，在84%的任务中超越SPaCQL，且计算效率大幅提升。 \
**Conclusion**：PLCQL通过自适应的动作替换机制，有效解决了离线多智能体学习中计算效率与分布外动作风险的平衡问题。

---

## Abstract
Offline multi-agent reinforcement learning (MARL) faces a critical challenge: the joint action space grows exponentially with the number of agents, making dataset coverage exponentially sparse and out-of-distribution (OOD) joint actions unavoidable. Partial Action Replacement (PAR) mitigates this by anchoring a subset of agents to dataset actions, but existing approach relies on enumerating multiple subset configurations at high computational cost and cannot adapt to varying states. We introduce PLCQL, a framework that formulates PAR subset selection as a contextual bandit problem and learns a state-dependent PAR policy using Proximal Policy Optimisation with an uncertainty-weighted reward. This adaptive policy dynamically determines how many agents to replace at each update step, balancing policy improvement against conservative value estimation. We prove a value-error bound showing that the estimation error scales linearly with the expected number of deviating agents. Compared with the previous PAR-based method SPaCQL, PLCQL reduces the number of per-iteration Q-function evaluations from n to 1, significantly improving computational efficiency. Empirically, PLCQL achieves the highest normalised scores on 66% of tasks across MPE, MaMuJoCo, and SMAC benchmarks, outperforming SPaCQL on 84% of tasks while substantially reducing computational cost.

---

## 论文详细总结（自动生成）

### 论文总结：在离线多智能体强化学习中学习部分动作替换 (PLCQL)

#### 1. 核心问题与整体含义
离线多智能体强化学习（Offline MARL）的核心挑战在于**联合动作空间的组合爆炸**。随着智能体数量 $n$ 的增加，可能的动作组合呈指数级增长，导致离线数据集对联合动作的覆盖极其稀疏。这使得学习算法在评估分布外（OOD）动作时，容易产生严重的 Q 值过估计，进而导致策略失效。

本文的研究动机在于改进现有的“部分动作替换”（Partial Action Replacement, PAR）技术。之前的 PAR 方法（如 SPaCQL）虽然通过锚定部分数据集动作来缓解 OOD 问题，但存在两个缺陷：一是计算成本高（需枚举多种子集配置），二是缺乏状态自适应性（无法根据当前环境状态动态调整替换策略）。

#### 2. 方法论
论文提出了 **PLCQL (PAR Learning-based Conservative Q-Learning)** 框架，其核心思想是将 PAR 子集大小的选择建模为一个**上下文多臂老虎机（Contextual Bandit）**问题。

*   **核心思想**：学习一个状态相关的 PAR 策略 $\pi_{par}(k|s')$，该策略根据当前状态 $s'$ 动态决定要替换多少个智能体（$k \in \{1, \dots, n\}$）的动作。
*   **关键技术细节**：
    *   **上下文老虎机建模**：将下一状态 $s'$ 作为上下文，子集大小 $k$ 作为动作。由于 $k$ 的选择不改变环境状态转移，因此建模为老虎机而非全 MDP，以保持轻量化。
    *   **不确定性权重奖励**：PAR 策略的奖励函数 $r_{par}$ 结合了 Q 函数的估计值和基于集成（Ensemble）方差的不确定性惩罚。这鼓励策略选择既能带来高价值改进、又具有高置信度（低方差）的替换规模。
    *   **PPO 优化**：使用近端策略优化（PPO）算法来训练这个 PAR 策略。
    *   **计算流程**：在每次 Q 函数更新时，PLCQL 仅采样一个 $k$ 值并进行一次 Q 函数评估，而 SPaCQL 需要评估 $n$ 个配置。
*   **理论支撑**：论文证明了一个价值误差界限，显示估计误差与 PAR 策略诱导的预期偏离智能体数量呈线性相关。

#### 3. 实验设计
*   **数据集与场景**：
    *   **MPE**：包括协作导航（CN）、捕食者-猎物（PP）和世界（World）任务。
    *   **MaMuJoCo**：Half-Cheetah 任务。
    *   **SMAC**：星际争霸 II 任务，涵盖 2s3z, 3s_vs_5z, 5m_vs_6m, 6h_vs_8z 等不同难度的地图。
*   **数据质量分级**：Expert（专家）、Medium（中等）、Medium-Replay（中等回放）、Random（随机）和 Mixed（混合）。
*   **对比方法**：
    *   策略约束类：OMAR, IQL, MA-TD3+BC, AWAC, MAICQ。
    *   价值约束类：MACQL, CFCQL, SPaCQL。
    *   扩散模型类：DoF。

#### 4. 资源与算力
*   **硬件使用**：实验在 **NVIDIA Tesla V100 GPU** 上执行。
*   **算力说明**：论文未明确给出总训练时长，但强调了计算效率的提升。在 6 个智能体的 SMAC 地图中，PLCQL 将每轮迭代的 Q 函数评估次数从 6 次降至 1 次，在 V100 上观察到了显著的“墙钟时间”（Wall-clock time）加速。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个基准测试的 32 个不同任务/数据集组合上进行了测试。
*   **重复性**：所有实验均使用 **5 个随机种子**运行，并报告了均值和标准差。
*   **消融实验**：作者进行了充分的消融研究，验证了“自适应学习”相对于固定 $k$ 值的优越性，以及“不确定性权重”对稳定学习的重要性。
*   **评价**：实验覆盖了连续和离散动作空间，对比了当前主流的各类离线 MARL 算法，实验设计客观且充分。

#### 6. 主要结论与发现
*   **性能领先**：PLCQL 在 66% 的任务中取得了最高归一化得分，并在 84% 的任务中优于其直接前作 SPaCQL。
*   **效率提升**：通过将子集选择转化为学习问题，PLCQL 显著降低了 PAR 机制的计算开销。
*   **自适应性**：实验证明，没有一个固定的替换规模 $k$ 能在所有任务中胜出，而 PLCQL 学习到的状态相关策略能有效平衡协调性与保守性。

#### 7. 优点
*   **创新性**：首次将 PAR 子集选择视为一个可学习的自适应过程。
*   **高效性**：大幅减少了离线 MARL 训练中的计算冗余。
*   **理论与实践结合**：不仅提供了严谨的价值误差界限证明，还在多个复杂基准上验证了实用性。
*   **即插即用**：该机制可以较容易地集成到其他基于值的离线 MARL 算法中。

#### 8. 不足与局限
*   **子集采样随机性**：虽然 $k$（替换数量）是自适应学习的，但具体替换哪 $k$ 个智能体目前仍是**均匀随机采样**，未考虑智能体之间特定的拓扑或依赖关系。
*   **集成依赖**：算法依赖于 Q 函数集成（Ensemble）来估计不确定性，这在智能体数量极多或内存受限的场景下可能存在扩展性挑战。
*   **超参数敏感性**：虽然减少了枚举开销，但引入了 PPO 相关的超参数（如温度参数 $T$），可能需要额外的调优。

（完）
