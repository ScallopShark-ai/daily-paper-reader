---
title: "Soft MPCritic: Amortized Model Predictive Value Iteration"
title_zh: Soft MPCritic：摊销模型预测价值迭代
authors: "Thomas Banker, Nathan P. Lawrence, Ali Mesbah"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01477v1"
tags: ["query:rl"]
score: 9.0
evidence: 结合强化学习与模型预测控制进行模型化价值迭代
tldr: Soft MPCritic 结合了强化学习与模型预测控制的优势，通过基于采样的规划器（MPPI）进行在线控制和价值目标生成。该框架利用拟合价值迭代训练终端 Q 函数，并引入摊销预热策略复用动作序列，显著降低了计算开销。它在保持规划质量的同时，通过短时域规划实现了在复杂控制任务中的高效学习，为合成鲁棒 MPC 策略提供了可扩展的方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01477v1/fig-001.webp\", \"caption\": \"Fig. 4: Cumulative reward for softMPCritic with uniform prior and RL baselines (SAC and DDPG). Lines represent the median of 10 seeds, shading percentiles. The horizontal dashed lines corresponds to SAC and DDPG after 106 time steps.\", \"page\": 6, \"index\": 1, \"width\": 476, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.01477v1/fig-002.webp\", \"caption\": \"Fig. 3: Ablation of softMPCritic with Gaussian prior. Left: using only a dynamics model ensemble (f Ensemble), terminal Q-function (Q), or both. Right: using MPPI only for control, Q-function targets, or both. Lines represent the median of 10 seeds, shading percentiles.\", \"page\": 6, \"index\": 2, \"width\": 484, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.01477v1/fig-003.webp\", \"caption\": \"Fig. 2: Performance of warm and cold starting MPPI target computations when utilizing a single or ensemble of dynamic models f . Lines represent the median of 10 seeds, shading percentiles. The horizontal dashed line corresponds to SAC.\", \"page\": 5, \"index\": 3, \"width\": 483, \"height\": 514}, {\"url\": \"assets/figures/arxiv/2604.01477v1/fig-004.webp\", \"caption\": \"Fig. 1: softMPCritic effectively combines key approximations of RL and MPPI for autonomous, self-improvement. During online control, the value-space planner updates to initial open-loop actions υ to align with the soft minimum of value function Qϕ. The updated sequence υ+, along with transition (s, a, ℓ, s′), is stored in the replay buffer to warmstart the planner’s value targets VMPPI\", \"page\": 3, \"index\": 4, \"width\": 500, \"height\": 331}]"
motivation: 旨在解决在大规模应用中结合强化学习与模型预测控制时面临的计算效率低下及长时域规划困难的问题。
method: 提出一种基于 MPPI 规划器和终端 Q 函数的框架，通过摊销预热策略复用动作序列来加速价值目标的生成。
result: 在经典及复杂的控制任务中，该方法能够通过鲁棒的短时域规划实现有效的策略学习。
conclusion: Soft MPCritic 为在策略提取或直接长时域规划失效的场景下构建实用的 MPC 策略提供了可扩展的蓝图。
---

## 摘要
强化学习 (RL) 和模型预测控制 (MPC) 具有互补的优势，但在大规模场景下将两者结合在计算上仍具挑战性。我们提出了 soft MPCritic，这是一个 RL-MPC 框架，它在（软）价值空间中进行学习，同时利用基于采样的规划进行在线控制和价值目标生成。soft MPCritic 通过模型预测路径积分控制 (MPPI) 实例化 MPC，并使用拟合价值迭代训练终端 Q 函数，使学习到的价值函数与规划器保持一致，并隐式地延长了有效规划时界。我们引入了一种摊销热启动策略，在计算基于 MPPI 的批量价值目标时，从在线观测中回收已规划的开环动作序列。这使得 soft MPCritic 在计算上具有可行性，同时保留了求解质量。soft MPCritic 以基于场景的方式进行规划，并使用一组针对下一步预测精度进行训练的动力学模型集成。这些要素共同使 soft MPCritic 能够通过在经典和复杂控制任务上的鲁棒、短时界规划进行有效学习。这些结果确立了 soft MPCritic 作为一个实用且可扩展的蓝图，用于在策略提取和直接长时界规划可能失效的环境中合成 MPC 策略。

## Abstract
Reinforcement learning (RL) and model predictive control (MPC) offer complementary strengths, yet combining them at scale remains computationally challenging. We propose soft MPCritic, an RL-MPC framework that learns in (soft) value space while using sample-based planning for both online control and value target generation. soft MPCritic instantiates MPC through model predictive path integral control (MPPI) and trains a terminal Q-function with fitted value iteration, aligning the learned value function with the planner and implicitly extending the effective planning horizon. We introduce an amortized warm-start strategy that recycles planned open-loop action sequences from online observations when computing batched MPPI-based value targets. This makes soft MPCritic computationally practical, while preserving solution quality. soft MPCritic plans in a scenario-based fashion with an ensemble of dynamic models trained for next-step prediction accuracy. Together, these ingredients enable soft MPCritic to learn effectively through robust, short-horizon planning on classic and complex control tasks. These results establish soft MPCritic as a practical and scalable blueprint for synthesizing MPC policies in settings where policy extraction and direct, long-horizon planning may fail.

---

## 论文详细总结（自动生成）

以下是对论文《Soft MPCritic: Amortized Model Predictive Value Iteration》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了如何有效结合**强化学习 (RL)** 与**模型预测控制 (MPC)**。
*   **背景**：MPC 擅长处理动力学约束和在线优化，但依赖准确的长时域模型；RL 擅长通过价值函数捕捉长期奖励，但策略提取过程往往不稳定且难以处理复杂约束。
*   **核心问题**：在大规模应用中，将两者结合面临巨大的计算开销（尤其是基于采样的 MPC）以及模型预测误差随时间累积的问题。
*   **研究动机**：提出一种完全在“价值空间”内运行的框架，利用短时域规划配合学习到的终端价值函数，既能缓解模型误差，又能通过“摊销（Amortized）”计算提高效率。

### 2. 方法论：核心思想与关键技术
Soft MPCritic 的核心是将 **MPPI（模型预测路径积分控制）** 与 **软价值迭代（Soft Value Iteration）** 相结合。

*   **核心思想**：使用 MPPI 作为在线控制器，同时将其作为生成 RL 训练目标的“算子”。通过训练一个终端 Q 函数来补偿 MPPI 的短时域限制，使规划器在有限步数内也能考虑到无限时界的收益。
*   **关键技术细节**：
    *   **软价值函数 (Soft Value Function)**：基于自由能原理，使用 Softmax 形式的算子代替硬性最大化，使策略具有更好的探索性和鲁棒性。
    *   **动力学模型集成 (Ensemble of Models)**：训练一组神经网络模型预测下一步状态，通过集成模型来应对模型不确定性，提高规划的鲁棒性。
    *   **摊销热启动策略 (Amortized Warm-start)**：这是本文效率提升的关键。在计算批量价值目标时，不从头开始优化 MPPI，而是复用经验回放池中存储的、之前在线规划得到的动作序列（$\upsilon$）进行微调。
*   **算法流程**：
    1.  在线控制：MPPI 利用当前 Q 函数和模型集成生成动作。
    2.  存储经验：将状态、动作、奖励、下一状态及**优化后的动作序列**存入回放池。
    3.  价值更新：从池中采样，利用存储的动作序列热启动 MPPI，计算新的价值目标并更新 Q 函数。

### 3. 实验设计
*   **实验场景**：
    1.  **双倒立摆 (Double Inverted Pendulum)**：用于验证算法的基础性能和计算效率。
    2.  **Hopper-v5 (机器人跳跃)**：高维、复杂的动力学环境，用于测试算法在挑战性任务中的表现。
*   **Benchmark 与对比方法**：
    *   **SAC (Soft Actor-Critic)**：最先进的无模型强化学习基准。
    *   **DDPG (Deep Deterministic Policy Gradient)**：经典的确定性策略梯度方法。
    *   **消融实验**：对比了“有无终端 Q 函数”、“有无模型集成”、“热启动 vs 冷启动”以及“MPPI 仅用于控制 vs 仅用于目标生成”等多种变体。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率指标**：论文通过“每秒环境步数 (SPS)”来间接衡量计算效率。实验表明，采用“热启动”策略后，在保持性能的同时，计算速度比“冷启动”快了约 2 到 7 倍（取决于采样数 $N$）。

### 5. 实验数量与充分性
*   **实验规模**：每个实验配置均运行了 **10 个不同的随机种子**，并给出了中位数和百分位数区间，这在 RL 论文中属于较为严谨的做法。
*   **充分性**：
    *   **消融实验非常全面**：深入探讨了 MPPI 各个组件（Q 函数、模型集成）对最终性能的贡献。
    *   **对比公平性**：在 Hopper 任务中，Soft MPCritic 在 20 万步内就达到了 SAC 在 100 万步时的性能，证明了其样本效率。
*   **局限性**：实验环境主要集中在 MuJoCo 仿真器中的经典任务，尚未在更复杂的视觉输入或真实物理机器人上进行验证。

### 6. 主要结论与发现
*   **对齐的重要性**：将 MPPI 同时用于在线控制和价值目标生成，能确保控制器与价值函数“对齐”，避免了策略提取带来的性能损失。
*   **短时域规划的威力**：配合高质量的终端 Q 函数，仅需 8 步的短时域规划即可超越长时域直接规划的效果。
*   **热启动的有效性**：通过复用动作序列，大幅降低了在 RL 训练循环中调用 MPC 的计算负担，使该框架具有实际可扩展性。
*   **鲁棒性**：模型集成能显著减少模型预测误差带来的负面影响。

### 7. 优点（亮点）
*   **计算效率高**：通过摊销策略解决了 MPC-RL 结合时最头疼的计算延迟问题。
*   **算法简洁**：不需要复杂的策略提取步骤（如 Actor 网络），完全在价值空间操作。
*   **性能卓越**：在样本效率和最终收敛奖励上均显著优于传统的 SAC 和 DDPG。

### 8. 不足与局限
*   **超参数敏感性**：MPPI 的温度参数 $\lambda$、采样数 $N$ 以及模型集成的规模可能需要精细调优。
*   **约束处理**：虽然论文提到 MPC 擅长处理约束，但在 Soft MPCritic 的实验中并未显式展示复杂硬约束的处理能力（主要关注奖励最大化）。
*   **模型依赖**：尽管使用了集成模型，但算法性能依然高度依赖于单步动力学模型的预测精度。

（完）
