---
title: Dynamic Lookahead Distance via Reinforcement Learning-Based Pure Pursuit for Autonomous Racing
title_zh: 基于强化学习纯追踪算法的自主赛车动态前视距离研究
authors: "Mohamed Elgouhary, Amr S. El-Wakeel"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28625v1"
tags: ["query:rl"]
score: 9.0
evidence: 用于自动驾驶赛车动态前视距离的PPO算法
tldr: 针对自动驾驶赛车中纯追踪算法前视距离难以兼顾稳定性和精确性的问题，本文提出一种结合强化学习（PPO）的混合控制框架。该方法通过学习车辆速度和多尺度曲率特征，动态调整前视距离。实验表明，该方法在F1TENTH仿真和真车测试中均显著提升了单圈速度和路径跟踪稳定性，并具备出色的跨场景泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28625v1/fig-001.webp\", \"caption\": \"Fig. 2: Training diagnostics for PPO. Reward and episode length are computed on a fixed evaluation environment; explained variance and entropy are logged during training.\", \"page\": 6, \"index\": 1, \"width\": 529, \"height\": 405}, {\"url\": \"assets/figures/arxiv/2603.28625v1/fig-002.webp\", \"caption\": \"Fig. 1: Training-and-deployment workflow. A map and minimum-curvature raceline are generated offline and used by the simulator environment and baseline Pure Pursuit controller during training. The PPO agent receives observations and reward from the environment and outputs a dynamic lookahead policy, which is then integrated with Pure Pursuit for simulation validation and real-world deployment on a 1:10-scale autonomous racing platform.\", \"page\": 5, \"index\": 2, \"width\": 882, \"height\": 487}]"
motivation: 传统纯追踪算法的前视距离固定，难以在高速直道稳定性和弯道循迹精度之间取得平衡。
method: 利用近端策略优化（PPO）算法训练智能体，根据实时车速和赛道曲率动态输出最优前视距离，并集成于ROS2控制框架。
result: 10比例真车的零样本迁移。
conclusion: 通过强化学习动态优化单一可解释参数，能有效增强经典控制器的性能，实现更激进且稳定的自动驾驶赛车控制。
---

## 摘要
纯追踪（Pure Pursuit, PP）算法因其简洁性和实时性能，在自动驾驶车辆的路径跟踪中得到了广泛应用。然而，其有效性对前视距离的选择十分敏感：较短的前视距离能提升过弯性能，但可能导致直道行驶不稳定；而较长的距离虽能提高平滑度，却会降低弯道精度。我们提出了一种混合控制框架，将近端策略优化（PPO）与经典的纯追踪控制器相结合，以在比赛过程中动态调整前视距离。PPO 智能体将车速和多时域曲率特征映射为在线前视指令。该智能体在 F1TENTH Gym 模拟器中使用 Stable-Baselines3 进行训练，并采用 KL 惩罚和学习率衰减以保证稳定性，随后部署在 ROS2 环境中以引导控制器。仿真实验将所提方法与固定前视距离纯追踪以及自适应纯追踪基准进行了对比。额外的实车实验将学习到的控制器与固定前视距离纯追踪控制器进行了比较。结果表明，所学习的策略提高了在未知赛道上的单圈耗时性能和重复完赛率，并实现了向硬件的零样本迁移。学习到的控制器通过在直道增加前视距离、在弯道减小前视距离进行自适应调整，证明了通过在线调整单个可解释参数来增强经典控制器的有效性。在未知赛道上，所提方法在蒙特利尔赛道达到 33.16 秒，在亚斯码头赛道达到 46.05 秒，同时比基准方法能容忍更激进的速度曲线缩放，并在所有测试设置中取得了最佳单圈成绩。初步的实车实验进一步支持了在 1:10 比例自主赛车平台上的从仿真到现实（sim-to-real）的迁移。

## Abstract
Pure Pursuit (PP) is a widely used path-tracking algorithm in autonomous vehicles due to its simplicity and real-time performance. However, its effectiveness is sensitive to the choice of lookahead distance: shorter values improve cornering but can cause instability on straights, while longer values improve smoothness but reduce accuracy in curves. We propose a hybrid control framework that integrates Proximal Policy Optimization (PPO) with the classical Pure Pursuit controller to adjust the lookahead distance dynamically during racing. The PPO agent maps vehicle speed and multi-horizon curvature features to an online lookahead command. It is trained using Stable-Baselines3 in the F1TENTH Gym simulator with a KL penalty and learning-rate decay for stability, then deployed in a ROS2 environment to guide the controller. Experiments in simulation compare the proposed method against both fixed-lookahead Pure Pursuit and an adaptive Pure Pursuit baseline. Additional real-car experiments compare the learned controller against a fixed-lookahead Pure Pursuit controller. Results show that the learned policy improves lap-time performance and repeated lap completion on unseen tracks, while also transferring zero-shot to hardware. The learned controller adapts the lookahead by increasing it on straights and reducing it in curves, demonstrating effectiveness in augmenting a classical controller by online adaptation of a single interpretable parameter. On unseen tracks, the proposed method achieved 33.16 s on Montreal and 46.05 s on Yas Marina, while tolerating more aggressive speed-profile scaling than the baselines and achieving the best lap times among the tested settings. Initial real-car experiments further support sim-to-real transfer on a 1:10-scale autonomous racing platform

---

## 论文详细总结（自动生成）

### 论文总结：基于强化学习的自主赛车动态前视距离纯追踪算法

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对自主赛车（Autonomous Racing）中的路径跟踪问题，聚焦于经典**纯追踪（Pure Pursuit, PP）算法**的局限性。PP 算法的核心参数是**前视距离（Lookahead Distance, $L_d$）**：
*   **短前视距离**：提高弯道响应灵敏度，但在直道易导致转向振荡和不稳定。
*   **长前视距离**：提高行驶平滑度，但在急弯处容易出现“切弯”或欠转向，导致循迹精度下降。
传统的固定值或基于规则的启发式调整方法难以兼顾高速直道的稳定性和复杂弯道的精确性。研究动机在于利用强化学习（RL）的自适应能力，动态优化这一关键参数，以提升赛车在极限速度下的表现。

#### 2. 方法论：核心思想与技术细节
论文提出了一种**混合控制框架**，保留了经典几何控制器的结构，仅将其中的 $L_d$ 参数交由 RL 策略在线生成。

*   **核心思想**：使用**近端策略优化（PPO）**算法训练一个智能体，根据车辆当前状态（速度、赛道曲率）实时输出最优的 $L_d$。
*   **状态空间（Observation）**：5维向量，包括当前车速 $v_t$、近/中/远三个时域的路径曲率 $\kappa_{0,t}, \kappa_{1,t}, \kappa_{2,t}$，以及曲率变化量 $\Delta \kappa_t$。
*   **动作空间（Action）**：连续值 $L_d \in [0.35, 4.0]$ 米。
*   **奖励函数（Reward）**：综合了速度、平滑度（动作惩罚）、曲率匹配、碰撞惩罚、路径进度以及防止停滞的项。特别引入了一个“启发式理想前视距离”作为引导信号来稳定训练。
*   **控制流程**：
    1.  **全局优化**：利用最小曲率法预计算最优赛车线和速度曲线。
    2.  **感知定位**：通过 LiDAR 和粒子滤波（MCL）进行定位。
    3.  **RL 决策**：PPO 策略根据当前状态输出 $L_d$。
    4.  **几何控制**：纯追踪算法根据 $L_d$ 计算瞬时曲率 $\gamma_t$，经过低通滤波后转换为转向角 $\delta_t$。

#### 3. 实验设计
*   **实验场景**：
    *   **仿真**：使用 F1TENTH Gym 模拟器。在 Austin 赛道训练，在 **Montreal** 和 **Yas Marina** 两个未见过的赛道进行零样本（Zero-shot）迁移测试。
    *   **实车**：使用 1:10 比例的 RoboRacer 自主赛车平台进行验证。
*   **对比基准（Benchmarks）**：
    1.  **固定前视距离 PP (Fixed-L PP)**：使用手动调优的固定 $L_d$。
    2.  **自适应启发式 PP (Adaptive PP)**：基于速度线性缩放 $L_d$ 的规则方法。
*   **评估指标**：平均单圈耗时、标准差（稳定性）、最大可承受速度缩放比例。

#### 4. 资源与算力
*   **硬件平台**：Dell Precision 3660 工作站，配置 Intel Core i9-13900 处理器，32 GiB RAM。
*   **显卡/计算环境**：使用 NVIDIA/Mesa Intel 显卡（主要依赖 CPU 进行强化学习训练）。
*   **训练时长**：总计训练 800,000 个环境步长，耗时约 **4 小时**。
*   **软件框架**：ROS2, Stable-Baselines3, Optuna（用于超参数优化）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真中对两个未知赛道各进行了 10 圈连续行驶测试。
    *   实车环境下进行了 10 圈连续行驶测试。
    *   提供了 PPO 训练过程中的奖励曲线、解释方差（Explained Variance）和熵（Entropy）等诊断数据。
*   **充分性与客观性**：实验设计较为客观。作者不仅对比了固定参数，还对比了基于规则的自适应方法，证明了 RL 带来的增益并非仅仅因为“动态调整”，而是因为“上下文感知的智能调整”。通过在未见过赛道上的测试，验证了算法的泛化能力。

#### 6. 主要结论与发现
*   **性能提升**：RL 驱动的动态前视距离在所有测试中均取得了最快单圈成绩。例如在 Montreal 赛道，比自适应基准快约 1 秒。
*   **鲁棒性增强**：RL 方法能容忍更激进的速度曲线（如 +15% 提速），而固定参数方法在相同速度下会崩溃（DNF）。
*   **行为模式**：观察发现，策略学到了“直道增加 $L_d$ 以求稳，弯道减小 $L_d$ 以求准”的直觉行为。
*   **Sim-to-Real**：成功实现了从仿真到 1:10 实车的零样本迁移，证明了该混合框架对模型误差具有一定的容忍度。

#### 7. 优点（亮点）
*   **可解释性强**：与端到端（End-to-End）学习转向指令的方法不同，该方法只调整一个具有明确物理意义的参数（$L_d$），保留了经典控制器的结构和透明度。
*   **易于集成**：基于 ROS2 开发，能够无缝接入现有的模块化自动驾驶技术栈。
*   **泛化能力出色**：仅在单一赛道训练即可在多个不同几何特征的赛道上直接运行，减少了繁琐的人工调参工作。

#### 8. 不足与局限
*   **实验覆盖面**：实车实验仅对比了固定 $L_d$ 方案，未在硬件上对比启发式自适应方案。
*   **避障能力**：虽然训练中考虑了 LiDAR 碰撞惩罚，但实验主要集中在单车竞速循迹，未深入探讨多车博弈或动态避障场景。
*   **模型依赖**：虽然是 RL 方法，但仍依赖于高质量的预计算赛车线和速度曲线，若全局规划失效，该控制器的表现尚不明确。
*   **硬件规模**：实验仅限于 1:10 缩比车模型，在全尺寸赛车上的动力学效应（如轮胎滑移、空气动力学）可能更为复杂，需进一步验证。

（完）
