---
title: Flow-based Policy With Distributional Reinforcement Learning in Trajectory Optimization
title_zh: 轨迹优化中结合分布强化学习的基于流的策略
authors: "Ruijie Hao, Longfei Zhang, Yang Dai, Yang Ma, Xingxing Liang, Guangquan Cheng"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00977v1"
tags: ["query:rl"]
score: 9.0
evidence: 分布强化学习与基于流的策略优化
tldr: 针对传统强化学习中高斯策略难以捕捉多峰分布以及均值回报引导不足的问题，本文提出FP-DRL算法。该算法利用流匹配技术建模策略，以提升对复杂分布的拟合能力与计算效率，并结合分布强化学习优化完整的回报分布。在MuJoCo基准测试中，FP-DRL在多项任务中达到SOTA水平，展现了卓越的策略表达能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00977v1/fig-001.webp\", \"caption\": \"Fig. 3. Training curves on benchmarks. The solid lines correspond to mean and shaded regions correspond to the standard error of the mean (SEM) over Three runs.\", \"page\": 12, \"index\": 1, \"width\": 687, \"height\": 364}, {\"url\": \"assets/figures/arxiv/2604.00977v1/fig-002.webp\", \"caption\": \"Fig. 2. Benchmarks. (a) Humanoid-v4: (s × a) ∈ R376 × R17. (b) Ant-v4: (s × a) ∈ R111 ×R8. (c) Hopper-v4: (s× a) ∈ R11 ×R3. (d) HalfCheetah-v4: (s× a) ∈ R17 ×R6. (e) InvertedPendulum-v: (s× a) ∈ R4 × R1. (f) Reacher-v4: (s× a) ∈ R11 × R2.\", \"page\": 11, \"index\": 2, \"width\": 685, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2604.00977v1/fig-003.webp\", \"caption\": \"Fig. 1. FM learns a velocity fields that transports samples from a simple prior distribution to the target data distribution via an ordinary differential equation (ODE).\", \"page\": 6, \"index\": 3, \"width\": 546, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2604.00977v1/fig-004.webp\", \"caption\": \"Fig. 4. Ablation studies of FP-DRL. (a) Performance comparison between a Gaussian policy and the proposed transformer-based flow policy. (b) Comparison of modeling returns using the mean versus a quantile-based distributional critic. (c) Effect of varying the number of quantiles (N=16, 32, and 64) for the distributional critic. (d) Training curves across different transformer sequence lengths (K=4, 7, 10, and 12).\", \"page\": 13, \"index\": 4, \"width\": 593, \"height\": 422}]"
motivation: 传统RL算法采用的高斯策略和均值回报无法有效处理多解问题中的多峰分布特性。
method: 提出FP-DRL算法，结合流匹配技术建模策略并利用分布强化学习优化回报分布。
result: 在MuJoCo控制任务中取得了SOTA性能，并证明了流策略在复杂分布表示上的优越性。
conclusion: FP-DRL通过增强策略的表达能力和回报的建模深度，显著提升了智能体在复杂轨迹优化任务中的表现。
---

## 摘要
强化学习 (RL) 已被证明在处理复杂的控制和决策任务方面非常有效。然而，在大多数传统的强化学习算法中，策略通常被参数化为对角高斯分布，这限制了策略捕捉多峰分布的能力，使其难以覆盖多解问题中的所有最优解；同时，回报被简化为均值，丢失了其多峰特性，从而无法为策略更新提供充足的指导。针对这些问题，我们提出了一种名为“结合分布强化学习的基于流的策略”（FP-DRL）的强化学习算法。该算法利用流匹配（flow matching）对策略进行建模，既保证了计算效率，又具备拟合复杂分布的能力。此外，它采用分布强化学习方法来建模和优化整个回报分布，从而更有效地指导多峰策略更新并提升智能体的性能。在 MuJoCo 基准测试上的实验表明，FP-DRL 算法在大多数 MuJoCo 控制任务中达到了最先进（SOTA）的性能，同时展现了流策略卓越的表征能力。

## 速览
**TLDR**：针对传统强化学习中高斯策略难以捕捉多峰分布以及均值回报引导不足的问题，本文提出FP-DRL算法。该算法利用流匹配技术建模策略，以提升对复杂分布的拟合能力与计算效率，并结合分布强化学习优化完整的回报分布。在MuJoCo基准测试中，FP-DRL在多项任务中达到SOTA水平，展现了卓越的策略表达能力。 \
**Motivation**：传统RL算法采用的高斯策略和均值回报无法有效处理多解问题中的多峰分布特性。 \
**Method**：提出FP-DRL算法，结合流匹配技术建模策略并利用分布强化学习优化回报分布。 \
**Result**：在MuJoCo控制任务中取得了SOTA性能，并证明了流策略在复杂分布表示上的优越性。 \
**Conclusion**：FP-DRL通过增强策略的表达能力和回报的建模深度，显著提升了智能体在复杂轨迹优化任务中的表现。

---

## Abstract
Reinforcement Learning (RL) has proven highly effective in addressing complex control and decision-making tasks. However, in most traditional RL algorithms, the policy is typically parameterized as a diagonal Gaussian distribution, which constrains the policy from capturing multimodal distributions, making it difficult to cover the full range of optimal solutions in multi-solution problems, and the return is reduced to a mean value, losing its multimodal nature and thus providing insufficient guidance for policy updates. In response to these problems, we propose a RL algorithm termed flow-based policy with distributional RL (FP-DRL). This algorithm models the policy using flow matching, which offers both computational efficiency and the capacity to fit complex distributions. Additionally, it employs a distributional RL approach to model and optimize the entire return distribution, thereby more effectively guiding multimodal policy updates and improving agent performance. Experimental trails on MuJoCo benchmarks demonstrate that the FP-DRL algorithm achieves state-of-the-art (SOTA) performance in most MuJoCo control tasks while exhibiting superior representation capability of the flow policy.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：FP-DRL (Flow-based Policy with Distributional RL)

#### 1. 核心问题与研究动机
*   **核心问题**：传统强化学习（RL）算法（如 SAC, TD3）通常将策略参数化为**单峰高斯分布**，并将回报（Return）简化为**标量均值**。
*   **研究背景**：在复杂的控制任务中，最优策略往往具有**多峰性**（即存在多个等效的最优动作），高斯分布无法捕捉这种特性，容易陷入局部最优。同时，仅靠均值回报会丢失风险信息和回报分布的细节，无法为复杂策略提供足够的更新引导。
*   **整体含义**：本文旨在结合**流匹配（Flow Matching）**的高效生成能力与**分布强化学习（Distributional RL）**的丰富表征能力，解决多解问题中的策略表达受限和引导信号不足的问题。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：将策略建模为流匹配模型以拟合多峰动作分布，并利用分位数回归（Quantile Regression）建模完整的回报分布作为 Critic，从而实现更精准的策略梯度引导。
*   **关键技术细节**：
    *   **基于流的策略表示（Flow-based Policy）**：不同于需要多步迭代的扩散模型（Diffusion），流匹配通过学习向量场（Velocity Field），利用常微分方程（ODE）将简单的噪声分布（高斯）映射到复杂的动作分布。本文使用 Transformer 架构来近似速度场 $v_\theta$。
    *   **分布 Critic 学习**：采用分位数回归方法，将回报分布 $Z(s, a)$ 表示为 $N$ 个分位点（Quantiles）。通过最小化分位数 Huber 损失（Quantile Huber Loss）来逼近 1-Wasserstein 距离，从而捕捉回报的 aleatoric 不确定性。
    *   **SAC 框架集成**：将上述流策略与分布 Critic 整合进软行为者-评论者（Soft Actor-Critic）框架。策略更新时，通过数值积分计算动作的对数概率（用于熵正则化），并直接最大化分位数分布的期望值。

#### 3. 实验设计
*   **实验场景**：使用了 **MuJoCo** 物理引擎中的 6 个连续控制基准任务：`Humanoid-v4`, `Ant-v4`, `HalfCheetah-v4`, `Hopper-v4`, `Reacher-v4`, `InvertedPendulum-v4`。
*   **对比基准（Baselines）**：
    *   经典算法：**SAC**, **TD3**。
    *   分布强化学习算法：**DSAC-T**。
    *   生成式策略算法：**SAC-Flow**, **DACER**（基于扩散模型）。
*   **评估指标**：训练 100 万步，取最后 10% 步数的平均回报，并在 3 个随机种子下取均值和标准差。

#### 4. 资源与算力
*   **硬件设备**：Intel Xeon Gold 6430 32核 CPU，**NVIDIA GeForce RTX 4090 GPU**。
*   **软件框架**：同时提供了 **JAX** 和 **PyTorch** 版本。
*   **效率提升**：文中提到使用 JAX 框架相比纯 PyTorch 实现了 **2-3倍** 的训练加速。
*   **训练时长**：未明确给出具体的小时数，但设定了统一的 100 万步训练上限。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 6 个主流 MuJoCo 任务上进行了对比实验。
    *   **消融实验**：针对“仅用流策略”、“仅用分布 Critic”、“Transformer 序列长度（K=4,7,10,12）”、“分位数数量（N=16,32,64）”四个维度进行了详细测试。
*   **充分性评价**：实验设计较为充分，涵盖了从简单到极高维度（如 Humanoid）的任务。通过消融实验证明了流匹配与分布 RL 结合的协同效应。对比算法包含了最新的 SOTA 算法（如 SAC-Flow），具有较强的说服力。

#### 6. 主要结论与发现
*   **性能领先**：FP-DRL 在大多数任务中达到了 **SOTA** 水平。例如在 `Ant-v4` 任务中，比 SAC-Flow 提升了 21.6%，比 DACER 提升了 81.6%。
*   **协同效应**：实验证明，分布 Critic 提供的丰富信号能显著提升流策略的训练稳定性；而流策略的多峰表达能力则能更好地利用分布 Critic 提供的信息。
*   **超参数敏感性**：发现较短的 ODE 步数（K=4）和适中的分位数（N=32）在性能与效率之间达到了最佳平衡。

#### 7. 优点
*   **高效性**：相比扩散模型策略，流匹配策略在推理时步数更少，更适合实时控制任务。
*   **表达力强**：成功打破了高斯分布的局限，能够处理具有多个最优解的复杂轨迹优化问题。
*   **开源贡献**：提供了 JAX 和 PyTorch 双版本代码，有利于社区复现和后续研究。

#### 8. 不足与局限
*   **计算开销**：虽然比扩散模型快，但流匹配在训练过程中仍涉及 ODE 数值积分和迹运算（Hutchinson 估计），计算成本高于传统的高斯策略。
*   **随机种子数量**：实验仅使用了 3 个随机种子，在强化学习领域，通常建议使用 5-10 个种子以进一步排除偶然性。
*   **应用范围**：目前仅在 MuJoCo 仿真环境中验证，尚未在真实机器人硬件或更复杂的视觉输入任务（如像素级 RL）中进行测试。

（完）
