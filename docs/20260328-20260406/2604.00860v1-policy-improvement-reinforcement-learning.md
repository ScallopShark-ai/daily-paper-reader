---
title: Policy Improvement Reinforcement Learning
title_zh: 策略提升强化学习
authors: "Huaiyang Wang, Xiaojie Li, Deqing Wang, Haoyi Zhou, Zixuan Huang, Yaodong Yang, Jianxin Li, Yikun Ban"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00860v1"
tags: ["query:rl"]
score: 9.0
evidence: 策略改进反馈的强化学习范式
tldr: 针对大语言模型强化学习（RLVR）中存在的“开环”优化问题，即仅依赖批次内信号而忽视迭代间改进验证，本文提出PIRL框架，将优化目标转为最大化累积策略改进。基于此开发的PIPO算法通过回顾性验证机制，对比滑动窗口历史基准来动态调整更新方向，实现了自校正的闭环优化，显著提升了数学推理任务的训练稳定性和最终性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00860v1/fig-001.webp\", \"caption\": \"Figure 4: Efficiency on MATH dataset. (a) Step Overhead: PIPO incurs a 12–19% per-step latency increase (b) Wall-clock Efficiency: Despite lower throughput, PIPO reaches higher accuracy faster than GRPO\", \"page\": 10, \"index\": 1, \"width\": 734, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2604.00860v1/fig-002.webp\", \"caption\": \"Figure 2: Theoretical distortion and empirical instability of GRPO. (a) Gradient Distortion: The gradient scaling factor η(pt) evaluated across success rates pt. As established in Corollary 3.2, GRPO (G = 8, 128) exhibit severe sensitivity explosion at the boundaries (pt → 0, 1). (b) Empirical Stability: Standard GRPO suffers from drastic gradient norm spikes (left) and severe Pass@1 collapse (right). Incorporating PIPO effectively stabilizes training.\", \"page\": 3, \"index\": 2, \"width\": 963, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.00860v1/fig-003.webp\", \"caption\": \"Table 5: Hyperparameter settings for standard Group-relative methods (GRPO, GSPO, DAPO) and their PIPO-enhanced versions\", \"page\": 23, \"index\": 3, \"width\": 972, \"height\": 463}, {\"url\": \"assets/figures/arxiv/2604.00860v1/fig-004.webp\", \"caption\": \"Table 1: Main Results on Mathematical Reasoning. We report Pass@1 accuracy (%) for Qwen3-4B-Base and Qwen3-8B-Base. PIPO is applied on top of three baselines: GRPO, GSPO, and DAPO\", \"page\": 9, \"index\": 4, \"width\": 968, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2604.00860v1/fig-005.webp\", \"caption\": \"Figure 3: Comparison of training dynamics. (a-b) Accuracy evolution on 4B and 8B models. (c-d) Response length evolution. PIPO (solid lines) consistently outperforms baselines (dashed lines) and promotes sustained reasoning chain growth.\", \"page\": 9, \"index\": 5, \"width\": 964, \"height\": 225}]"
motivation: 现有RLVR方法缺乏迭代间的策略改进反馈，仅靠瞬时批次统计量指导更新，容易导致优化漂移或模型崩溃。
method: 提出PIPO算法，利用回顾性验证机制对比当前策略与历史基准，通过强化有益更新和抑制有害更新实现闭环优化。
result: 在数学推理基准测试中，PIPO在性能和训练稳定性上均显著优于GRPO及其变体。
conclusion: 引入策略改进反馈将开环优化转为自校正闭环过程，是提升大语言模型推理能力和训练鲁棒性的有效途径。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为提升大语言模型推理能力的核心后训练范式。然而，现有方法存在一个共同的盲点：它们基于瞬时的组级或批次级统计量来优化策略，而从未验证所产生的更新是否真正提升了模型。这种开环设计——在每一步中孤立地进行更新，且仅受组内（批次内）奖励信号的引导——意味着优化过程可能会发生漂移或崩溃，且缺乏检测和纠正这些失败的机制。我们认为，缺失的关键要素是策略提升反馈：即直接衡量和优化迭代间进展的能力。为此，我们引入了策略提升强化学习（PIRL），该框架将代理奖励最大化替换为最大化跨迭代累积策略提升的显式目标，并证明了这一时间目标与最大化最终任务性能是完全一致的。基于 PIRL，我们提出了策略提升策略优化（PIPO），它通过回溯验证实现了闭环优化。在每次迭代中，PIPO 会根据滑动窗口历史基准评估之前的更新是否产生了真正的提升，随后积极强化有益的更新并抑制有害的更新，从而将开环过程转变为自我纠正过程。我们提供的理论分析表明，PIPO 在期望上对 PIRL 目标执行上升操作；在数学推理基准测试上的实验证明，相比于 GRPO 及其变体，该方法具有更好的稳定性和性能。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has become a central post-training paradigm for improving the reasoning capabilities of large language models. Yet existing methods share a common blind spot: they optimize policies based on instantaneous group-level or batch-level statistics without ever verifying whether the resulting update actually improved the model. This open-loop design -- updating in isolation at each step, guided only by within-group (batch) reward signals -- means optimization can drift or collapse with no mechanism to detect and correct these failures. We argue that the missing ingredient is policy improvement feedback: the ability to measure and optimize inter-iteration progress directly. To this end, we introduce Policy Improvement Reinforcement Learning (PIRL), a framework that replaces surrogate reward maximization with the explicit objective of maximizing cumulative policy improvement across iterations, and prove this temporal objective is perfectly aligned with maximizing final task performance. Building on PIRL, we propose Policy Improvement Policy Optimization (PIPO), which implements closed-loop optimization through retrospective verification. At each iteration, PIPO evaluates whether the previous update yielded genuine improvement against a sliding-window historical baseline, then actively reinforces beneficial updates and suppresses the harmful ones -- transforming an open-loop process into a self-correcting one. We provide theoretical analysis showing that PIPO performs ascent on the PIRL objective in expectation, and experiments on mathematical reasoning benchmarks demonstrate improved stability and performance over GRPO and its variants.

---

## 论文详细总结（自动生成）

这篇论文由北京大学和北京航空航天大学的研究团队合作完成，提出了一种名为 **PIPO (Policy Improvement Policy Optimization)** 的新型强化学习算法，旨在解决大语言模型（LLM）在推理任务后训练中的稳定性与性能瓶颈。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的可验证奖励强化学习（RLVR）方法（如 GRPO、PPO 等）本质上是**“开环（Open-loop）”**的。它们仅根据当前批次（Batch）或组（Group）内的瞬时统计量进行策略更新，而从未验证该更新是否真正提升了模型的整体性能。
*   **研究动机**：
    *   **梯度畸变与不稳定性**：作者从理论上证明了 GRPO 在成功率极高或极低（$p \to 0$ 或 $1$）的边界区域存在“敏感性爆炸”，导致梯度噪声剧增，引发训练崩溃或模式坍塌。
    *   **缺乏反馈机制**：由于缺乏迭代间的性能对比，模型可能在错误的优化方向上持续漂移，尤其是在奖励稀疏的推理任务中。

### 2. 方法论：PIRL 框架与 PIPO 算法
*   **核心思想**：将优化目标从“最大化瞬时代理奖励”转变为**“最大化跨迭代的累积策略提升”**。通过引入回顾性验证，将开环优化转变为自校正的**闭环（Closed-loop）**过程。
*   **关键技术细节**：
    *   **PIRL 目标函数**：定义策略提升 $\Delta J_t = J(\theta_t) - J(\theta_{t-1})$，证明最大化累积提升等同于最大化最终策略性能。
    *   **PIPO 算法流程**：
        1.  **阶段 1：前向探索（Forward Exploration）**：使用标准方法（如 GRPO）进行一次常规的策略更新。
        2.  **阶段 2：回顾性验证（Retrospective Verification）**：在下一次迭代中，通过新采样的批次均值与滑动窗口历史基准（Historical Baseline）对比，计算标准化提升信号 $\xi_t$。
    *   **策略提升奖励（PI Reward）**：
        $$\hat{r}_{PI} = \text{局部归因（组内优势）} \times \text{全局验证（提升信号的截断函数 } \phi(\xi_t))$$
        该奖励机制会根据验证结果动态调整：若上一步更新确实带来了提升（$\xi_t > 0$），则强化该更新；若导致性能下降（$\xi_t < 0$），则抑制或修正该更新。

### 3. 实验设计
*   **模型规模**：Qwen3-4B-Base 和 Qwen3-8B-Base（均使用基础模型以测试从零激发推理能力的能力）。
*   **训练数据集**：MATH 数据集（约 7.5k 题目）。
*   **评测基准（Benchmarks）**：
    *   **数学推理**：MATH500、AIME 2025、AMC 2023、MINERVA。
    *   **科学推理**：SciKnowEval（涵盖化学、生物、材料、物理）。
*   **对比方法**：GRPO、GSPO、DAPO 及其对应的 PIPO 增强版。

### 4. 资源与算力
*   **硬件环境**：单节点 8 × NVIDIA A100 (80GB) GPU。
*   **软件框架**：基于 VeRL 框架实现。
*   **训练细节**：训练 3-9 个 Epoch，Batch Size 为 128，最大响应长度 4096 标记。
*   **效率说明**：PIPO 相比 GRPO 增加了约 **12%–19%** 的单步耗时（由于需要计算回顾性梯度和缓存 Logits），但由于收敛更快且更稳定，其**墙钟时间（Wall-clock time）效率反而更高**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个模型参数量级（4B/8B）和三个主流 RL 算法变体。
*   **消融实验**：针对滑动窗口大小 $K$（取值 4 到 16）和修正范围 $\phi$（单向 vs 双向修正）做了详细对比。
*   **充分性评价**：实验设计较为充分，不仅验证了数学能力的提升，还通过 SciKnowEval 验证了泛化性。对比实验在相同超参数下进行，保证了公平性。

### 6. 主要结论与发现
*   **性能提升**：PIPO 在所有基准测试中均一致性地优于原始算法。例如，在 AIME 2025 上，GSPO+PIPO 相比 GSPO 提升了 7.4% 的绝对准确率。
*   **训练稳定性**：PIPO 有效消除了 GRPO 中常见的梯度尖峰，防止了 Pass@1 准确率的突然坍塌。
*   **推理深度**：PIPO 鼓励模型生成更长、更复杂的思维链（CoT），响应长度随训练稳步增长。

### 7. 优点
*   **理论扎实**：对 GRPO 的梯度畸变给出了严谨的数学证明，并证明了 PIRL 目标与最终性能的一致性。
*   **即插即用**：PIPO 作为一个元框架，可以无缝集成到现有的 PPO 或 GRPO 风格的方法中，无需额外的 Critic 网络。
*   **自校正能力**：通过历史基准引入了时间维度的信任区域，使模型具备了“反思”上一步更新质量的能力。

### 8. 不足与局限
*   **计算开销**：虽然总训练时间可能缩短，但单步计算量和显存占用（需缓存前一步数据）有所增加。
*   **超参数敏感性**：滑动窗口 $K$ 的选择对性能有一定影响，过大或过小都会削弱提升效果。
*   **应用范围限制**：该方法高度依赖于“可验证奖励（Verifiable Rewards）”，对于主观性强、缺乏客观评判标准的通用对话任务，其效果尚未验证。

（完）
