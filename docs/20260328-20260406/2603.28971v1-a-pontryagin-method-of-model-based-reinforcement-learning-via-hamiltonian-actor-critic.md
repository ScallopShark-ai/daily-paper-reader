---
title: A Pontryagin Method of Model-based Reinforcement Learning via Hamiltonian Actor-Critic
title_zh: 一种通过哈密顿 Actor-Critic 实现的基于模型的强化学习庞特里亚金方法
authors: "Chengyang Gu, Yuxin Pan, Hui Xiong, Yize Chen"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28971v1"
tags: ["query:rl"]
score: 10.0
evidence: 通过哈密顿Actor-Critic实现的基于模型的强化学习
tldr: 针对基于模型的强化学习中长时序价值估计受模型误差累积影响的问题，本文受庞特里亚金极大值原理启发，提出了哈密顿行动者-评论家（HAC）算法。该方法通过直接优化定义在动力学和奖励函数上的哈密顿量，避免了显式的价值函数学习，从而降低了对模型误差的敏感性并提供收敛保证。实验证明，HAC在连续控制任务的在线和离线设置中均优于现有基准，展现出极高的采样效率和对分布偏移的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28971v1/fig-001.webp\", \"caption\": \"Fig. 2: Learning Curves Comparison (Offline RL). We compare our HAC with IQL, SAC-Off and MOPO on LQR, Pendulum, and MountainCar, Swimmer. For MOPO and HAC which require multi-step imaginary rollouts, rollout horizon is to K = 10 for LQR and Pendulum to cover all control horizon, K = 5 for MountainCar and Swimmer. Results are averaged over 5 random seeds.\", \"page\": 7, \"index\": 1, \"width\": 512, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2603.28971v1/fig-002.webp\", \"caption\": \"Fig. 4: Critic Estimation Comparison: Plots comparing normalized estimated Q-values (in MVE-DDPG) and Hamiltonian (in our HAC) against ground-truth sampled from 10 random test episodes in LQR. An ideal critic should concentrates on the red unity line.\", \"page\": 18, \"index\": 2, \"width\": 1054, \"height\": 455}, {\"url\": \"assets/figures/arxiv/2603.28971v1/fig-003.webp\", \"caption\": \"Fig. 1: Learning Curves Comparison (Online RL): We compare our HAC with DDPG, SAC and MVE-DDPG on Pendulum, MountainCar, Swimmer and Hopper. For MVEDDPG and HAC which require multi-step imaginary rollouts, we set the rollout horizon to K = 10 for Pendulum to cover all control horizon, K = 5 for MountainCar and Swimmer, and K = 3 for Hopper. All results are averaged over 5 random seeds.\", \"page\": 6, \"index\": 3, \"width\": 512, \"height\": 422}]"
motivation: 传统的基于模型的强化学习方法在长时序价值估计时，容易受到模型误差累积和价值函数近似偏差的影响。
method: 提出HAC算法，利用庞特里亚金极大值原理直接优化哈密顿量，从而在不依赖显式价值函数的情况下实现策略优化。
result: 在连续控制基准测试中，HAC在收敛速度、控制性能以及对分布外（OOD）场景的鲁棒性方面均显著优于模型无关和MVE等基准方法。
conclusion: HAC通过引入哈密顿优化机制有效规避了价值近似难题，为提升强化学习的采样效率和稳健性提供了一种高效的新方案。
---

## 摘要
基于模型的强化学习 (MBRL) 通过利用学习到的动力学模型进行策略优化，提高了样本效率。然而，Actor-Critic 等方法的有效性往往受到复合模型误差的限制，这些误差会降低长时程价值估计的准确性。现有方法（如基于模型的价值扩展，MVE）通过多步展开部分缓解了这一问题，但仍对展开时界的选择和残差模型偏差较为敏感。受庞特里亚金极大值原理 (PMP) 的启发，我们提出了哈密顿 Actor-Critic (HAC)，这是一种基于模型的方法，通过直接优化在确定性系统的学习动力学和奖励上定义的哈密顿量，消除了显式的价值函数学习。通过避免价值近似，HAC 降低了对模型误差的敏感性，同时提供了收敛性保证。在在线和离线强化学习设置下的连续控制基准测试中，大量实验表明，HAC 在控制性能、收敛速度以及对分布偏移（包括分布外 OOD 场景）的鲁棒性方面均优于无模型和基于 MVE 的基准方法。在数据有限的离线设置中，HAC 达到或超过了现有最先进方法的水平，突显了其强大的样本效率。

## 速览
**TLDR**：本研究针对基于模型的强化学习中模型误差累积导致价值估计不准的问题，提出了一种基于庞特里亚金最大值原理（PMP）的哈密顿行动者-评论家（HAC）算法。该方法通过直接优化哈密顿函数取代了传统的显式价值函数学习，有效降低了对模型误差的敏感性。实验表明，HAC在在线和离线连续控制任务中均优于现有基准，展现出极高的样本效率、收敛速度以及对分布外（OOD）场景的强鲁棒性。 \
**Motivation**：解决基于模型的强化学习中，因模型误差累积导致的长时域价值估计不准及对超参数敏感的问题。 \
**Method**：借鉴庞特里亚金最大值原理，提出直接优化哈密顿函数而非学习价值函数的HAC算法。 \
**Result**：在多项连续控制任务中，HAC在性能、收敛速度及抗分布偏移能力上均显著优于模型无关和基于MVE的方法。 \
**Conclusion**：HAC通过消除价值函数近似，为确定性系统的模型化强化学习提供了一种更高效、更稳健的优化框架。

---

## Abstract
Model-based reinforcement learning (MBRL) improves sample efficiency by leveraging learned dynamics models for policy optimization. However, the effectiveness of methods such as actor-critic is often limited by compounding model errors, which degrade long-horizon value estimation. Existing approaches, such as Model-Based Value Expansion (MVE), partially mitigate this issue through multi-step rollouts, but remain sensitive to rollout horizon selection and residual model bias. Motivated by the Pontryagin Maximum Principle (PMP), we propose Hamiltonian Actor-Critic (HAC), a model-based approach that eliminates explicit value function learning by directly optimizing a Hamiltonian defined over the learned dynamics and reward for deterministic systems. By avoiding value approximation, HAC reduces sensitivity to model errors while admitting convergence guarantees. Extensive experiments on continuous control benchmarks, in both online and offline RL settings, demonstrate that HAC outperforms model-free and MVE-based baselines in control performance, convergence speed, and robustness to distributional shift, including out-of-distribution (OOD) scenarios. In offline settings with limited data, HAC matches or exceeds state-of-the-art methods, highlighting its strong sample efficiency.

---

## 论文详细总结（自动生成）

### 论文总结：基于哈密顿 Actor-Critic 的 MBRL 庞特里亚金方法

#### 1. 核心问题与整体含义（研究动机和背景）
在基于模型的强化学习（MBRL）中，提高样本效率的核心在于利用学习到的动力学模型进行策略优化。然而，传统的 **Actor-Critic** 架构在 MBRL 中面临两大挑战：
*   **复合模型误差（Compounding Model Errors）：** 在想象的轨迹展开（Rollout）中，微小的模型偏差会随步数累积，导致长时序价值估计严重失真。
*   **价值近似偏差：** 现有的价值扩展方法（如 MVE）高度依赖 Critic 网络对 Q 值的近似，而 Critic 本身又受限于模型生成的虚假轨迹，形成负反馈循环，导致训练不稳定且对展开步数（Horizon）极其敏感。
**研究动机：** 受经典控制理论中**庞特里亚金极大值原理（PMP）**的启发，探索是否可以跳过显式的价值函数（Critic）学习，直接利用动力学模型和哈密顿量（Hamiltonian）来指导策略优化。

#### 2. 论文提出的方法论：HAC 算法
*   **核心思想：** 提出 **Hamiltonian Actor-Critic (HAC)**。在确定性动力学系统中，将 RL 目标转化为最优控制问题。通过定义哈密顿量 $H$（包含即时奖励和伴随状态与动力学的内积），利用 PMP 提供的必要条件代替传统的 Critic 网络。
*   **关键技术细节：**
    *   **哈密顿量定义：** $H(s_t, a_t, \lambda_{t+1}) = c(s_t, a_t) + \lambda_{t+1}^\top f(s_t, a_t)$，其中 $c$ 是代价函数，$\lambda$ 是伴随状态（Costate）。
    *   **伴随状态递归：** 通过反向递归计算 $\lambda_t = \nabla_{s_t} H$，从而捕捉未来奖励对当前状态的敏感度。
    *   **解析雅可比技术（Analytical Jacobian）：** 为了解决自动微分（Autograd）在计算高阶梯度时效率低且易报错的问题，针对 ReLU 激活的神经网络动力学模型，推导了闭式解析雅可比矩阵，实现了“无图化”的高效计算。
*   **算法流程：** 
    1. 学习动力学模型 $\hat{f}$。
    2. 在想象空间进行 $K$ 步展开。
    3. 从终端状态开始反向递归计算伴随状态 $\hat{\lambda}$。
    4. 计算各步的哈密顿量，并以此作为 Loss 直接更新 Actor 策略网络，无需训练 Critic 网络。

#### 3. 实验设计
*   **实验场景：** 涵盖了从简单到复杂的连续控制任务，包括 LQR（线性二次调节器）、Pendulum（倒立摆）、MountainCar（登山车）以及 MuJoCo 物理引擎中的 Swimmer 和 Hopper。
*   **Benchmark 与对比方法：**
    *   **在线 RL：** 对比了 DDPG、SAC（无模型基准）和 MVE-DDPG（基于模型的价值扩展基准）。
    *   **离线 RL：** 对比了 IQL、Offline-SAC（无模型离线）和 MOPO（最先进的基于模型的离线方法）。
    *   **鲁棒性测试：** 针对初始状态偏移（Distributional Shift）进行了专门的 OOD 实验。

#### 4. 资源与算力
*   **硬件环境：** 使用了 Google Colaboratory 实例，配置为 **NVIDIA T4 GPU (16GB)** 和 12GB RAM。
*   **模型规模：** 动力学模型、策略网络均采用 3 层 MLP（每层 128 个神经元）。
*   **训练时长：** 文中未明确给出总训练小时数，但在消融实验中提到 HAC 的单次梯度更新耗时略高于单 Critic 的 MVE，但远低于多 Critic 集成（Ensemble）的方法。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 5 个不同复杂度的环境测试，涵盖在线和离线两种主流模式。
*   **重复性：** 所有实验结果均基于 **5 个随机种子** 的平均值，并标注了标准差（阴影区域）。
*   **消融与理论验证：** 包含了针对集成数量（Ensemble size）的消融实验，以及针对 Critic 估计误差的理论界限验证实验。
*   **充分性评价：** 实验设计较为全面，不仅验证了性能，还通过 LQR 任务验证了理论推导的正确性（如误差界限），实验对比具有客观性。

#### 6. 主要结论与发现
*   **性能优越：** HAC 在大多数任务中收敛速度更快，且最终奖励值高于或等同于最先进的基准方法。
*   **误差界限更紧：** 理论和实验均证明，HAC 的哈密顿量估计误差上限低于 MVE 的价值估计误差上限。
*   **高采样效率：** 在离线设置下，HAC 仅需约 25%-30% 的样本量即可达到其他方法的效果。
*   **强鲁棒性：** 在面对初始状态偏移等 OOD 场景时，HAC 表现出比 MVE 更好的稳定性，不会出现轨迹发散。

#### 7. 优点
*   **架构简化：** 彻底消除了 Actor-Critic 框架中对 Critic 网络的依赖，减少了超参数（如 Critic 学习率、目标网络更新频率等）。
*   **理论支撑扎实：** 结合了最优控制理论（PMP）与深度学习，提供了收敛性证明和误差界限分析。
*   **计算优化：** 提出的解析雅可比技术显著提升了基于梯度的伴随状态计算效率。

#### 8. 不足与局限
*   **系统限制：** 目前的方法主要针对**确定性动力学系统**设计，虽然附录提到了随机系统的扩展思路，但主实验尚未覆盖复杂的随机环境。
*   **对模型的依赖：** 性能高度依赖于动力学模型 $\hat{f}$ 的质量，如果模型在某些区域完全失效，HAC 缺乏像 MOPO 那样的显式不确定性惩罚机制。
*   **特定奖励函数：** 理论证明部分假设奖励函数为二次型或线性，对于极其稀疏或非平滑的奖励函数，其表现尚待进一步验证。

（完）
