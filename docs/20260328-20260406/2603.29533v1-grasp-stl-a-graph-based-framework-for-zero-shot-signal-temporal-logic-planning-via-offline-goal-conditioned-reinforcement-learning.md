---
title: "GraSP-STL: A Graph-Based Framework for Zero-Shot Signal Temporal Logic Planning via Offline Goal-Conditioned Reinforcement Learning"
title_zh: GraSP-STL：一种通过离线目标条件强化学习实现零样本信号时序逻辑规划的基于图的框架
authors: "Ancheng Hou, Ruijia Liu, Xiang Yin"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29533v1"
tags: ["query:rl"]
score: 9.0
evidence: 用于零样本规划的离线目标条件强化学习
tldr: 本研究提出GraSP-STL框架，旨在解决离线零样本信号时序逻辑（STL）规划问题。该方法仅利用任务无关的离线数据集，无需环境交互或针对特定任务的重训，即可合成满足任意未知STL规范的控制策略。通过学习目标条件价值函数构建状态空间图抽象，将规划转化为图搜索问题，并利用算术几何平均鲁棒性进行评估。该框架实现了可重用可达性学习与任务规划的分离，显著提升了长时程复杂任务的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29533v1/fig-001.webp\", \"caption\": \"Fig. 3: Case study of planning and execution.\", \"page\": 7, \"index\": 1, \"width\": 512, \"height\": 538}, {\"url\": \"assets/figures/arxiv/2603.29533v1/fig-002.webp\", \"caption\": \"TABLE II: Performance Metrics for Large-Scale Evaluation\", \"page\": 8, \"index\": 2, \"width\": 506, \"height\": 691}, {\"url\": \"assets/figures/arxiv/2603.29533v1/fig-003.webp\", \"caption\": \"TABLE I: STL formula templates grouped by difficulty\", \"page\": 6, \"index\": 3, \"width\": 506, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2603.29533v1/fig-004.webp\", \"caption\": \"Fig. 1: Overall framework of proposed GraSP-STL.\", \"page\": 3, \"index\": 4, \"width\": 481, \"height\": 512}]"
motivation: 旨在解决在缺乏环境模型和额外交互的情况下，如何仅利用离线数据实现对任意未知STL任务的零样本规划。
method: 通过学习目标条件价值函数建立可达性度量并构建图抽象，将STL规划转化为基于路径点的图搜索问题，并由目标条件策略执行。
result: 实验结果证明该框架在多种离线STL规划任务中表现优异，能够有效通过组合离线数据中的短时程行为来完成长时程任务。
conclusion: GraSP-STL通过解耦可达性学习与逻辑规划，为离线强化学习在处理复杂时序逻辑任务时提供了高效且泛化性强的解决方案。
---

## 摘要
本文研究了信号时序逻辑（STL）规范下的离线零样本规划。我们假设仅能访问由任务无关的行为策略收集的状态-动作-状态转移离线数据集，且没有解析动力学模型，不进行进一步的环境交互，也不进行特定任务的重新训练。目标是合成一种控制策略，使其生成的轨迹满足任意未见的 STL 规范。为此，我们提出了 GraSP-STL，这是一个基于图搜索的框架，用于从离线轨迹中进行零样本 STL 规划。该方法从离线数据中学习目标条件价值函数，并利用其在状态空间上导出有限时域可达性度量。基于该度量，它构建了一个有向图抽象，其节点表示代表性状态，边编码了可行的短时域转移。规划随后被表述为对路标点序列的图搜索，使用算术-几何平均鲁棒性及其区间语义进行评估，并由学习到的目标条件策略执行。所提出的框架将可重用的可达性学习与任务条件规划分离，从而实现了对未见 STL 任务的零样本泛化，并通过组合离线数据中的短时域行为实现长时域规划。实验结果证明了该方法在一系列离线 STL 规划任务中的有效性。

## Abstract
This paper studies offline, zero-shot planning under Signal Temporal Logic (STL) specifications. We assume access only to an offline dataset of state-action-state transitions collected by a task-agnostic behavior policy, with no analytical dynamics model, no further environment interaction, and no task-specific retraining. The objective is to synthesize a control strategy whose resulting trajectory satisfies an arbitrary unseen STL specification. To this end, we propose GraSP-STL, a graph-search-based framework for zero-shot STL planning from offline trajectories. The method learns a goal-conditioned value function from offline data and uses it to induce a finite-horizon reachability metric over the state space. Based on this metric, it constructs a directed graph abstraction whose nodes represent representative states and whose edges encode feasible short-horizon transitions. Planning is then formulated as a graph search over waypoint sequences, evaluated using arithmetic-geometric mean robustness and its interval semantics, and executed by a learned goal-conditioned policy. The proposed framework separates reusable reachability learning from task-conditioned planning, enabling zero-shot generalization to unseen STL tasks and long-horizon planning through the composition of short-horizon behaviors from offline data. Experimental results demonstrate its effectiveness on a range of offline STL planning tasks.

---

## 论文详细总结（自动生成）

### 论文总结：GraSP-STL 框架

#### 1. 核心问题与整体含义（研究动机和背景）
该论文关注的是**离线零样本信号时序逻辑（STL）规划**问题。
*   **研究动机**：在许多实际场景中，获取精确的系统动力学模型非常困难，且出于安全或成本考虑，无法进行实时的在线环境交互。
*   **核心挑战**：如何在仅拥有一个由任务无关（task-agnostic）策略收集的离线数据集（包含状态-动作转移）的情况下，合成能够满足任意、未见过的复杂时序逻辑任务（如“先去A再去B，且过程中避开C”）的控制策略，且无需针对新任务进行任何重新训练。

#### 2. 方法论：核心思想与技术细节
GraSP-STL 提出了一种将**离线目标条件强化学习（GCRL）**与**图搜索**相结合的分层框架：
*   **可达性度量学习**：利用离线数据集训练一个目标条件价值函数 $V(s, g)$。该函数被转化为一种时间距离度量 $\hat{d}(s, g) = -V(s, g)$，用于评估状态间的有限时域可达性。
*   **图抽象构建**：
    *   **节点采样**：通过网格均匀采样和时间距离感知聚类，从海量离线数据中提取代表性状态作为图节点。
    *   **边构建**：若两节点间的估计距离小于预设阈值 $k$，则建立有向边。采用方向多样性策略确保图的稀疏性与连通性。
*   **基于 AGM 鲁棒性的图搜索**：
    *   将规划问题表述为在抽象图上的路标点（waypoint）序列搜索。
    *   **核心技术**：引入**算术-几何平均（AGM）鲁棒性**及其**区间语义**。相比传统的 min-max 鲁棒性，AGM 更平滑且信息量更大。
    *   **搜索算法**：通过维护一个鲁棒性区间 $[ \underline{\eta}, \bar{\eta} ]$，在搜索过程中对不可行路径进行剪枝，并利用启发式函数引导搜索向高鲁棒性区域进行。
*   **底层执行**：搜索出的路标点序列由学习到的目标条件策略 $\pi(a|s, g)$ 逐段执行。

#### 3. 实验设计
*   **实验场景**：使用了 **OGBench** 中的 **AntMaze** 环境。这是一个具有 29 维状态空间的 8 自由度四足机器人（Ant），任务是在包含障碍物的复杂迷宫中移动。
*   **数据集**：包含 10,000 条由预训练策略收集的轨迹，每条长度为 500 步。
*   **Benchmark 与任务**：设计了 12 种 STL 公式模板（T1-T12），涵盖了基础到达、逻辑析取、顺序访问、安全约束、持久性（Persistence）以及嵌套时序算子等不同难度等级。
*   **对比与评估指标**：主要评估规划成功率（PSR）、执行成功率（ESR）以及规划耗时（PT）。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量以及训练总时长。
*   **实现细节**：提到使用了多层感知机（MLP）来参数化策略和价值函数，并采用了加权行为克隆（Weighted BC）等离线 RL 常用技术。

#### 5. 实验数量与充分性
*   **实验规模**：进行了大规模评估，针对 12 个模板各生成了 200 个随机配置（改变目标位置、大小和起点），总计 **2400 个测试任务**。
*   **充分性评价**：实验设计较为充分。通过从简单到复杂的任务分级，验证了框架在处理长时程（Long-horizon）任务和复杂逻辑结构时的性能。同时，通过案例研究展示了鲁棒性区间随时间收敛的过程，增强了结果的可解释性。

#### 6. 主要结论与发现
*   **高效的零样本能力**：框架在未见任务上表现优异，总体规划成功率达到 **95.58%**，执行成功率达到 **81.58%**。
*   **长时程缝合能力**：证明了通过图搜索可以有效地将离线数据中的短时片段“缝合”起来，完成超出单个片段长度的复杂长时程任务。
*   **计算效率**：即使对于最复杂的嵌套 STL 任务，平均规划时间也控制在 10 秒以内（T12 模板除外，约为 25 秒），体现了良好的可扩展性。

#### 7. 优点（亮点）
*   **解耦设计**：将通用的可达性学习（离线 RL）与具体的逻辑规划（图搜索）分离，使得模型无需重训即可适配新任务。
*   **鲁棒性评估创新**：将 AGM 鲁棒性区间语义引入图搜索，解决了部分观测序列下 STL 满足度难以评估的问题，并实现了高效剪枝。
*   **可解释性**：图搜索生成的路标点序列为复杂的机器人行为提供了清晰的高层解释。

#### 8. 不足与局限
*   **数据依赖性**：规划性能高度依赖于离线数据的空间覆盖程度。如果数据在某些关键区域缺失，图构建将不完整。
*   **离散化偏差**：基于图的抽象本质上是离散的，可能无法找到连续空间中的最优路径。
*   **执行误差敏感**：在时间约束非常紧凑的任务（如 T9 持续性任务）中，底层控制器的微小跟踪误差可能导致整个 STL 公式失效，缺乏一定的“时间裕度（Slack）”考虑。
*   **环境限制**：目前主要在 2D 导航类任务（AntMaze）上验证，对于更复杂的动力学交互任务（如物体操纵）的适用性有待进一步研究。

（完）
