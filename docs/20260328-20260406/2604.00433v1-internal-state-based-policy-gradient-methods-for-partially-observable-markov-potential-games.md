---
title: Internal State-Based Policy Gradient Methods for Partially Observable Markov Potential Games
title_zh: 基于内部状态的部分可观测马尔可夫势博弈策略梯度方法
authors: "Wonseok Yang, Thinh T. Doan"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00433v1"
tags: ["query:rl"]
score: 10.0
evidence: 部分可观测马尔可夫势博弈中的多智能体强化学习
tldr: 本文研究部分可观测马尔可夫势博弈（POMPGs）的多智能体强化学习问题。针对部分可观测性和信息分散带来的挑战，研究者利用公共信息框架并引入内部状态（有限状态控制器）来压缩历史信息，防止维度爆炸。通过提出基于内部状态的自然策略梯度方法，本文首次建立了非渐近收敛界，并将其分解为统计误差和近似误差。仿真结果表明，该方法在多种部分可观测环境下均优于仅使用当前观测的策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00433v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 3000, \"height\": 1879}, {\"url\": \"assets/figures/arxiv/2604.00433v1/fig-002.webp\", \"caption\": \"\", \"page\": 4, \"index\": 2, \"width\": 1256, \"height\": 480}]"
motivation: 针对部分可观测马尔可夫势博弈中信息分散和历史信息随时间无限增长导致的计算与收敛难题。
method: 结合公共信息框架与有限状态控制器，提出一种基于内部状态的自然策略梯度（NPG）算法。
result: 理论上建立了算法的非渐近收敛界，并将其分解为统计误差和有限状态控制器的近似误差。
conclusion: 实验证明，引入内部状态能有效提升智能体在部分可观测环境下的博弈性能，优于仅依赖当前观测的传统方法。
---

## 摘要
本文研究了部分可观测马尔可夫势博弈中的多智能体强化学习。由于部分可观测性、去中心化信息以及维数灾难，解决这一问题具有挑战性。首先，为了应对前两个挑战，我们利用了公共信息框架，该框架允许智能体基于共享信息和局部信息进行决策。其次，为了确保计算的可处理性，我们研究了一种压缩累积信息的内部状态，以防止其随时间无限增长。随后，我们实现了一种基于内部状态的自然策略梯度方法，用于寻找马尔可夫势博弈的纳什均衡。我们的主要贡献是为该方法建立了一个非渐近收敛界。我们的理论界可分解为两个具有解释性的部分：一个是在标准马尔可夫势博弈中也会出现的统计误差项，以及一个反映使用有限状态控制器所产生的近似误差项。最后，在多个部分可观测环境下的仿真实验表明，与仅使用当前观测值的设定相比，所提出的使用有限状态控制器的方法在性能上取得了持续的提升。

## Abstract
This letter studies multi-agent reinforcement learning in partially observable Markov potential games. Solving this problem is challenging due to partial observability, decentralized information, and the curse of dimensionality. First, to address the first two challenges, we leverage the common information framework, which allows agents to act based on both shared and local information. Second, to ensure tractability, we study an internal state that compresses accumulated information, preventing it from growing unboundedly over time. We then implement an internal state-based natural policy gradient method to find Nash equilibria of the Markov potential game. Our main contribution is to establish a non-asymptotic convergence bound for this method. Our theoretical bound decomposes into two interpretable components: a statistical error term that also arises in standard Markov potential games, and an approximation error capturing the use of finite-state controllers. Finally, simulations across multiple partially observable environments demonstrate that the proposed method using finite-state controllers achieves consistent improvements in performance compared to the setting where only the current observation is used.

---

## 论文详细总结（自动生成）

以下是对论文《Internal State-Based Policy Gradient Methods for Partially Observable Markov Potential Games》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文关注的是**多智能体强化学习（MARL）**中一个极具挑战性的子领域：**部分可观测马尔可夫势博弈（POMPGs）**。
*   **研究动机**：在现实场景（如无人机搜救、自动驾驶）中，智能体通常无法获取环境的全貌（部分可观测），且信息是去中心化的。
*   **核心挑战**：
    1.  **部分可观测性**：导致智能体需要依赖历史信息，但历史记录随时间呈指数级增长（维度灾难）。
    2.  **信念空间不可数**：虽然可以将部分可观测问题转化为基于“信念状态”的 MDP，但信念空间在连续空间中是不可数的，导致直接优化变得不可行。
    3.  **去中心化执行**：智能体之间缺乏全局协调。
*   **整体含义**：本文旨在提出一种既能处理部分可观测性，又在计算上可行的策略梯度算法，并为其提供理论收敛保证。

### 2. 方法论
论文提出了一种**基于内部状态的自然策略梯度（Internal State NPG）方法**：
*   **公共信息框架（Common Information Framework）**：允许智能体共享一部分局部观测到公共内存中，将去中心化的 POMDP 简化为一种等效的中心化模型。
*   **内部状态表示（Internal State Representation）**：为了防止历史信息无限增长，引入有限的“内部状态”来压缩共享历史和局部记忆。这相当于使用**有限状态控制器（FSC）**来近似最优策略。
*   **算法流程**：
    1.  **参数化**：使用 Softmax 策略参数化内部状态到动作的映射。
    2.  **策略更新**：采用自然策略梯度（NPG）更新规则，考虑策略空间的几何结构（Fisher 信息矩阵）而非简单的欧几里得梯度。
    3.  **理论创新**：推导出了一个非渐近收敛界。该界由两部分组成：一是标准 MPG 中的统计误差 $O(1/\sqrt{T})$，二是由于使用有限状态控制器近似真实信念状态而产生的**近似误差 $\epsilon_{FSC}$**。

### 3. 实验设计
*   **实验场景**：使用了 **POSGGym** 基准测试库中的三个环境：
    1.  **Multi-Agent Tiger (MATiger)**：双智能体协作听声辨位，避免老虎并寻找宝藏。
    2.  **Multi-Access Broadcast Channel (MABC)**：节点竞争通信信道，需避免碰撞以最大化吞吐量。
    3.  **Level-Based Foraging (LBF)**：网格世界中的食物采集，需要智能体间的空间协调。
*   **对比方法（Benchmark）**：
    *   **有限内存策略（Finite-memory policy）**：即本文提出的基于内部状态的方法（设置内存窗口长度 $t_w=2$）。
    *   **反应式策略（Reactive policy）**：仅基于当前观测值进行决策的基准方法。
*   **实现细节**：使用了基于神经网络的独立信任区域策略优化（ITRPO）算法。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。
*   **实现方式**：提到使用了神经网络（NN）来参数化策略函数和价值函数，并采用了参数共享机制。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同的部分可观测环境中进行了测试，展示了学习曲线和智能体行为轨迹。
*   **充分性评价**：
    *   **客观性**：实验涵盖了协作、竞争和空间导航等不同性质的博弈，具有一定的代表性。
    *   **局限性**：实验主要集中在双智能体场景，对于更大规模的多智能体系统（如数十个智能体）的扩展性验证不足。此外，对比基准较为单一，主要对比了“有无内存”的区别，未与其他先进的 POMDP 求解器进行横向对比。

### 6. 主要结论与发现
*   **收敛性验证**：实验结果与理论推导一致，算法在所有环境中均表现出良好的收敛性。
*   **内存的重要性**：在 MABC 等环境中，反应式策略完全无法学习，而基于内部状态的策略能够成功找到协调方案，证明了压缩历史信息对处理部分可观测性的必要性。
*   **性能提升**：有限状态控制器能有效近似信念状态，显著优于仅依赖当前观测的方法。
*   **误差分解**：理论证明了性能差距主要源于内部状态对真实信念状态的近似精度（即 $\epsilon_{FSC}$）。

### 7. 优点
*   **理论完备**：首次为基于内部状态的 POMPG 建立了非渐近收敛界，填补了该领域的理论空白。
*   **计算可行性**：通过有限状态压缩解决了信念空间不可数的问题，使算法能够处理大规模状态空间。
*   **可解释性**：收敛界的分解（统计误差+近似误差）为理解算法性能提供了清晰的物理含义。

### 8. 不足与局限
*   **势博弈限制**：该方法严格依赖于“势函数”的存在，对于非势博弈（如纯竞争博弈或一般和博弈）的适用性有限。
*   **内部状态构造**：内部状态的更新函数（如窗口长度）仍需人工预设或依赖特定架构（如 RNN），如何自动学习最优的压缩表示仍是挑战。
*   **实验深度**：缺乏与循环神经网络（RNN）或其他基于 Transformer 的 MARL 算法在复杂长程依赖任务中的性能对比。
*   **超参数敏感性**：收敛界中的 $a$ 项（最小访问概率）可能导致在初始策略较差或内部状态空间过大时收敛变慢。

（完）
