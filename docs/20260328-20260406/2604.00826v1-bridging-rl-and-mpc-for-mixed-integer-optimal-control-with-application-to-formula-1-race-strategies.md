---
title: Bridging RL and MPC for mixed-integer optimal control with application to Formula 1 race strategies
title_zh: 桥接强化学习与模型预测控制以实现混合整数最优控制，并应用于一级方程式赛车策略
authors: "Joschua Wüthrich, Romir Damle, Giona Fieni, Melanie N. Zeilinger, Christopher H. Onder, Andrea Carron"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00826v1"
tags: ["query:rl"]
score: 9.0
evidence: 用于最优控制的混合强化学习与模型预测控制框架
tldr: 本研究提出一种结合强化学习（RL）与模型预测控制（MPC）的混合框架，用于解决包含离散和连续变量的混合整数最优控制问题。该方法在全混合动作空间训练RL代理，部署时利用RL生成的离散序列参数化非线性MPC并提供连续热启动，同时将Critic作为终端代价。在F1赛车策略应用中，该框架实现了接近离线全局最优的性能，且无需重新训练即可通过MPC模块化扩展适应环境扰动。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00826v1/fig-001.webp\", \"caption\": \"Fig. 3. Comparison of the MINLP offline benchmark (black), the trained RL agent (red), the online hybrid RL-MPC method for a horizon of N = 15 laps (blue), and the online hybrid RL-MPC method for a horizon of N ≥ 40 laps (cyan, dashed), with constraint bounds shown as gray dashed lines. The plot shows the fuel allocation as a percentage of the nominal allocation (top), the battery energy allocation where positive values denote charging and negative values discharging (middle), and the pit stop timing with compound selection (bottom).\", \"page\": 6, \"index\": 1, \"width\": 486, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2604.00826v1/fig-002.webp\", \"caption\": \"Fig. 2. Cost-to-go evaluated along the closed-loop trajectory for γ = 0.9999. In the top plot, the critics with ReLU (blue) and Smooth ReLU (red) activation functions approximate C∗(x, u) (black), all decreasing as fewer laps remain. The bottom plot shows that both critics underestimate C∗(x, u) by up to 30 s which is around 0.5% of total race time, an error that becomes increasingly relevant for shorter prediction horizons, where the MPC relies more heavily on the critic to capture longterm performance. The difference between ReLU and SmoothReLU remains negligible along the optimal solution trajectory.\", \"page\": 5, \"index\": 2, \"width\": 488, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2604.00826v1/fig-003.webp\", \"caption\": \"Fig. 1. Hybrid RL-MPC framework. The RL actor is rolled out over the prediction horizon N to supply discrete action trajectories and a continuous warm-start to the MPC, which optimizes continuous inputs with Cθ(x, u c, ud) as terminal cost. Only the first continuous input uc,∗ 0 from the MPC and the first discrete input ud,RL\", \"page\": 4, \"index\": 3, \"width\": 450, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2604.00826v1/fig-004.webp\", \"caption\": \"Fig. 5. Comparison of the hybrid RL-MPC without traffic model (black) and with traffic model (15) (blue), with constraint bounds shown as gray dashed lines. The plot shows the fuel energy allocation (top), battery energy allocation (second), pit stop actions (third), and gap time to the competitor (bottom), where positive values indicate being behind. Both methods retain the nominal pit stop strategy, but only the hybrid RL-MPC with the traffic model modulates its energy allocation immediately after the first pit stop to clear the traffic earlier, finishing 5.19 s ahead of the nominal strategy.\", \"page\": 7, \"index\": 4, \"width\": 486, \"height\": 487}]"
motivation: 针对混合整数最优控制问题，现有方法难以兼顾离散决策的长期一致性与连续控制的实时约束处理。
method: 在全混合空间训练RL代理，并将其Actor生成的离散序列与Critic终端代价融入非线性MPC的在线优化中。
result: 在F1赛车策略实验中，该混合方法性能接近离线混合整数非线性规划基准，且显著优于纯RL代理。
conclusion: 该框架成功融合了RL的长期预测能力与MPC的鲁棒性，为复杂混合整数控制任务提供了一种高效且具备零成本适应性的方案。
---

## 摘要
我们提出了一种用于混合整数最优控制的强化学习 (RL) 与模型预测控制 (MPC) 混合框架，其中离散变量进入代价函数和动力学方程，但不涉及约束条件。现有的分层方法仅将 RL 用于离散动作空间，而将连续优化留给 MPC。与这些方法不同，我们在完整的混合动作空间上训练 RL 智能体，从而确保与底层马尔可夫决策过程的代价保持一致。在部署期间，RL 执行器在预测时域内展开，通过离散动作序列对无整数非线性 MPC 进行参数化，并提供连续热启动。学习到的评论家网络作为终端代价，用以捕捉长期性能。我们证明了该框架的递归可行性，并在一个一级方程式赛车策略问题上验证了其有效性。实验结果表明，该混合方法相对于离线混合整数非线性规划基准实现了接近最优的性能，且优于独立的 RL 智能体。此外，该混合方案通过模块化的 MPC 扩展，能够以零重训练成本适应未见的扰动。

## 速览
**TLDR**：本研究提出一种结合强化学习（RL）与模型预测控制（MPC）的混合框架，用于解决包含离散和连续变量的混合整数最优控制问题。该方法在全混合动作空间训练RL代理，部署时利用RL生成的离散序列参数化非线性MPC并提供连续热启动，同时将Critic作为终端代价。在F1赛车策略应用中，该框架实现了接近离线全局最优的性能，且无需重新训练即可通过MPC模块化扩展适应环境扰动。 \
**Motivation**：针对混合整数最优控制问题，现有方法难以兼顾离散决策的长期一致性与连续控制的实时约束处理。 \
**Method**：在全混合空间训练RL代理，并将其Actor生成的离散序列与Critic终端代价融入非线性MPC的在线优化中。 \
**Result**：在F1赛车策略实验中，该混合方法性能接近离线混合整数非线性规划基准，且显著优于纯RL代理。 \
**Conclusion**：该框架成功融合了RL的长期预测能力与MPC的鲁棒性，为复杂混合整数控制任务提供了一种高效且具备零成本适应性的方案。

---

## Abstract
We propose a hybrid reinforcement learning (RL) and model predictive control (MPC) framework for mixed-integer optimal control, where discrete variables enter the cost and dynamics but not the constraints. Existing hierarchical approaches use RL only for the discrete action space, leaving continuous optimization to MPC. Unlike these methods, we train the RL agent on the full hybrid action space, ensuring consistency with the cost of the underlying Markov decision process. During deployment, the RL actor is rolled out over the prediction horizon to parametrize an integer-free nonlinear MPC through the discrete action sequence and provide a continuous warm-start. The learned critic serves as a terminal cost to capture long-term performance. We prove recursive feasibility, and validate the framework on a Formula 1 race strategy problem. The hybrid method achieves near-optimal performance relative to an offline mixed-integer nonlinear program benchmark, outperforming a standalone RL agent. Moreover, the hybrid scheme enables adaptation to unseen disturbances through modular MPC extensions at zero retraining cost.

---

## 论文详细总结（自动生成）

以下是对论文《Bridging RL and MPC for mixed-integer optimal control with application to Formula 1 race strategies》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何高效解决**混合整数最优控制问题 (MIOCP)**。这类问题同时包含离散决策（如赛车换胎、设备开关）和连续控制（如能量分配、流量调节），通常具有极高的计算复杂度（NP-hard）。
*   **研究动机**：
    *   **MPC 的局限**：在处理长时域混合整数问题时，在线求解非线性混合整数规划（MINLP）计算量巨大，难以实时应用。
    *   **RL 的局限**：虽然能处理长时域决策，但难以保证状态约束的硬性满足，且在连续动作的精细控制上往往不如基于模型的优化方法。
    *   **现有混合方法的不足**：以往研究常将 RL 仅用于离散空间，导致与底层马尔可夫决策过程（MDP）的代价函数不一致，且未能充分利用 RL 的长期预测能力来引导 MPC。

### 2. 方法论
该论文提出了一种结合 RL 长期视野和 MPC 局部约束处理能力的混合框架：
*   **核心思想**：在全混合动作空间训练 RL，部署时利用 RL 的 Actor 生成离散序列和热启动值，利用 Critic 作为 MPC 的终端代价，从而将复杂的 MINLP 简化为易于求解的连续非线性规划 (NLP)。
*   **关键技术细节**：
    1.  **离线训练**：使用软行为者-评论家算法 (SAC) 在包含连续和离散动作的全空间进行训练，确保 Critic（价值函数）与混合 MDP 的代价结构一致。
    2.  **在线部署（Actor 展开）**：在每个时间步，将 RL Actor 在预测时域 $N$ 内进行开环展开，提取出一组离散动作序列 $\{u^d\}$。
    3.  **MPC 参数化**：将上述离散序列固定到 MPC 模型中，使原本的混合整数问题退化为连续动作的 NLP 问题。
    4.  **终端代价与热启动**：使用 RL Critic 作为 MPC 的终端代价函数（捕捉时域外的长期收益），并利用 Actor 生成的连续动作序列作为 NLP 求解器的热启动初值。
    5.  **平滑处理**：为满足梯度优化器的要求，将 Critic 网络中的 ReLU 激活函数替换为平滑的 Smooth ReLU。
*   **理论保证**：证明了在“离散决策不影响约束可行性”的假设下，该框架具有**递归可行性**，即 MPC 层能确保闭环约束始终满足。

### 3. 实验设计
*   **应用场景**：**一级方程式 (F1) 赛车策略优化**。
    *   **离散决策**：进站时机及轮胎配方选择（软胎、中性胎、硬胎）。
    *   **连续控制**：每圈的燃料消耗和电池能量分配。
*   **基准方法 (Benchmarks)**：
    1.  **离线 MINLP**：作为全局最优解的基准（理论上限）。
    2.  **独立 RL 智能体**：纯数据驱动的方法。
    3.  **不同时域的混合 RL-MPC**：测试 $N=5, 10, 15, 40$ 等不同预测长度下的表现。
*   **对比维度**：总比赛时间（性能）、计算耗时、对未见扰动（交通拥堵）的适应性。

### 4. 资源与算力
*   **硬件环境**：论文提到在线求解是在一台商用笔记本电脑上完成的（Intel Core Ultra 7, 64 GB RAM）。
*   **算力细节**：
    *   **求解耗时**：混合框架在 $N=15$ 时平均求解时间约为 168ms，远快于离线 MINLP 的秒级耗时。
    *   **训练时长**：文中**未明确说明** RL 代理的具体训练时长或所使用的 GPU 型号。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对比了 4 种不同预测时域下的性能变化，展示了性能随 $N$ 增加而逼近最优解的趋势。
    *   设计了专门的“交通场景”案例研究，验证框架在不重新训练 RL 的情况下，仅通过修改 MPC 模块即可应对新环境的能力。
*   **充分性评价**：实验设计较为充分，涵盖了从标准性能评估到鲁棒性验证的闭环。通过与离线全局最优解对比，客观地量化了方法的次优度（Gap）。

### 6. 主要结论与发现
*   **性能优异**：混合 RL-MPC 显著优于纯 RL 代理。在 $N=15$ 时，其相对于最优解的差距仅为 9.4ms（总赛程约 90 分钟），比纯 RL 减少了 99% 的误差。
*   **计算高效**：通过将离散变量固定，NLP 的求解速度满足实时控制需求。
*   **零成本适应性**：在加入“交通拥堵”模型后，无需重新训练 RL，仅靠 MPC 层的模型更新，混合框架就能比名义策略快 5.19 秒，证明了其模块化的优势。

### 7. 优点
*   **一致性**：在全动作空间训练 RL，解决了以往分层方法中价值函数不匹配的问题。
*   **理论支撑**：提供了递归可行性的证明，增强了学习控制系统的安全性。
*   **实用性强**：在 F1 这种高度动态且复杂的场景下证明了有效性，且具备极强的工程灵活性（热启动、终端代价引导）。

### 8. 不足与局限
*   **结构化假设限制**：该框架依赖于“离散动作不影响约束可行性”的假设。如果离散决策直接决定了系统是否会违反硬约束（例如：开关闸门决定水位是否溢出），该方法可能失效。
*   **次优度界限**：虽然实验表现接近最优，但缺乏关于 RL 近似误差如何影响最终控制性能的严谨理论界限。
*   **应用范围**：目前仅在确定性系统上验证，尚未讨论在具有高度随机性的环境（如天气突变、事故黄旗）下的表现。

（完）
