---
title: Generalizable Dense Reward for Long-Horizon Robotic Tasks
title_zh: 长程机器人任务的可泛化稠密奖励
authors: "Silong Yong, Stephen Sheng, Carl Qi, Xiaojie Wang, Evan Sheehan, Anurag Shivaprasad, Yaqi Xie, Katia Sycara, Yesh Dattatreya"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00055v1"
tags: ["query:vla"]
score: 9.0
evidence: 机器人多模态策略学习与强化学习
tldr: 针对机器人基础策略在长程任务中因分布偏移和误差累积导致性能下降的问题，本文提出VLLR框架。该框架结合了基于LLM和VLM的任务进度识别（外部奖励）以及基于策略自确定性的内在奖励。通过LLM分解任务并利用VLM初始化价值函数，配合PPO微调中的自确定性引导，VLLR在无需人工奖励工程的情况下，显著提升了机器人在复杂任务中的成功率和泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00055v1/fig-001.webp\", \"caption\": \"Fig. 3. We showcase an example where raw progress estimation from Nova Pro is noisy and our method is able to identify the actual progress made by the agent. The task is to find a clock and grasp it. In the first column, we can see that throughout the rollout, the progress estimation is noisy and problematic, unable to provide meaningful reward signals. In the second column, our method is able to provide three clear jumps in terms of progress estimation. The first jump rewards the fact that the robot sees the clock, the second jump rewards the robot positioning itself in front of the clock and the third jump rewards the robot actually picking up the clock. In the third column, we showcase the actual observation Nova Pro is provided. We highlight the clock in red box in frame 1 and 20. Frame 1 showcase the fact that the robot sees the clock and frame 20 demonstrates the robot positioning itself in front of the clock. We connected these two observations to the first two jumps in our progress estimation.\", \"page\": 5, \"index\": 1, \"width\": 487, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.00055v1/fig-002.webp\", \"caption\": \"Fig. 2. We showcase an example to compare different VLM progress estimation including Qwen, Nova Pro and CLIP. The VLMs are tasked to estimate progress for finding a laptop that is not in plain sight. We found that Qwen tends to over-saturate the estimation, mostly caused by falsely recognizing the laptop, CLIP is unstable, wrong and unable to identify overall task completion, and Nova Pro is the best at progress estimation, which means it provides correct signal when seeing the laptop and approaching it. Here the rollout is collected using A* and the ground truth progress should be linearly increasing.\", \"page\": 4, \"index\": 2, \"width\": 487, \"height\": 272}, {\"url\": \"assets/figures/arxiv/2604.00055v1/fig-003.webp\", \"caption\": \"Fig. 1. Method overview. VLLR is a reward model with three components, i.e. sparse task success reward, intrinsic reward and extrinsic reward (Sec. III-D). The environment provides task instruction and a scene graph context to the LLM for task decomposition (Sec. III-B.1). Then the decomposed subgoals are fed into a VLM together with the current and last observations (Sec. III-B.2). The VLM provides a noisy progress estimate which is then smoothed out (Sec. III-B.3) and used as a reward signal for initializing the value function (Sec. III-B.4) used for PPO. The intrinsic reward is calculated using the policy’s output action distribution (Sec. IIIC). It is fed together with the task success signal into the PPO algorithm and then produces updates for finetuning the pretrained policy (Sec. III-D).\", \"page\": 3, \"index\": 3, \"width\": 487, \"height\": 272}]"
motivation: 现有的机器人模仿学习策略在长程任务中易受误差累积影响，且强化学习微调往往依赖繁琐的人工奖励设计。
method: 提出VLLR框架，利用LLM分解任务并结合VLM进行价值函数初始化，同时引入策略自确定性作为每步的内在奖励进行PPO微调。
result: "在CHORES基准测试中，VLLR相比预训练策略提升了高达56%的成功率，并在分布外任务上比现有SOTA方法提升了10%。"
conclusion: VLLR证明了结合视觉语言模型进度识别与策略内在确定性，可以在无需人工干预的情况下有效提升长程机器人任务的性能与泛化性。
---

## 摘要
现有的机器人基础策略主要通过大规模模仿学习进行训练。虽然这些模型展示了强大的能力，但由于分布偏移和误差累积，它们在处理长程任务时往往表现不佳。虽然强化学习（RL）可以微调这些模型，但在没有手动奖励工程的情况下，它无法在多样化的任务中取得良好效果。我们提出了 VLLR，这是一个稠密奖励框架，结合了：（1）来自大语言模型（LLM）和视觉语言模型（VLM）的用于任务进度识别的外在奖励，以及（2）基于策略自确定性（self-certainty）的内在奖励。VLLR 利用 LLM 将任务分解为可验证的子任务，然后利用 VLM 估计进度以在简短的热身阶段初始化价值函数，从而避免了完整训练过程中高昂的推理成本；而自确定性在整个 PPO 微调过程中提供每一步的内在引导。消融研究揭示了互补的优势：基于 VLM 的价值初始化主要提高了任务完成效率，而自确定性主要提升了成功率，特别是在分布外任务上。在涵盖移动操作和导航的 CHORES 基准测试中，VLLR 相比预训练策略实现了高达 56% 的绝对成功率提升，在分布内任务上相比最先进的 RL 微调方法提升了高达 5%，在分布外任务上提升了高达 10%，且无需任何手动奖励工程。更多可视化结果见 https://silongyong.github.io/vllr_project_page/。

## Abstract
Existing robotic foundation policies are trained primarily via large-scale imitation learning. While such models demonstrate strong capabilities, they often struggle with long-horizon tasks due to distribution shift and error accumulation. While reinforcement learning (RL) can finetune these models, it cannot work well across diverse tasks without manual reward engineering. We propose VLLR, a dense reward framework combining (1) an extrinsic reward from Large Language Models (LLMs) and Vision-Language Models (VLMs) for task progress recognition, and (2) an intrinsic reward based on policy self-certainty. VLLR uses LLMs to decompose tasks into verifiable subtasks and then VLMs to estimate progress to initialize the value function for a brief warm-up phase, avoiding prohibitive inference cost during full training; and self-certainty provides per-step intrinsic guidance throughout PPO finetuning. Ablation studies reveal complementary benefits: VLM-based value initialization primarily improves task completion efficiency, while self-certainty primarily enhances success rates, particularly on out-of-distribution tasks. On the CHORES benchmark covering mobile manipulation and navigation, VLLR achieves up to 56% absolute success rate gains over the pretrained policy, up to 5% gains over state-of-the-art RL finetuning methods on in-distribution tasks, and up to $10\%$ gains on out-of-distribution tasks, all without manual reward engineering. Additional visualizations can be found in https://silongyong.github.io/vllr_project_page/

---

## 论文详细总结（自动生成）

以下是对论文《Generalizable Dense Reward for Long-Horizon Robotic Tasks》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的机器人基础模型（如通过大规模模仿学习训练的模型）在处理**长程（Long-horizon）任务**时表现不佳。主要原因在于：
    1.  **误差累积与分布偏移**：模仿学习在长序列操作中微小的错误会不断放大。
    2.  **奖励稀疏性**：强化学习（RL）虽能优化策略，但长程任务往往只有最终的成功信号，导致信用分配（Credit Assignment）极其困难。
    3.  **奖励工程难题**：手动设计稠密奖励既耗时又难以泛化到不同任务。
*   **研究背景**：论文旨在探索一种无需人工干预、具备泛化能力的稠密奖励机制，利用大模型（LLM/VLM）的先验知识来引导机器人完成复杂任务。

### 2. 方法论：VLLR 框架
VLLR（Vision-Language-Long-horizon Reward）结合了外部语义监督和内部策略置信度，分为三个核心部分：
*   **外部奖励（Extrinsic Reward）**：
    *   **任务分解**：利用 LLM（如 Claude-3.7）结合环境的“场景图（Scene Graph）”将高层指令分解为有序的子目标（Subgoals）。
    *   **进度估计**：利用 VLM（如 Amazon Nova Pro）对比当前观测与子目标，输出进度值 $p_t$。
    *   **噪声抑制算法**：为防止 VLM 幻觉导致的奖励提前饱和，引入了基于滑动窗口的中值滤波平滑处理，确保奖励信号的单调性和可靠性。
*   **内在奖励（Intrinsic Reward）**：
    *   **策略自确定性（Self-certainty）**：计算策略输出动作分布的集中程度（负熵）。其核心假设是：当观测与模型内部的“世界模型”一致时，动作分布会更尖锐，这可作为每一步的稠密引导。
*   **两阶段优化流程**：
    *   **阶段 I（价值初始化）**：仅在最初的 200K 步使用 VLM 进度奖励来训练价值函数（Critic），避免了全过程调用 VLM 带来的高昂推理成本。
    *   **阶段 II（策略微调）**：使用 PPO 算法，结合稀疏成功奖励和内在自确定性奖励进行最终训练。

### 3. 实验设计
*   **数据集/场景**：使用 **CHORES 基准测试**，包含基于 ProcThor 程序的 191,568 个虚拟房屋环境。
*   **任务类型**：
    *   **分布内（ID）**：Fetch（取物）、Pick-Up（拾取）、ObjectNav（物体导航）、RoomVisit（房间访问）。
    *   **分布外（OOD）**：ObjNavRel（基于相对属性导航，如“找到最大的苹果”）、ObjNavAff（基于功能属性导航，如“找个能坐的地方”）。
*   **对比方法**：
    *   **SOTA RL 方法**：FLaRe（目前最强的 RL 微调基准）。
    *   **基础模型**：SPOC、Poliformer、SPOC++。
    *   **传统 RL**：PIRLNav、JSRL（仅使用稀疏奖励）。

### 4. 资源与算力
*   **训练步数**：导航任务训练 20M 步，涉及精细操作的任务（如 Fetch, Pick-up）训练 50M 步。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号和数量。但提到为了降低推理延迟，选择了轻量级的 Nova Pro 作为 VLM，并采用两阶段训练法将 VLM 调用限制在初始化的极短时间内（总步数的 <0.5%）。

### 5. 实验数量与充分性
*   **实验规模**：在约 17,000 个未见过的测试环境中进行了评估，确保了统计意义上的显著性。
*   **消融实验**：详细对比了“仅稀疏奖励”、“仅自确定性奖励”、“仅 VLM 奖励”以及“完整 VLLR”的效果。
*   **充分性评价**：实验设计较为充分，涵盖了从纯导航到导航+操作的多种复杂度，且专门设置了 OOD 任务验证泛化性。对比基准包含了 2024/2025 年最新的研究成果，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能提升**：VLLR 相比预训练策略提升了高达 **56%** 的成功率；相比 SOTA RL 方法（FLaRe），在 ID 任务提升 5%，在 OOD 任务提升 **10%**。
*   **互补效应**：
    *   **VLM 初始化**：主要提升了**任务完成效率**（路径更短，SEL 指标更高），因为它提供了全局语义引导。
    *   **自确定性奖励**：主要提升了**最终成功率**，因为它提供了稳定的每一步梯度引导，加速了收敛。
*   **泛化能力**：即使在预训练数据中未出现的属性推理任务上，VLLR 也能通过 LLM 的推理能力提供有效奖励。

### 7. 优点
*   **无需人工奖励工程**：完全自动化地从自然语言指令生成稠密奖励。
*   **计算效率高**：通过两阶段训练巧妙解决了 VLM 推理慢、成本高的问题。
*   **鲁棒性强**：设计的噪声过滤算法有效解决了大模型常见的“幻觉”对强化学习训练的干扰。
*   **内在置信度的创新应用**：将 LLM 中的自确定性概念成功迁移至机器人动作策略中。

### 8. 不足与局限
*   **动作空间限制**：目前实验主要基于**离散动作空间**，在连续高维动作空间（如复杂灵巧手操作）中的表现尚待验证。
*   **环境依赖**：任务分解依赖于“场景图（Scene Graph）”的获取，在完全未知的真实物理环境中，构建准确的场景图本身就是一个挑战。
*   **视觉噪声敏感度**：虽然有平滑算法，但如果 VLM 在特定视角下持续误判，仍可能导致价值函数偏离。

（完）
