---
title: Asymmetric Actor-Critic for Multi-turn LLM Agents
title_zh: 面向多轮大语言模型智能体的非对称 Actor-Critic 框架
authors: "Shuli Jiang, Zhaoyang Zhang, Yi Zhang, Shuo Yang, Wei Xia, Stefano Soatto"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00304v1"
tags: ["query:rl"]
score: 9.0
evidence: 用于大模型智能体的非对称 Actor-Critic 框架
tldr: 针对多轮对话智能体在不可重试场景下的可靠性挑战，本文提出一种非对称Actor-Critic框架。该框架利用强大的闭源模型作为Actor执行任务，并由轻量级开源模型作为Critic进行实时监控与干预。通过利用“生成难、验证易”的非对称性，并结合专门的微调数据流水线，该方法在不改变Actor的前提下显著提升了任务成功率，使小模型Critic能达到甚至超越大模型的监督效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00304v1/fig-001.webp\", \"caption\": \"Figure 1: Existing actor–critic methods assume trainable actors and structured action spaces. We propose an asymmetric actor–critic framework where a lightweight open-source critic supervises a fixed proprietary actor at runtime, enabling reliable control for one-shot multiturn conversational agents.\", \"page\": 1, \"index\": 1, \"width\": 458, \"height\": 291}, {\"url\": \"assets/figures/arxiv/2604.00304v1/fig-002.webp\", \"caption\": \"Table 13: Statistics of the generated critic training data. Each critic evaluation point in a trajectory corresponds to one training sample. Positive samples denote critic rejections, while negative samples denote approvals. For UserBench, the critic is trained on data aggregated across all environments.\", \"page\": 18, \"index\": 2, \"width\": 779, \"height\": 204}]"
motivation: 现有的智能体优化方法通常依赖多次尝试或全量微调，难以在不可重试的实时交互场景中直接应用闭源大模型。
method: 提出非对称架构，由闭源大模型担任Actor，微调后的轻量级开源模型担任Critic，在同一交互轨迹内进行实时监督与干预。
result: 在tau-bench和UserBench上的实验表明，该框架显著提升了任务成功率，且微调后的小模型Critic表现优于大型闭源模型。
conclusion: 非对称Actor-Critic框架证明了通过轻量级模型对强大Actor进行实时监督是提升多轮对话智能体可靠性的有效途径。
---

## 摘要
大语言模型（LLMs）展现出强大的推理和对话能力，但在多轮交互中确保行为的可靠性仍然具有挑战性。在许多现实应用中，智能体必须在无法重试的单次尝试场景下取得成功。现有方法要么依赖于反思或事后评估（这需要额外的尝试），要么假设模型完全可训练，从而无法利用闭源 LLMs。我们提出了一种用于可靠对话智能体的非对称 Actor-Critic 框架。其中，一个强大的闭源 LLM 担任 Actor，而一个较小的开源 Critic 提供运行时监督，监控 Actor 的动作并在同一交互轨迹内进行干预。与基于训练的 Actor-Critic 方法不同，我们的框架监督的是在开放式对话环境中运行的固定 Actor。该设计利用了生成与验证之间的不对称性：虽然高质量的生成需要大模型，但有效的监督通常可以通过较小的模型来实现。我们还引入了一个数据生成流水线，在不修改 Actor 的情况下产生用于 Critic 微调的监督信号。在 $τ$-bench 和 UserBench 上的实验表明，与强大的单智能体基准相比，我们的方法显著提高了可靠性和任务成功率。此外，轻量级开源 Critic 在担任 Critic 角色时可以媲美甚至超越更大的闭源模型，且 Critic 微调在多种最先进方法的基础上带来了额外的提升。

## 速览
**TLDR**：针对多轮对话智能体在不可重试场景下的可靠性挑战，本文提出一种非对称Actor-Critic框架。该框架采用强大的闭源模型作为Actor执行任务，配合轻量级开源模型作为Critic进行实时监控与干预。利用生成与验证之间的不对称性，通过微调后的轻量级Critic在不改变Actor的前提下显著提升了任务成功率，为增强闭源模型智能体的交互可靠性提供了新方案。 \
**Motivation**：现有的智能体优化方法往往依赖多次尝试或模型可训练性，难以在不可重试的闭源模型应用场景中保证多轮交互的可靠性。 \
**Method**：提出一种非对称框架，由闭源大模型担任Actor，轻量级开源模型担任Critic进行运行时监督与干预，并设计了专门的数据生成管线用于Critic的微调。 \
**Result**：在τ-bench和UserBench上的实验证明，该方法显著提升了任务成功率，且微调后的轻量级Critic性能可媲美甚至超越大型闭源模型。 \
**Conclusion**：研究表明通过轻量级模型对强大模型进行实时监督是提升智能体可靠性的有效路径，验证了小模型在监督大模型行为方面的潜力。

---

## Abstract
Large language models (LLMs) exhibit strong reasoning and conversational abilities, but ensuring reliable behavior in multi-turn interactions remains challenging. In many real-world applications, agents must succeed in one-shot settings where retries are impossible. Existing approaches either rely on reflection or post-hoc evaluation, which require additional attempts, or assume fully trainable models that cannot leverage proprietary LLMs. We propose an asymmetric actor-critic framework for reliable conversational agents. A powerful proprietary LLM acts as the actor, while a smaller open-source critic provides runtime supervision, monitoring the actor's actions and intervening within the same interaction trajectory. Unlike training-based actor-critic methods, our framework supervises a fixed actor operating in open-ended conversational environments. The design leverages a generation-verification asymmetry: while high-quality generation requires large models, effective oversight can often be achieved by smaller ones. We further introduce a data generation pipeline that produces supervision signals for critic fine-tuning without modifying the actor. Experiments on $τ$-bench and UserBench show that our approach significantly improves reliability and task success over strong single-agent baselines. Moreover, lightweight open-source critics rival or surpass larger proprietary models in the critic role, and critic fine-tuning yields additional gains over several state-of-the-art methods.

---

## 论文详细总结（自动生成）

以下是对论文《Asymmetric Actor-Critic for Multi-turn LLM Agents》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在**不可重试（One-shot）**的多轮对话场景中，确保基于闭源大模型（如 Claude-4）的智能体能够严格遵守业务规则、领域约束和用户偏好。
*   **研究动机**：
    *   **闭源限制**：最强大的 LLM 通常是闭源的，无法进行全量微调。
    *   **单次尝试压力**：在客服或交易场景中，智能体往往只有一次行动机会，事后反思（Reflection）或多次尝试（Retries）在实时交互中成本过高或不可行。
    *   **现有方法局限**：传统的 Actor-Critic 框架通常假设 Actor 可训练且动作空间有限，而对话智能体的动作是开放式的自然语言。

### 2. 方法论：非对称 Actor-Critic 框架
*   **核心思想**：利用“生成难、验证易”的非对称性。使用强大的闭源模型作为 **Actor** 负责生成，使用轻量级开源模型作为 **Critic** 负责实时监控与干预。
*   **关键技术细节**：
    *   **运行时监督（Runtime Supervision）**：在每一轮对话中，Actor 先提出候选动作，Critic 评估该动作是否违反约束。若违反，Critic 提供反馈，Actor 在同一轮次内修正动作后再执行。
    *   **非对称架构**：Actor 固定不变（Fixed），Critic 可针对特定领域进行微调（Trainable）。
    *   **Critic 定向数据生成流水线**：
        1.  **任务提取**：从现有对话数据中推断潜在任务规范。
        2.  **任务过滤**：通过多次运行识别出 Actor 容易失败的“硬任务”。
        3.  **自我博弈采集**：让 Actor 与 Critic 交互，保留那些通过 Critic 干预后转败为胜的轨迹。
        4.  **样本提取**：将这些干预点转化为 SFT 数据（输入：上下文+候选动作；输出：Critic 的修正建议）。

### 3. 实验设计
*   **数据集与场景**：
    *   **$\tau$-bench**：模拟零售和航空领域的客服场景，强调政策合规性和工具调用。
    *   **UserBench**：旅游规划场景，强调从多轮对话中推断隐式用户偏好并提供最优推荐。
*   **基准模型（Baselines）**：
    *   **Single Actor**：仅使用 Claude-4 Sonnet。
    *   **CRITIC**：基于提示词的自我修正方法。
    *   **ReAct**：经典的推理-行动交替框架。
    *   **APIGen-MT**：针对多轮对话微调的开源模型（如 xLAM-2-70B）。
    *   **IRMA**：针对 $\tau$-bench 的输入重构方法。
*   **对比维度**：对比了不同规模的 Critic（Qwen3-4B/8B/32B）以及是否经过 SFT 微调的效果。

### 4. 资源与算力
*   **模型使用**：Actor 统一使用 Claude-4 Sonnet。Critic 使用 Qwen3 系列开源模型。
*   **算力细节**：
    *   论文提到使用 **TRL 框架**进行微调，采用 **LoRA** 技术（Rank=16, Alpha=32）。
    *   训练参数：AdamW 优化器，Warmup 比例 0.08，梯度累加 16 步，训练 5-15 个 Epoch。
    *   **未明确说明**具体的 GPU 型号、数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流 Benchmark 的多个子领域（零售、航空、T-22/33/44）进行了测试。
    *   每个任务均进行 **5 次独立运行**并取平均值（Pass@1），保证了结果的统计显著性。
    *   进行了 **Critic 能力消融实验**，对比了不同参数量（4B 到 32B）以及闭源模型作为 Critic 的表现。
*   **充分性评价**：实验设计较为充分，涵盖了从“零样本监督”到“微调后监督”的完整对比，且通过定性案例分析展示了 Critic 如何纠正 Actor 的具体错误，逻辑闭环清晰。

### 6. 主要结论与发现
*   **非对称增益**：引入轻量级 Critic 显著提升了任务成功率，尤其在复杂约束处理上。
*   **验证易于生成**：**8B 规模的开源 Critic 在监督任务上的表现优于参数量大得多的闭源模型（Claude-4）**，证明了专用小模型在验证任务上的潜力。
*   **微调有效性**：通过提出的数据流水线微调后的 Critic（Asym-AC-SFT）在所有指标上均达到 SOTA，证明了定制化监督信号的价值。

### 7. 优点与亮点
*   **实用性强**：解决了闭源模型无法微调且实时交互无法多次重试的痛点。
*   **成本效益高**：用极小代价（8B 模型）提升了昂贵闭源模型的可靠性。
*   **数据效率**：提出了一种无需人工标注、基于自我博弈和硬任务过滤的 Critic 训练数据生成方法。

### 8. 不足与局限
*   **延迟增加**：由于每轮对话增加了 Critic 评估步骤，系统的端到端响应延迟会增加。
*   **Actor 依赖性**：如果 Actor 本身的推理能力极差，即使 Critic 指出错误，Actor 可能也无法根据反馈做出正确修正。
*   **计算开销**：虽然 Critic 模型小，但在高并发场景下，额外的推理轮次仍会增加计算成本。
*   **泛化风险**：Critic 的微调高度依赖于特定领域的合成数据，跨领域的泛化能力尚未在更多样化的任务中得到验证。

（完）
