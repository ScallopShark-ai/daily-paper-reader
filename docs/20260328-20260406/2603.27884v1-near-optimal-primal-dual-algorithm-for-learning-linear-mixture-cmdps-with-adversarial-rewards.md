---
title: Near-Optimal Primal-Dual Algorithm for Learning Linear Mixture CMDPs with Adversarial Rewards
title_zh: 具有对抗性奖励的线性混合 CMDP 学习的近优原对偶算法
authors: "Kihyun Yu, Seoungbin Bae, Dabeen Lee"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27884v1"
tags: ["query:rl"]
score: 10.0
evidence: 线性混合约束马尔可夫决策过程的理论收敛分析和遗憾界
tldr: 针对具有对抗性奖励的线性混合CMDP提出了一种近乎最优的原始-对偶安全强化学习算法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27884v1/fig-001.webp\", \"caption\": \"Fig. 1: Regret and constraint violation of PD-POWERS.\", \"page\": 6, \"index\": 1, \"width\": 350, \"height\": 435}]"
motivation: 线性混合约束马尔可夫决策过程的理论收敛分析和遗憾界。
method: We propose a primal-dual policy optimization algorithm that achieves regret and constraint violation bounds of $。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
我们研究了在全信息反馈和未知转移核下，具有对抗性奖励的有限时界线性混合约束马尔可夫决策过程（CMDPs）中的安全强化学习。我们提出了一种原对偶策略优化算法，在温和条件下实现了 $\widetilde{O}(\sqrt{d^2 H^3 K})$ 的遗憾（regret）和约束违反（constraint violation）界，其中 $d$ 是特征维度，$H$ 是时界，$K$ 是回合数。据我们所知，这是首个针对具有对抗性奖励的线性混合 CMDP 的可证明高效算法。特别地，我们的遗憾界是近优的，在对数因子内匹配了已知的极小极大下界。核心思想是引入正则化对偶更新，从而实现基于漂移（drift-based）的分析。这一步至关重要，因为当奖励函数在不同回合之间变化时，基于强对偶性的分析无法直接应用。此外，我们将基于加权岭回归的参数估计扩展到约束设置中，使我们能够构建更紧致的置信区间，这对于推导近优遗憾界至关重要。

## Abstract
We study safe reinforcement learning in finite-horizon linear mixture constrained Markov decision processes (CMDPs) with adversarial rewards under full-information feedback and an unknown transition kernel. We propose a primal-dual policy optimization algorithm that achieves regret and constraint violation bounds of $\widetilde{O}(\sqrt{d^2 H^3 K})$ under mild conditions, where $d$ is the feature dimension, $H$ is the horizon, and $K$ is the number of episodes. To the best of our knowledge, this is the first provably efficient algorithm for linear mixture CMDPs with adversarial rewards. In particular, our regret bound is near-optimal, matching the known minimax lower bound up to logarithmic factors. The key idea is to introduce a regularized dual update that enables a drift-based analysis. This step is essential, as strong duality-based analysis cannot be directly applied when reward functions change across episodes. In addition, we extend weighted ridge regression-based parameter estimation to the constrained setting, allowing us to construct tighter confidence intervals that are crucial for deriving the near-optimal regret bound.

---

## 论文详细总结（自动生成）

### 论文总结：具有对抗性奖励的线性混合 CMDP 学习的近优原对偶算法

#### 1. 核心问题与整体含义（研究动机和背景）
本文研究的是**安全强化学习（Safe RL）**中的一个挑战性场景：在有限时界的**线性混合约束马尔可夫决策过程（Linear Mixture CMDPs）**中，面对**对抗性奖励（Adversarial Rewards）**进行在线学习。
*   **背景**：传统的 CMDP 算法多局限于表格级状态空间，难以扩展到大规模环境。虽然线性函数近似（如线性混合 MDP）解决了扩展性，但现有工作大多假设奖励函数是固定的（平稳环境）。
*   **核心问题**：当奖励函数在每个回合（Episode）由对抗者任意选择（非平稳环境）且转移概率未知时，如何设计一个既能最大化累积奖励，又能保证约束违反最小化的算法，并达到理论上的最优收敛速率。

#### 2. 方法论：核心思想与关键技术
论文提出了 **PD-POWERS**（Primal-Dual Policy Optimization With BERnstein bonuS）算法，其核心由三个关键组件构成：
*   **正则化对偶更新（Regularized Dual Update）**：针对对抗性奖励导致强对偶性分析失效的问题，引入了带有正则项的对偶变量更新公式。这种设计允许使用“漂移分析（Drift-based analysis）”来控制对偶变量的增长，而无需预先知道 Slater 常数。
*   **策略优化（Policy Optimization）**：结合了熵正则化和策略扰动（Policy Perturbation）。通过在每回合对策略进行微小扰动，确保策略远离概率单纯形的边界，从而防止对偶变量发散。
*   **加权岭回归参数估计（Weighted Ridge Regression）**：为了获得更紧致的置信区间，算法估计了状态值函数的条件方差，并利用 Bernstein 型集中不等式进行加权回归。这使得算法在时界 $H$ 上的依赖达到了近乎最优。

#### 3. 实验设计
*   **场景设置**：研究者构建了一个具有 10 个阶段（$H=10$）和 5 维特征（$d=5$）的合成 CMDP 环境。
*   **奖励与约束**：奖励函数每 10 个回合交替变换（模拟对抗性/非平稳性），并设置了特定的约束阈值 $b=6$。
*   **Benchmark（基准）**：对比对象是一个**随机策略（Random Policy）**，即在每一步均匀随机选择动作。
*   **评估指标**：累积遗憾（Regret）和累积约束违反（Constraint Violation）。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   由于该研究侧重于理论分析和算法的收敛界证明，其实验部分主要用于验证理论结果的亚线性增长趋势，而非大规模工程应用，因此对算力的需求预计较低。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 5 组不同随机种子的独立模拟实验，展示了平均值及 95% 的置信区间。
*   **充分性评价**：实验数量相对较少（仅 1 个合成场景），且对比基准较为单一（仅对比了随机策略）。作为一篇以理论证明为主的论文，其实验部分更多是作为“概念验证（Proof of Concept）”，证明算法在非平稳环境下确实能实现亚线性的遗憾和违反增长。对于实际复杂环境的泛化性验证尚不充分。

#### 6. 主要结论与发现
*   **理论突破**：PD-POWERS 算法在遗憾和约束违反上均达到了 $\widetilde{O}(\sqrt{d^2 H^3 K})$ 的界。
*   **近优性**：该遗憾界在特征维度 $d$、时界 $H$ 和回合数 $K$ 的依赖上，匹配了已知的不带约束线性混合 MDP 的极小极大下界（Minimax Lower Bound），证明了算法的近乎最优性。
*   **对抗鲁棒性**：证明了即使奖励函数是动态变化的，通过正则化对偶更新也能有效平衡收益与安全。

#### 7. 优点
*   **理论严谨性**：首次为具有对抗性奖励的线性混合 CMDP 提供了可证明高效的算法。
*   **技术创新**：巧妙地结合了漂移分析与加权岭回归，解决了对抗环境下强对偶性不成立的难题。
*   **参数友好**：对偶更新步骤不需要预验知识（如 Slater 常数 $\gamma$），增强了算法的实用性。

#### 8. 不足与局限
*   **计算复杂度**：算法依赖于集成算子（Integrating Oracle）来计算期望值，如果该算子不可用，在大规模状态空间下的计算开销可能与状态数 $|S|$ 相关，导致效率下降。
*   **Slater 条件依赖**：遗憾界中包含 $1/\gamma^2$ 项，当安全区域非常狭窄（$\gamma$ 趋近于 0）时，算法性能会显著下降。
*   **实验覆盖面**：缺乏在真实世界数据集或更复杂的模拟器（如 Safety Gym）上的验证，难以评估其在处理高维原始输入（如图像）时的表现。

（完）
