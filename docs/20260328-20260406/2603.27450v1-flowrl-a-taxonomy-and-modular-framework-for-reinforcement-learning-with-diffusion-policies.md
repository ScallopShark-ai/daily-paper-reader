---
title: "FlowRL: A Taxonomy and Modular Framework for Reinforcement Learning with Diffusion Policies"
title_zh: FlowRL：一种基于扩散策略的强化学习分类体系与模块化框架
authors: "Chenxiao Gao, Edward Chen, Tianyi Chen, Bo Dai"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27450v1"
tags: ["query:rl"]
score: 9.0
evidence: 扩散策略强化学习的分类法和模块化框架
tldr: 扩散模型和流模型在策略表示上展现出巨大潜力，但由于缺乏显式对数概率，其强化学习训练面临挑战。本文通过引入统一的分类法整合了现有的扩散/流策略RL算法，并开发了基于JAX的高性能开源框架FlowRL，支持JIT编译以实现高吞吐量训练。通过在Gym-Locomotion、DMC和IsaacLab等多个基准上的系统测试，本文为算法选择提供了指导，为生成式模型在机器人领域的应用奠定了基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27450v1/fig-001.webp\", \"caption\": \"Figure 7: Effect of diffusion steps on performance. Performance of QSM, SDAC, and DACER when varying the number of diffusion steps (2, 5, 10, and 50). Returns are normalized by taskspecific maximum scores and averaged across Ant, HalfCheetah, Humanoid, and Walker2d.\", \"page\": 11, \"index\": 1, \"width\": 691, \"height\": 182}, {\"url\": \"assets/figures/arxiv/2603.27450v1/fig-002.webp\", \"caption\": \"Figure 1: The overview of FLOWRL.\", \"page\": 7, \"index\": 2, \"width\": 617, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2603.27450v1/fig-003.webp\", \"caption\": \"Table 2: Normalized returns of the final policy checkpoint for offline DPRL methods. Methods marked with † denote results reported in the original paper. Results are averaged over five independent runs of 10 evaluation episodes each. Numbers within 95% of the highest scores are bolded.\", \"page\": 10, \"index\": 3, \"width\": 722, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2603.27450v1/fig-004.webp\", \"caption\": \"Table 4: On-policy algorithm-specific hyperparameters on IsaacLab. Only parameters that differ from the shared defaults are listed.\", \"page\": 18, \"index\": 4, \"width\": 490, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2603.27450v1/fig-005.webp\", \"caption\": \"Figure 2: Representative tasks across three continuous-control suites. Left: MuJoCo Gymnasium. Middle: DeepMind Control Suite. Right: IsaacLab.\", \"page\": 8, \"index\": 5, \"width\": 764, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2603.27450v1/fig-006.webp\", \"caption\": \"Table 1: Summarization of existing DPRL algorithms, based on their guidance mechanism and reference policy.\", \"page\": 4, \"index\": 6, \"width\": 830, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2603.27450v1/fig-007.webp\", \"caption\": \"Table 3: Shared training settings per benchmark.\", \"page\": 17, \"index\": 7, \"width\": 594, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2603.27450v1/fig-008.webp\", \"caption\": \"Figure 9: Effect of noise schedule. Performance of SDAC, QSM, and DACER with a cosine (black) or linear noise schedule.\", \"page\": 12, \"index\": 8, \"width\": 696, \"height\": 182}]"
motivation: 针对扩散策略在强化学习中缺乏统一分类及高效训练框架的问题，旨在消除现有方法间的隔阂并提升开发效率。
method: 提出了一个综合分类法，并开发了支持即时编译和高吞吐量训练的模块化JAX框架FlowRL。
result: 在多个主流机器人仿真环境上完成了标准化基准测试，提供了扩散策略算法的性能对比和实践指南。
conclusion: 本研究为扩散策略RL提供了清晰的理论基础、高效的工具链和严谨的算法基准，有力推动了该领域的标准化发展。
---

## 摘要
凭借其卓越的灵活性，扩散模型和流模型已成为策略表示领域极具前景的候选方案。然而，由于缺乏用于原生策略梯度估计器的显式对数概率，在这些策略上进行高效的强化学习（RL）仍然是一个挑战。尽管已有许多尝试旨在解决这一问题，但该领域仍缺乏一个统一的视角来协调这些看似迥异的方法，从而阻碍了持续的发展。在本文中，我们通过引入一套针对扩散/流策略强化学习算法的全面分类体系填补了这一空白。为了支持可复现性和敏捷原型设计，我们推出了一个基于 JAX 的模块化开源代码库，该库利用即时编译（JIT）实现高吞吐量训练。最后，我们在 Gym-Locomotion、DeepMind Control Suite 和 IsaacLab 上提供了系统化且标准化的基准测试，对基于扩散的方法进行了严格的横向对比，并为从业者根据应用场景选择合适的算法提供了指导。我们的工作为理解和算法设计奠定了清晰的基础，为该领域的未来研究提供了一个高效的工具包，并为生成模型和机器人领域的从业者提供了算法指南。我们的代码可在 https://github.com/typoverflow/flow-rl 获取。

## Abstract
Thanks to their remarkable flexibility, diffusion models and flow models have emerged as promising candidates for policy representation. However, efficient reinforcement learning (RL) upon these policies remains a challenge due to the lack of explicit log-probabilities for vanilla policy gradient estimators. While numerous attempts have been proposed to address this, the field lacks a unified perspective to reconcile these seemingly disparate methods, thus hampering ongoing development. In this paper, we bridge this gap by introducing a comprehensive taxonomy for RL algorithms with diffusion/flow policies. To support reproducibility and agile prototyping, we introduce a modular, JAX-based open-source codebase that leverages JIT-compilation for high-throughput training. Finally, we provide systematic and standardized benchmarks across Gym-Locomotion, DeepMind Control Suite, and IsaacLab, offering a rigorous side-by-side comparison of diffusion-based methods and guidance for practitioners to choose proper algorithms based on the application. Our work establishes a clear foundation for understanding and algorithm design, a high-efficiency toolkit for future research in the field, and an algorithmic guideline for practitioners in generative models and robotics. Our code is available at https://github.com/typoverflow/flow-rl.

---

## 论文详细总结（自动生成）

### 论文总结：FlowRL —— 扩散策略强化学习的分类体系与模块化框架

#### 1. 核心问题与研究动机
*   **研究背景**：深度强化学习（RL）传统上依赖简单的分布（如高斯分布）来表示策略，但这难以捕捉高维控制任务中的复杂、多模态动作分布。
*   **核心问题**：扩散模型（DM）和流模型（FM）虽然表达能力强，但由于其迭代采样的特性，缺乏显式的对数概率（log-probabilities），导致传统的策略梯度估计和重参数化技巧难以直接应用。
*   **研究动机**：当前基于扩散策略的强化学习（DPRL）方法层出不穷，但领域内缺乏统一的分类视角，且不同算法在实验时存在网络架构、噪声计划等干扰因子，导致难以公平评价算法的真实贡献。

#### 2. 方法论：分类体系与 FlowRL 框架
*   **统一分类体系**：论文从两个维度对现有 DPRL 算法进行了系统化分类：
    *   **引导机制（Guidance Mechanism）**：决定如何利用价值函数 $Q(s, a)$ 优化策略。包括：
        *   **Best-of-N (BoN) 采样**：推理时从参考分布中采样 $N$ 个动作并筛选最优者。
        *   **Q值引导**：将动作梯度 $\nabla_a Q$ 注入采样过程（类似分类器引导）。
        *   **重参数化**：通过采样链进行时间反向传播（BPTT）或单步近似优化。
        *   **加权匹配**：将策略改进转化为加权监督学习（如 $Q$ 值加权的得分匹配）。
        *   **策略梯度**：通过离散化 SDE 或近似似然比（如 PPO 的变体）来估计梯度。
    *   **参考策略（Reference Policy）**：决定正则化目标。包括均匀分布（最大熵 RL）、上一轮策略（镜像下降）或行为策略（离线 RL）。
*   **FlowRL 框架**：
    *   **技术栈**：基于 **JAX** 开发，利用其即时编译（JIT）功能。
    *   **核心设计**：模块化解耦了网络架构（MLP, SimBa, BroNet）、生成模型（DDPM, EDM, CNF）和 RL 算法逻辑。
    *   **效率优化**：使用 `vmap` 处理评论家集成（Critic Ensembles），使用 `lax.scan` 优化迭代去噪循环，实现高吞吐量训练。

#### 3. 实验设计
*   **数据集与场景**：
    *   **Gym-Locomotion (MuJoCo)**：标准运动任务（如 Ant, HalfCheetah）。
    *   **DeepMind Control Suite (DMC)**：更具挑战性的连续控制任务。
    *   **IsaacLab**：GPU 加速的大规模并行仿真环境，用于在线 On-policy 测试。
    *   **D4RL**：用于离线 RL 性能评估。
*   **对比基准 (Baselines)**：
    *   **传统方法**：SAC, PPO, IQL。
    *   **扩散 RL 方法**：QSM, DACER, SDAC, QVPO, DPMD, DPPO, GenPO, FPO, Diffusion-QL, DAC, BDPO 等。

#### 4. 资源与算力
*   **算力说明**：论文强调了 JAX 带来的效率提升，但在正文中**未明确列出具体的 GPU 型号和数量**（如 A100 或 H100 的具体配置）。
*   **训练细节**：在 IsaacLab 实验中，使用了 **1024 个并行环境**，总计训练 1 亿（100M）帧。在线 Off-policy 实验则训练了 100 万（1M）帧。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个大类基准、超过 18 个具体任务，涵盖了在线 Off-policy、在线 On-policy 和离线 RL 三种主流设置。
*   **消融实验**：非常充分。作者深入探讨了：
    1.  **动作维度**的影响（发现加权匹配方法在高维空间表现较差）。
    2.  **扩散步数**的影响（发现重参数化方法在步数多时反而性能下降）。
    3.  **网络骨干 (Backbone)** 的影响（对比了 MLP 和 SimBa）。
    4.  **噪声计划**（Cosine vs Linear）的影响。
*   **客观性**：通过在统一框架下对齐超参数和网络结构，排除了非算法因素的干扰，实验结果具有较高的客观性和公平性。

#### 6. 主要结论与发现
*   **无“银弹”算法**：没有一种扩散 RL 算法能在所有任务中绝对领先，但在 Gym 任务中 SDAC、DACER 和 DPMD 表现最强。
*   **离线 RL 优势**：扩散策略在离线 RL 中显著优于传统方法（如 IQL），证明了其捕捉多模态分布的能力。
*   **架构至关重要**：更换更先进的网络架构（如 SimBa）对性能的提升往往超过了算法本身的改进。
*   **维度敏感性**：依赖函数值评估的加权匹配方法在处理极高维动作空间时效率较低。

#### 7. 优点
*   **系统性强**：首次为扩散策略 RL 建立了清晰的分类学，理清了领域内杂乱的方法。
*   **工具链高效**：开源的 FlowRL 框架利用 JAX 解决了扩散模型训练慢的痛点，对社区贡献巨大。
*   **实验严谨**：通过控制变量法揭示了许多之前被忽视的工程细节（如 Backbone 的影响）。

#### 8. 不足与局限
*   **计算成本**：尽管有 JAX 加速，扩散策略的推理和训练开销仍显著高于传统的高斯策略，限制了其在超高频控制场景的应用。
*   **算法稳定性**：实验显示部分算法（如 FPO）在特定任务中存在训练崩溃的风险。
*   **任务局限**：实验主要集中在连续控制（Locomotion），未涉及稀疏奖励、长程规划或视觉输入等更复杂的 RL 场景。

（完）
