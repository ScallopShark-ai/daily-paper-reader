---
title: An Output Feedback Q-learning Algorithm for Optimal Control of Nonlinear Systems with Koopman Linear Embedding
title_zh: 一种基于Koopman线性嵌入的非线性系统最优控制输出反馈Q学习算法
authors: "Victor G. Lopez, Malte Heinrich, Matthias A. Müller"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29858v1"
tags: ["query:rl"]
score: 9.0
evidence: 非线性系统中Q学习的理论保证
tldr: 本研究针对非线性系统强化学习中理论保证较弱且过度依赖函数近似的问题，提出一种基于Koopman线性嵌入的输出反馈Q学习算法。该方法仅利用输入输出数据，无需系统模型或Koopman提升函数的先验知识，且不使用神经网络等近似技术，从而保留了与线性系统一致的强理论保证。此外，论文还分析了Koopman嵌入存在近似误差时的算法性能，并通过仿真验证了其有效性。
source: arxiv
selection_source: fresh_fetch
motivation: 解决非线性系统强化学习算法通常依赖函数近似且缺乏强理论保证的局限性。
method: 结合Koopman线性嵌入理论，采用仅依赖输入输出数据的输出反馈Q学习算法进行最优控制。
result: 在无需模型和提升函数信息的情况下，实现了具有线性系统级别理论保证的非线性控制。
conclusion: 该算法在Koopman嵌入为近似表示时仍具鲁棒性，为非线性系统提供了一种可靠的数据驱动控制方案。
---

## 摘要
在强化学习文献中，适用于线性时不变（LTI）系统的算法已获得了强大的理论保证。然而，在非线性情况下，主要依赖于函数逼近策略（例如神经网络）的算法仅获得了较弱的结果。在本文中，我们研究了一种已知的输出反馈Q学习算法在具有Koopman线性嵌入的一类非线性系统中的适用性。该算法仅使用输入输出数据，且不需要了解系统模型或Koopman提升函数。此外，该算法不使用函数逼近技术，并保留了与LTI系统相同的理论保证。此外，我们还分析了当Koopman线性嵌入仅为真实非线性系统的近似时，该算法的性能表现。仿真实例验证了该方法的适用性。

## Abstract
In the reinforcement learning literature, strong theoretical guarantees have been obtained for algorithms applicable to LTI systems. However, in the nonlinear case only weaker results have been obtained for algorithms that mostly rely on the use of function approximation strategies like, for example, neural networks. In this paper, we study the applicability of a known output-feedback Q-learning algorithm to the class of nonlinear systems that admit a Koopman linear embedding. This algorithm uses only input-output data, and no knowledge of either the system model or the Koopman lifting functions is required. Moreover, no function approximation techniques are used, and the same theoretical guarantees as for LTI systems are preserved. Furthermore, we analyze the performance of the algorithm when the Koopman linear embedding is only an approximation of the real nonlinear system. A simulation example verifies the applicability of this method.

---

## 论文详细总结（自动生成）

### 论文总结：基于 Koopman 线性嵌入的非线性系统输出反馈 Q 学习算法

#### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是如何为**非线性系统**设计具有**强理论保证**（如稳定性、收敛性和最优性）的强化学习（RL）控制算法。
*   **背景**：在线性时不变（LTI）系统中，Q 学习算法已有成熟的理论支撑。但在非线性领域，现有方法大多依赖神经网络（NN）等函数近似器，这导致理论分析困难，且往往只能得到次优解或局部稳定性。
*   **动机**：利用 **Koopman 理论**将非线性动力学嵌入到高维线性空间（KLE），从而将非线性控制问题转化为线性控制问题。作者旨在开发一种算法，既能处理非线性，又无需预先知道系统模型或复杂的“提升函数”（Lifting Functions），并保留 LTI 系统的高效性。

#### 2. 论文提出的方法论
核心思想是将 **Koopman 线性嵌入** 与 **数据驱动的输出反馈 Q 学习** 相结合。
*   **关键技术细节**：
    *   **Koopman 线性嵌入 (KLE)**：假设存在一组提升函数 $\Psi(x)$，使非线性系统在更高维空间表现为线性形式。
    *   **非最小状态构造**：由于 $\Psi(x)$ 未知且状态可能不可测，算法利用历史输入输出数据（滞后算子 $l$）构造一个等效的线性状态 $z_t$。
    *   **持续激励 (PE) 条件**：利用 Willems 基本引理的变体，通过输入输出数据的矩阵秩来验证数据是否包含足够的信息。
*   **算法流程**：
    1.  **数据采集**：收集系统的输入输出轨迹。
    2.  **构造状态空间**（Algorithm 1）：通过奇异值分解或矩阵变换，从原始数据中提取出能够表征系统动态的非最小状态 $z_t$。
    3.  **Q 学习迭代**（Algorithm 2）：在不需要模型参数的情况下，通过求解广义 Lyapunov 方程（贝尔曼方程）迭代更新控制增益 $K$。该过程具有**二次收敛率**，且每一步生成的策略都能保证系统稳定。

#### 3. 实验设计
*   **实验场景**：一个具有 3 维状态和 2 维输入的非线性多项式动力系统。该系统包含高次项（如 $x^3, x^5$），具有典型的非线性特征。
*   **Benchmark（基准）**：基于模型知识计算出的理论最优 LQR 控制器。
*   **对比方法**：
    *   **RL-NN1**：使用浅层神经网络近似代价函数和策略，但基函数选择不完全匹配系统特征。
    *   **RL-NN2**：使用包含系统真实提升函数的基函数进行神经网络训练（理想化的 NN 结构）。
    *   **QL（本文方法）**：完全不使用模型知识和提升函数信息。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提到**具体的 GPU 或 CPU 型号。
*   **训练时长**：文中给出了平均计算时间。本文算法（QL）单次运行约 **0.002 秒**；而对比的神经网络方法（RL-NN）由于需要大量迭代（10,000 次），耗时约 **7.6 至 9.1 秒**。这表明本文方法在计算效率上具有显著优势。

#### 5. 实验数量与充分性
*   **实验规模**：作者进行了 100 次随机初始条件的独立实验，并计算了平均代价误差。
*   **充分性评价**：
    *   **收敛性验证**：实验展示了算法在 10 次迭代内即可达到极高精度（误差接近 $10^{-16}$）。
    *   **对比公平性**：对比了 NN 方法在不同基函数下的表现，证明了即使 NN 拥有“完美”的基函数，其收敛速度和精度仍不如本文提出的解析解法。
    *   **局限性**：实验仅针对一个特定的多项式系统，缺乏在更复杂、更大规模或非精确 KLE 系统上的大规模消融实验。

#### 6. 论文的主要结论与发现
*   **理论一致性**：证明了对于存在 KLE 的非线性系统，可以应用 LTI 系统的输出反馈 Q 学习算法，且保留二次收敛和每步稳定的特性。
*   **无需先验知识**：算法不需要知道具体的 Koopman 提升函数，这解决了 Koopman 理论在实际应用中“寻找合适基函数”的痛点。
*   **鲁棒性潜力**：初步分析表明，当 KLE 只是系统的近似表示时，该算法等价于求解最小二乘意义下的线性近似最优控制，具有一定的鲁棒性。

#### 7. 优点
*   **强理论保证**：相比于黑盒 NN 方法，该方法提供了严格的稳定性证明和收敛率分析。
*   **极高效率**：计算开销极小，适合嵌入式或实时控制场景。
*   **数据驱动**：仅依赖输入输出数据，避开了非线性系统建模的难题。

#### 8. 不足与局限
*   **KLE 存在性假设**：算法前提是系统必须存在（或近似存在）Koopman 线性嵌入，对于某些高度奇异或不连续的非线性系统，该假设可能不成立。
*   **近似误差分析尚浅**：虽然讨论了 KLE 不精确时的情况，但缺乏深度的误差界限分析和针对大幅度建模偏差的补偿机制。
*   **实验覆盖面**：目前的仿真仅限于数值算例，缺乏在实际物理系统（如机器人、电力电子）上的验证。

（完）
