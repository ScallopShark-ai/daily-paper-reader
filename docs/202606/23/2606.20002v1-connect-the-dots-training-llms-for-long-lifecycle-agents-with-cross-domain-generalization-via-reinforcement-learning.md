---
title: "Connect the Dots: Training LLMs for Long-Lifecycle Agents with Cross-Domain Generalization Via Reinforcement Learning"
title_zh: 连结要点：通过强化学习训练面向长生命周期智能体的跨领域泛化大语言模型
authors: "Yanxi Chen, Weijie Shi, Yuexiang Xie, Boyi Hu, Yaliang Li, Bolin Ding, Jingren Zhou"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20002v1"
tags: ["query:gah"]
score: 9.0
evidence: 使用端到端强化学习训练LLM代理，通过长序列推演在环境中持续探索和学习，可直接用于游戏玩代理。
tldr: 在长期部署场景中，LLM驱动的智能体需要持续探索环境、积累经验并迭代更新自身上下文，以适应新任务。为此，提出Connect the Dots (CoD)框架，设计端到端RL算法与特定任务环境，训练LLM在长序列中交替解决任务和更新上下文。实验证明，该RL训练有效，LLM习得的元能力在训练域内、跨域及Ralph-loop场景下均展现出泛化性。相关工作连接多个研究线索，为发展更强大的LLM智能体提供了新方向，代码已开源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20002v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1658, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1608, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1523, \"height\": 1466, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1527, \"height\": 1463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1360, \"height\": 1272, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20002v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1561, \"height\": 1453, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 815, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 545, \"label\": \"Table\"}]"
motivation: 面向LLM智能体在长期部署中持续学习和自适应能力不足的问题。
method: 提出CoD框架，通过端到端RL训练LLM在长序列中交替执行任务和更新上下文，并使用细粒度信用分配。
result: 端到端RL训练有效，习得元能力在训练域内、跨域及Ralph-loop设定中均展现出泛化潜力。
conclusion: CoD框架连接多个研究线索，为LLM智能体发展开辟新机遇，并开源实现。
---

## 摘要
本研究提出了一个通用框架，用于训练大语言模型（LLMs）来“连结要点”（Connect the Dots，CoD），这是长生命周期智能体所需的一种元能力：当基于LLM的AI智能体部署在环境中时，它在持续探索环境的同时解决一长串任务，从自身经验中学习，并迭代式自我更新其对环境的上下文，从而在更新后的上下文中对未来任务取得逐步提升的性能。CoD框架的主要组成部分包括：（1）面向端到端强化学习（RL）的算法设计与基础设施，该RL具有交叉进行任务求解与上下文更新片段的长rollout序列；（2）用于在训练中激励和引出LLMs目标元能力的任务与环境，以及用于在评估中忠实衡量进展。我们给出了CoD框架的概念验证实现，包括一种带有细粒度信用分配的GRPO风格RL算法，以及针对目标元能力（而非特定领域的LLM能力或标准的逐任务RL）定制的任务和环境。实验结果验证了CoD设置下端到端RL训练的有效性，并展示了所引出的元能力在分布外泛化方面的潜力——包括训练领域内、跨不同领域以及从CoD到Ralph循环设置。我们的CoD研究连接了多个先前工作流，并为推进LLMs和AI智能体开辟了新的机会。为促进进一步的研究和应用，我们在 https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod 发布了实现。

## Abstract
This work presents a general framework for training large language models (LLMs) to "Connect the Dots" (CoD), a meta-capability required by long-lifecycle agents: as an LLM-based AI agent gets deployed in an environment, it solves a long sequence of tasks while continuously exploring the environment, learning from its own experiences, and iteratively self-updating its context about the environment, thereby achieving progressively better performance on future tasks conditioned on the updated context. Major components of the CoD framework include: (1) algorithm design and infrastructure for end-to-end reinforcement learning (RL) with long rollout sequences interleaving solve-task and update-context episodes; (2) tasks and environments for incentivizing and eliciting the targeted meta-capability in LLMs during training, as well as for faithfully measuring progress during evaluation. We present proof-of-concept implementations of the CoD framework, including a GRPO-style RL algorithm with fine-grained credit assignment, as well as tasks and environments tailored to the targeted meta-capability (rather than domain-specific LLM capabilities or standard task-by-task RL). Empirical results validate the efficacy of end-to-end RL training in the CoD setting, and demonstrate the potential for out-of-distribution generalization -- within the training domains, across different domains, and from CoD to Ralph-loop settings -- of the elicited meta-capability. Our investigation of CoD connects several lines of prior works, and opens up new opportunities for advancing LLMs and AI agents. To facilitate further research and applications, we release our implementations at \url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod}.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将使用中文、以Markdown形式，对给定的论文进行结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义

*   **研究动机与背景**：
    *   当前，大语言模型（LLM）正从聊天机器人、推理模型，逐渐演进为各种AI智能体（Agent）的核心。未来的趋势是实现**长生命周期智能体部署**，即智能体在环境中长期运行，通过持续学习和测试时自适应来提升性能。
    *   实现此愿景需要一种关键的**元能力：“连接要点”（Connect the Dots, CoD）**。它指智能体能在同一个环境中连续解决一系列不同但相关的任务，同时主动探索环境、从经验中学习，并迭代式**自我更新其对环境的“上下文”**，从而更有效地解决未来任务。
    *   目前的LLM在CoD元能力上存在明显短板，在信息不完备的环境中容易“迷失”，并过度依赖人类精心设计的复杂Agent脚手架。现有标准的**逐任务强化学习（task-by-task RL）** 本质上是训练LLM从零开始独立解决每个任务，这与长生命周期部署的目标不匹配。

*   **整体含义**：
    *   本文旨在提供一个**专门的框架（CoD）**，通过端到端强化学习来显式地训练LLM的CoD元能力。这不同于提升特定领域能力（如数学、编程），而是培养一种可跨领域泛化的“学会如何学习”和“自我进化”的高阶能力。

### 2. 论文提出的方法论

*   **核心思想**：
    *   定义了**CoD-Deploy**和**CoD-Train**两种模式。`CoD-Train`的Rollout模式与`CoD-Deploy`完全一致，即在一个环境中，交替进行“解决任务”和“更新上下文”的片段，形成一个长序列轨迹。这种设计比标准RL高一个层级，它将一个“任务序列”视为一条完整的RL轨迹。
    *   通过RL训练，使LLM不仅学会解决任务，还学会**如何生成有效的上下文（以文本形式，如“提示”）来帮助解决未来的任务**。

*   **关键技术细节与算法流程**：
    *   **信用分配（Credit Assignment）**：这是核心挑战。论文遵循动态规划原理，定义每个片段的回报为**当前奖励 + 未来所有解决任务片段的平均奖励**。对于更新上下文片段，其回报包含未来所有任务奖励；对于解决任务片段，其回报包含当前及未来任务奖励。
    *   **优势计算（Advantage Calculation）**：采用GRPO风格的算法。具体做法是：在同一任务序列的多个Rollout轨迹（一组G个）中，将处于**同一位置**（如，所有轨迹的第j个任务）的片段的回报求平均作为基线，然后用每个片段的回报减去该基线得到优势值。
    *   **训练稳定性增强**：为缓解训练不稳定，引入了一种**自适应重加权（re-weighting）启发式方法**。当批次（batch）的平均优势为负时，通过`exp(优势/温度)`对训练样本进行重加权，以平衡正负梯度，其中温度系数`T`通过二分搜索自适应确定。

### 3. 实验设计

*   **环境与数据集**：论文设计了三个专门用于衡量和训练CoD元能力的定制化模拟环境，而非使用标准的NLP/RL数据集。
    *   **FrozenLake-Obscure**：经典FrozenLake的变体，动作到方向的映射在环境中是随机且未知的。智能体必须通过探索自行发现并记录（更新上下文）这个映射，才能稳定解决后续任务。
    *   **Alchemy-Random**：一个合成元素的游戏，每个环境的合成配方是随机生成的。智能体需要在上下文中记录已发现的成功和失败组合，以高效合成目标元素。
    *   **TerminalSimulator**：一个终端命令模拟器，智能体需执行`scp`、`unzip`等命令来完成文件操作等日常任务。
*   **基准与对比方法**：
    *   **主要对比**：采用了两种RL训练设置。
        *   **设置A**：仅在FrozenLake-Obscure上进行CoD-Train。
        *   **设置B**：在FrozenLake-Obscure和Alchemy-Random的混合数据上进行CoD-Train。
    *   **算法对比**：在附录中对比了三种梯度计算方法——标准GRPO（含重要性采样权重）、`REC-OneSide-NoIS`（无重要性采样权重）以及最终采用的**无重要性采样权重自适应重加权算法**。
*   **评估指标**：
    *   **训练奖励**：在CoD-Train Rollout过程中，不同位置（即第0、1、2...个任务）的平均任务解决奖励。
    *   **泛化能力评估**：
        *   **分布外泛化（OOD）**：在**更困难的同类环境**（如更大的地图、更多的元素）和**更长的任务序列**（长度从4增至8）上进行评估。
        *   **跨领域泛化**：将在A环境下训练的模型直接在**未见过的其他领域环境**（如从FrozenLake-Obscure泛化到Alchemy-Random/TerminalSimulator）中进行评估。
        *   **Ralph-loop泛化**：评估模型在**重复解决同一个任务**的场景下的性能，观察其是否通过迭代自我改进而提升。

### 4. 资源与算力

*   论文中**未明确提及**所使用GPU的具体型号、数量以及总的训练时长。
*   文中提到了关键的训练超参数：**批次大小为32，Rollout组大小为8，学习率为1e-6，最大Token数为12k/32k**等，但未给出完整的算力清单。

### 5. 实验数量与充分性

*   **实验数量**：论文进行了较为系统性的实验，主要包含：
    *   **两种训练设置**（A和B）下的主实验。
    *   每种设置下，在**三个不同环境**（FrozenLake-Obscure、Alchemy-Random、TerminalSimulator）上的**训练域内、跨领域、Ralph-loop设置**的多种OOD评估。
    *   附录中关于**RL算法稳定性的消融实验**，对比了三种梯度计算方法。
*   **充分性与客观性**：
    *   **充分性**：实验设计针对CoD框架的核心主张（元能力学习与泛化）进行了全面的验证，覆盖了多种泛化场景，具有较好的说服力。
    *   **公平性与客观性**：论文坦诚地讨论了局限性，例如TerminalSimulator上的跨领域性能提升原因尚不明确（可能仅仅是通用上下文管理能力带来的好处），对实验结果的解读较为客观，没有过度吹嘘。

### 6. 论文的主要结论与发现

*   **端到端RL的有效性**：通过CoD-Train框架进行端到端强化学习，**能够有效引出并提升LLM的CoD元能力**。以FrozenLake-Obscure为例，从头解决任务的奖励从0.18提升至0.45，但在序列中第4个任务的奖励从0.28大幅提升至0.76，证明智能体学会了利用自身上下文。
*   **元能力的泛化潜力**：训练得到的CoD元能力展现出显著的泛化能力。
    *   **分布内泛化**：能处理训练时未见过的更困难环境实例和更长任务序列。
    *   **跨领域泛化**：从FrozenLake-Obscure学到的探索和上下文更新能力，可以迁移至逻辑完全不同的Alchemy-Random和TerminalSimulator环境中。
    *   **模式泛化**：元能力也能泛化到Ralph-loop设置，即在重复解决同一任务时实现自我提升。
*   **与标准RL的区别**：CoD-Train与标准任务层面RL是**互补而非替代**关系，类似于流体智力与晶体智力的关系。CoD专注于提升“学会学习”的流体智力。

### 7. 优点

*   **问题定义新颖且重要**：准确定义了“连接要点”（CoD）这一长生命周期智能体的核心元能力，区别于一般的任务解决能力，问题视角深刻。
*   **方法论框架清晰**：`CoD-Deploy`和`CoD-Train`的抽象定义清晰，将探索、利用、上下文更新整合到一个端到端的RL层次化训练框架中，为后续研究提供了范本。
*   **细粒度的信用分配**：提出的基于“同一位置”平均奖励的细粒度优势计算方法，解决了长序列RL中的信用分配难题，优于简单求和奖励的粗粒度方法，这是实现有效训练的关键。
*   **验证环境设计精妙**：`FrozenLake-Obscure`和`Alchemy-Random`等环境设计精巧，通过隐藏关键信息制造了“信息论上限”，迫使智能体必须学会探索和上下文管理才能成功，能真实地衡量CoD元能力。
*   **全面的泛化性验证**：通过分布内、跨领域、Ralph-loop三种场景的OOD评估，有力地证明了所学能力是一种可迁移的元能力，而非死记硬背特定环境。

### 8. 不足与局限

*   **算力细节缺失**：论文未公开实验所需的计算资源和训练时长，使其成本效益和复现可行性难以评估。
*   **训练稳定性问题**：混合多领域训练时，奖励曲线出现波动和不稳定现象。尽管提出了自适应重加权方法，但该方法的启发式性质表明RL算法仍有很大的优化空间。
*   **上下文机制过于简单**：当前实现的“上下文”仅为一段动态更新的文本提示，方式较为原始。作者也承认，未来需要探索更复杂的机制，如持久记忆库或技能文件。
*   **跨领域泛化机理不明**：在`TerminalSimulator`上的跨领域性能提升的根本原因尚不清楚，可能是通用能力带来的间接增益，削弱了“跨领域CoD元能力”这一论断的纯粹性。
*   **环境相对简单**：当前验证环境均为模拟游戏或简化模拟器，与复杂的、真实的、用于生产的智能体部署场景（如长期项目管理、个人助手终身学习）仍有很大差距。从游戏环境到真实环境的泛化仍是巨大的挑战。
*   **模型规模与基础限制**：实验仅基于8B参数的Qwen3模型，且关闭了思考（thinking）模式。CoD框架在更大模型、结合推理能力时的表现，以及其上限如何，均有待探索。

（完）
